// The pipeline step's command is set in the Buildkite UI, for every branch at
// once, as `node .buildkite/ci.mjs`. This keeps that command working; the
// pipeline is generated by ci.ts.
import "./ci.ts";
