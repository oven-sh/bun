# Codex Defensibility Corrections — 2026-05-16

This file records corrections applied after the registry reached quiet
convergence. It exists to keep the public artifacts internally consistent and
to separate *current verdicts* from *historical phase notes*.

**Live totals after the latest correction in this file:** 106 EXP entries; 70
`CONFIRMED_UB`; 17 `NO_EVIDENCE`; 17 `DEFERRED`; 2 `RESOLVED`; 0 `OPEN`; 0
`NEEDS_REFINEMENT`. Earlier checkpoint counts in this log are historical
snapshots from before the EXP-047 safe-boundary correction, EXP-099..104
receiver-reentry promotions, EXP-106 PipeWriter promotion, EXP-107/108/110
callback-receiver sweep promotions, EXP-111 bundler Send/Sync promotion,
EXP-109 root-graph demotion, and other verdict cleanups.

## Corrections Applied

1. **Ast-grep sweep review**
   - Updated `CODEX_AST_GREP_SWEEP_REVIEW_2026-05-16.md` to reference the raw
     detector dumps that the audit ran (`phase3_raw/codex_ast_grep_ub_patterns_2026-05-16.txt`
     and the near-duplicate at `codex_sweeps/`). Both raw dumps were dropped
     from the published PR to keep the diff reviewable; the triaged digest in
     `CODEX_AST_GREP_SWEEP_REVIEW_2026-05-16.md` preserves every actionable
     signal and the dumps can be regenerated by rerunning the skill's ast-grep
     ruleset against `origin/main@4d443e5402`.
   - Replaced stale "EXP-017 was kept OPEN" wording with the current
     `NO_EVIDENCE` production-source verdict plus regression-guard framing.
   - Replaced "OPEN / NEEDS_REFINEMENT caveat" language with current caveat
     categories (`DEFERRED`, `NO_EVIDENCE`, source-specific follow-up).

2. **EXP-017 volatile/data-race phase notes**
   - Updated `phase2_findings_16_volatile.md` and
     `phase2_findings_07_data_races.md` so they no longer contradict the Phase
     5 source-overlap closure.
   - Current wording: the primitive Miri race model is real, but no current Bun
     source path rewrites `callback` after queue publication; registry verdict
     is `NO_EVIDENCE` for production UB.

3. **Strict-provenance status normalization**
   - Updated `phase8_remediation_plan.md` so EXP-020 / EXP-029 / EXP-048 are
     described as `DEFERRED` strict-provenance release-gate migrations, not
     `NEEDS_REFINEMENT` production-UB proof gaps.

4. **Phase-4 table status note**
   - Updated `phase4_unified_findings.md` to state explicitly that the large
     table preserves historical row statuses for traceability.
   - The then-current verdict totals were delegated to the registry and final
     report:
     58 `CONFIRMED_UB`, 0 `OPEN`, 0 `NEEDS_REFINEMENT`, 14 `NO_EVIDENCE`, 16
     `DEFERRED`, 1 `RESOLVED` across 89 EXP entries.
   - Item 22 below supersedes those totals after EXP-094/EXP-095 were
     integrated: 60 `CONFIRMED_UB` across 91 EXP entries.

5. **Convergence appendix**
   - Updated `FINAL_UB_REPORT.md` from "through round 46" to the then-current
     tracker round. Later items supersede this with round 61 after
     EXP-094/095 and the final wording/count cleanup:
     `OPEN=0`, `NEEDS_REFINEMENT=0`, `CONFIRMED_UB=60`, `DEFERRED=16`,
     `prev_quiet=true`.

6. **Late EXP source/log spot check**
   - Added `CODEX_LATE_EXP_SPOTCHECK_2026-05-16.md`.
   - Spot-checked EXP-073..084 against current source and raw Miri/TB logs.
   - No demotions required, but the file records the important framing limit:
     several late EXPs are generic safe-API / unsafe-contract defects, not
     claimed production crash paths.

7. **`&self -> &mut` sweep**
   - Added `CODEX_MUT_FROM_REF_SWEEP_2026-05-16.md`.
   - Found 70 textual hits across `src/**/*.rs`.
   - Did not inflate the confirmed-UB count: tied the broad R-2 queue back to
     existing confirmed witnesses (EXP-057/079/083/084) and documented which
     hits are honest `unsafe fn` boundaries, private source-disciplined helpers,
     or lint/remediation targets.

8. **Validity-API sweep**
   - Added `CODEX_VALIDITY_API_SWEEP_2026-05-16.md`.
   - Promoted the prior unsafe-audit P3-BC-001 `fmt::Raw` UTF-8 contract bug
     into the UB registry as EXP-085 after a Miri witness over invalid bytes.
   - Updated `FINAL_UB_REPORT.md`, `phase4_unified_findings.md`,
     `phase8_remediation_plan.md`, and `UB_RUNBOOK.md` from 50/80 to 51/81
     where they describe current registry totals.
   - Corrected stale final-report `OPEN` / `OPEN-CLUSTER` labels for F-A-2 and
     F-21-2; historical `OPEN` labels are now explicitly confined to
     `phase4_unified_findings.md` traceability rows.

9. **Send/Sync follow-up sweep**
   - Added `CODEX_SEND_SYNC_SWEEP_2026-05-16.md`.
   - Corrected `phase2_findings_08_send_sync.md` stale row counts: current
     `origin/main` has 157 textual unsafe Send/Sync impl rows, not 115 total
     (115 concrete + 42 generic).
   - Updated the Phase-2 Send/Sync doc's current-status overlay so EXP-018,
     EXP-045, and EXP-047 were then treated as live `CONFIRMED_UB` entries,
     while the two private/function-local generic `SendPtr<T>` rows stayed
     hardening-only. Item 133 later corrects EXP-047 to hardening /
     `NO_EVIDENCE` as project UB.
   - No new EXP was promoted; this was artifact-defensibility work.

10. **Unchecked-intrinsics sweep**
    - Added `CODEX_UNCHECKED_INTRINSICS_SWEEP_2026-05-16.md`.
    - Promoted `src/bun.rs:1582-1586` `pub fn unsafe_assert(bool)` to
      EXP-086 after a Miri witness showed `unsafe_assert(false)` reaches
      `unreachable_unchecked`.
    - Framed it narrowly: no current in-tree callers, so this is a safe helper
      contract defect, not a production call-path claim.
    - Reviewed other `unreachable_unchecked` / `unwrap_unchecked` families and
      left them under existing entries (NEW-V-4, EXP-084) or hardening-only
      local assertions.

11. **Safe-API contract sweep**
    - Added `CODEX_SAFE_API_CONTRACT_SWEEP_2026-05-16.md`.
    - Promoted the previously under-demoted F-L-6 row,
      `src/bundler/ThreadPool.rs:414-428,629-652`
      `ThreadPool::get_worker(&self, id) -> &'static mut Worker`, to EXP-087.
    - Miri Tree-Borrows confirmed the exact safe duplicate-call witness:
      two calls for the same worker cell produce two live `&mut Worker`s, and
      the second write is rejected after the first write disables the tag.
    - Framed it narrowly as a safe-API contract defect. Current production
      call-site reachability remains caller-discipline-dependent; the artifact
      does not claim a proven bundler crash path.
    - Updated `FINAL_UB_REPORT.md`, `UB_RUNBOOK.md`, `phase8_remediation_plan.md`,
      `phase4_unified_findings.md`, and `phase2_findings_15_lifetimes_escape.md`
      to 53 `CONFIRMED_UB` / 83 registry entries at the time of the safe-API sweep.

12. **Alignment/provenance follow-up sweep**
    - Added `CODEX_ALIGNMENT_PROVENANCE_SWEEP_2026-05-16.md`.
    - Promoted the Phase-2 UTF-16 reinterpretation cluster to EXP-088 after a
      source-shaped Miri witness proved `E::String::init_utf16` narrows a
      `2 * len_u16` backing byte range to `len_u16` bytes, then `slice16()`
      re-expands it as `len_u16` UTF-16 code units.
    - Updated `FINAL_UB_REPORT.md`, `UB_RUNBOOK.md`, `phase8_remediation_plan.md`,
      `phase4_unified_findings.md`, and `phase2_findings_03_alignment.md` to
      54 `CONFIRMED_UB` / 84 registry entries at the time of the
      alignment/provenance sweep.

13. **Uninit scratch-buffer follow-up sweep**
    - Added `CODEX_UNINIT_SCRATCH_SWEEP_2026-05-16.md`.
    - Corrected Phase-2 Anti-pattern C: `MaybeUninit::uninit().assume_init()`
      for `[u8; N]`, `[u16; N]`, or `[u32; N]` is immediate invalid-value UB,
      not merely a read-before-write hazard. "Every bit pattern is valid" is
      not enough; the integer elements must be initialized.
    - Promoted `PathBuffer::uninit`, `WPathBuffer::uninit`, and
      `depth_buf_uninit` to EXP-089 with a default-Miri invalid-value witness.
    - Updated `FINAL_UB_REPORT.md`, `UB_RUNBOOK.md`, `phase8_remediation_plan.md`,
      `phase4_unified_findings.md`, and `phase2_findings_05_uninit.md` to
      55 `CONFIRMED_UB` / 85 registry entries at the time of the
      uninit-scratch sweep.

14. **Type-punning layout follow-up sweep**
    - Added `CODEX_TYPE_PUNNING_LAYOUT_SWEEP_2026-05-16.md`.
    - Rechecked `src/sys/linux_syscall.rs:198-209`
      `rustix::fs::Stat -> libc::stat`, because the source const assert proves
      only size/align, not field offsets.
    - Added `experiments/CODEX-stat-layout/` with compile-time size/align and
      public-field-offset assertions; `cargo check` passes for both
      `x86_64-unknown-linux-gnu` and `aarch64-unknown-linux-gnu`, the two
      targets enabled by Bun's fast path.
    - Kept the verdict as sound, but corrected the Type-Punning phase note so
      it cites the field-offset witness instead of implying the source's
      size/align assert alone proves full layout. No registry/count changes.

15. **Set-len falsification probe**
    - Added EXP-090 as `NO_EVIDENCE` for the tempting
      `src/http/h3_client/encode.rs:58-107` prefix-`set_len` hypothesis.
    - A source-shaped Miri probe (`experiments/EXP-090`) shows the current
      no-Drop `quic::Header` vector shape completes cleanly: set the length to
      reserve a prefix, push later headers, then assign the prefix before any
      read.
    - Updated `phase2_findings_05_uninit.md` to keep the site in the sound
      bucket with a stronger caveat. Registry totals are now 55
      `CONFIRMED_UB` / 86 entries, with 14 `NO_EVIDENCE`.
    - Reran the convergence tracker; latest stored result is round 53 at the
      time of this item, later superseded by round 54 with the same registry
      verdict totals after stale-status cleanup.
      `OPEN=0`, `NEEDS_REFINEMENT=0`, and `prev_quiet=true`.

16. **Provenance phase-note status normalization**
    - Corrected `phase2_findings_02_provenance.md` and
      `phase3_dynamic_findings.md` so EXP-020/EXP-029 no longer say
      `NEEDS_REFINEMENT`.
    - Current registry status is `DEFERRED`: these are strict-provenance
      release-gate migrations with complete witnesses, not default-Miri UB and
      not unresolved proof gaps.
    - Refreshed the `phase2_findings_08_send_sync.md` overlay and
      `phase4_unified_findings.md` table-status note to point at the then-current
      round-53 totals instead of older 51/81 or 52/82 snapshots. Item 18 later
      moved the same status note to round 54.

17. **Zeroed-validity follow-up sweep**
    - Added `CODEX_ZEROED_VALIDITY_SWEEP_2026-05-16.md`.
    - Re-read every direct `core::mem::zeroed` site plus the
      `bun_core::ffi::Zeroable` wrapper bodies. No new EXP was added: current
      direct sites are C ABI POD / out-param storage or marker-trait-gated
      wrappers.
    - Clarified that `EVP_MD_CTX: Zeroable` is not a proven zero-validity bug:
      the Rust mirror is all-zero-valid as a Rust value (byte-array union + raw
      pointers). The real issue is BoringSSL C/Rust layout drift hardening under
      EXP-063.
    - Corrected `phase4_unified_findings.md` F-10-4/F-10-5 from `OPEN` to
      `DEFERRED` so the unified table matches the registry semantics:
      EXP-063 already owns these as remediation-design hardening, not unresolved
      UB proof obligations.
    - Fixed `FINAL_UB_REPORT.md`'s stale live-total paragraph from
      `13 NO_EVIDENCE` / `85` to `14 NO_EVIDENCE` / `86` and added EXP-090 to
      the late-additions sentence.

18. **Unified-table stale `OPEN` status sweep**
    - Reconciled phase-4 rows whose open status was stale relative to existing
      registry evidence:
      F-S-16 is now `CONFIRMED_UB-SHAPE` under EXP-010; F-S-26 is
      `REVIEWED-SUBSUMED` by F-S-32/EXP-080; F-21-2 is
      `REVIEWED-SUBSUMED` under the EXP-012/S4 propagation vehicle; F-21-9 is
      `DEFERRED` under EXP-070; F-NF6-1 is `DEFERRED` under EXP-064 /
      EXP-051's checked-bit-pattern shape.
    - Re-read `src/perf/tracy.rs:705-798` and all `dlsym::<T>` call sites:
      every current monomorphisation is a `tracy_fns::*` unsafe extern fn
      pointer. Demoted F-NF6-2 from `OPEN` to `REVIEWED-HARDENING`; the
      remaining work is a compile-time typed-dlsym gate, not a live UB claim.
    - Left the two allocator rows F-NF20-2/F-NF20-3 open intentionally at the
      time of that sweep. Item 19 below supersedes the F-NF20-3 half after a
      source-faithful safe-generic-API witness; F-NF20-2 remains the only
      non-registry allocator follow-up row still open in the Phase-4 table.
    - Reran the convergence tracker; latest stored result at that time was
      round 54 with the same registry verdict totals and `prev_quiet=true`.

19. **EXP-091 bindgen layout promotion**
    - Re-read `src/jsc/bindgen.rs:235-353` and promoted F-NF20-3 from an
      allocator follow-up hypothesis into registry entry EXP-091.
    - Added `experiments/EXP-091`, a source-faithful safe-generic-API witness:
      `BindgenArray<Child>::convert_from_extern` can return a `Vec<ZigType>`
      over storage allocated as `Vec<ExternType>` when element sizes match but
      alignments differ.
    - Miri rejects the returned vector's drop with an incorrect deallocation
      layout. This is stronger than a production reachability claim: the safe
      public generic API shape itself is unsound unless reuse is gated on
      exact allocation-layout equality, or the conversion allocates fresh
      storage.
    - Updated `FINAL_UB_REPORT.md`, `phase4_unified_findings.md`,
      `phase8_remediation_plan.md`, and `UB_RUNBOOK.md` to 56
      `CONFIRMED_UB` / 87 registry entries. The round-52/53/54 historical
      rows in the final report now match their actual JSON files (55
      confirmed before EXP-091), and round 55 records the EXP-091 promotion.
    - Normalized stale Phase-4 table labels that could be mistaken for live
      registry proof obligations: F-A-2 is now a `DEFERRED-VEHICLE`,
      F-S-28 is `REVIEWED-FOLLOW-UP`, and F-21-4 is `SUBSUMED-FOLLOW-UP`.
      At this point F-NF20-2 was the only remaining literal `OPEN` row in the
      Phase-4 table; item 20 below supersedes that.

20. **EXP-092 `ReadResult::to_stream` owned-slice promotion**
    - Re-read `src/runtime/webcore/streams.rs:2533-2597` and promoted
      F-NF20-2 from a producer-discipline follow-up into registry entry
      EXP-092.
    - Added `experiments/EXP-092`, a source-shaped safe-API witness: safe code
      creates a raw slice pointer to stack memory, calls the safe
      `ReadResult::to_stream(...)` mirror with a different `buf`, and receives
      `StreamResult::Owned(Vec<u8>)`.
    - Miri rejects the returned vector's drop with heap deallocation of stack
      memory. This proves pointer inequality from `buf` is not an ownership
      proof.
    - Updated `FINAL_UB_REPORT.md`, `phase2_findings_20_alloc_pairing.md`,
      `phase4_unified_findings.md`, `phase8_remediation_plan.md`, and
      `UB_RUNBOOK.md` to 57 `CONFIRMED_UB` / 88 registry entries at that time
      (item 21 supersedes this with EXP-093). The Phase-4 table now has no
      literal `OPEN` rows.

21. **EXP-093 PE alignment promotion**
    - While cleaning stale `EXP-022` references, found that the Phase-2
      Bucket-3 PE alignment candidate had never been carried into the canonical
      registry after EXP-022..025 were intentionally left unused.
    - Added `EXP-093`, `experiments/EXP-093`, and
      `phase5_experiment_results/EXP-093.log`.
    - Miri symbolic-alignment rejects the source-shaped
      `Vec<u8> -> *const SectionHeader -> &[SectionHeader]` construction with
      an odd section-header offset.
    - Updated source-of-truth totals to 58 `CONFIRMED_UB` / 89 registry
      entries and convergence round 57 at that time. Item 22 supersedes this
      count after EXP-094/EXP-095 were integrated.

22. **EXP-094 / EXP-095 late evidence integration**
    - Integrated the Phase-11 path-b Miri hit on
      `bun_core::deprecated::DoublyLinkedList<T>` as `EXP-094` / F-014b.
      This is a production unit-test failure under Miri, not a standalone-only
      witness: re-minting `&mut` references to already-linked nodes disables
      the raw-pointer tags that the list later traverses.
    - Re-read the Mach-O sibling of EXP-093 and promoted the unaligned command
      mutation tail to `EXP-095` / F-004d. `LoadCommand::cast<T>()` is sound
      because it returns an owned value through `read_unaligned`; the defect is
      `macho.rs:121-130, 361-403`, where command/section bytes are materialised
      as `&mut T` / `&mut [T]`.
    - Added `experiments/EXP-095` and
      `phase5_experiment_results/EXP-095.log`; Miri symbolic-alignment rejects
      `&mut *cmd_ptr.cast::<SymtabCommand>()` with an unaligned-reference
      error.
    - Updated source-of-truth totals to 60 `CONFIRMED_UB` / 91 registry
      entries. Later final-report cleanup reran convergence through round 61;
      item 34 records the subsequent round-62 row-count correction.

23. **Bucket-3 alignment wording correction**
    - Re-read the remaining lower-confidence Bucket-3 items after the Mach-O
      promotion.
    - Corrected `phase2_findings_03_alignment.md` so
      `INotifyWatcher::Event` is framed as hardening-only under current source
      invariants: `EventListBytes` is `#[repr(C, align(4))]`, the file asserts
      `align_of::<Event>() == 4`, and Linux `inotify(7)` documents padding of
      names so subsequent records land on a suitable boundary.
    - Kept `md/containers.rs` as a missing compile-time guard, not a current
      UB finding: current `BlockHeader` and `VerbatimLine` both align to 4;
      the useful fix is `const _: () = assert!(align_of::<VerbatimLine>() <=
      align_of::<BlockHeader>())`.
    - No registry verdict changed; convergence remained quiet; later
      final-report cleanup reran the tracker through round 61; item 34 records
      the subsequent round-62 row-count correction.

24. **Phase-2 placeholder / stale-status cleanup**
    - Reconciled Phase-2 bucket notes with the settled 91-entry experiment
      registry. Removed stale `EXP-NEW-*` placeholder IDs and "recommended
      EXP" / "still pending" language from the bucket artifacts.
    - Updated Bucket 11 panic-safety rows at that time: NF-1 → EXP-038 /
      `NO_EVIDENCE`, NF-2 → EXP-039 / `CONFIRMED_UB`, NF-5 → EXP-040 /
      `NO_EVIDENCE`. Correction 50 supersedes the NF-2 verdict after applying
      the same panic-abort policy check to EXP-039.
    - Updated Bucket 14 const-mutation rows: F14-A/B/C now map directly to
      EXP-041/042/043, F14-D/E/F to EXP-075/076/074, and EXP-011 is correctly
      described as `CONFIRMED_UB`.
    - Updated Bucket 6 type-punning rows: native-plugin `BunLoader` now maps
      to EXP-051; `PropertyIdTag` is tracked as F-NF6-1 / EXP-064 vehicle;
      Tracy `dlsym` remains reviewed hardening, not an unregistered EXP.
    - Updated Bucket 2 strict-provenance rows: TaggedPtr /
      StringOrTinyString / ZigString reproducer families map to EXP-048 /
      EXP-049 / EXP-050 and are `DEFERRED` release-gate migrations, not
      unresolved proof work. Item 112 later split out the separate
      `SmolStr.rs` representation as EXP-096.
    - Updated Phase-0 metadata and the Phase-1 AO review so the bundler
      parallel-callback cluster no longer says its dynamic witness is pending;
      it now points at EXP-010 / `CONFIRMED_UB`.
    - No registry verdict changed; registry lint still passes. Later
      final-report cleanup reran convergence through round 61; item 34 records
      the subsequent round-62 row-count correction.

25. **Late-stage count / bead-handoff / soak-plan cleanup**
    - Earlier late-stage cleanup attempted to correct Phase 4 / Final Report /
      Runbook row-count references. Item 34 first superseded the numeric part
      of that cleanup; item 40 later superseded item 34 after a fresh parse.
      The then-current live unified table was 168 rows = 156 `F-*` rows + 12
      `NEW-*` rows. Item 112 later added F-P-17 / EXP-096, bringing the live
      table to 169 rows.
    - Corrected the final-report strong-negative headline from a stale "14"
      count to 17 strong-negative subsystem/bucket review rows.
    - Removed the last literal `NEEDS_REFINEMENT` transition notes from
      CONFIRMED_UB rows F-006 / F-008 / F-009, and renamed the F-S-27
      severity from `DEFENSIBLE-WITH-OPEN-PROOF` to
      `DEFENSIBLE-WITH-SEQUENCING-EVIDENCE`.
    - Tightened EXP-094 wording from "production test failure" to "in-tree
      unit-test failure" across the registry and Phase 4; this preserves the
      strength of the evidence without implying a production runtime trace.
    - Replaced EXP-094's too-absolute "Falsifiability: None" with a
      source-change falsifiability condition (delete/redesign the intrusive
      list or prevent caller re-minting of `&mut node` while raw links exist).
    - Softened the UB runbook CI heading from "would catch every current
      CONFIRMED_UB" to "Required high-yield coverage gates" and added an
      explicit note that safe-API contract findings still need per-EXP
      regression tests / compile-fail checks.
    - Fixed dangling old-path references to the real
      current path, `src/runtime/jsc_hooks.rs`, across Phase 2, Phase 4, the
      registry, final report, and runbook.
    - Clarified that EXP-071's JSON output and the proposed N-API layout-dump
      helper are
      future generated artifacts / helper scripts, not files that exist in the
      current workspace.
    - Added a current-status correction to Phase 9: the local `.beads/`
      graph has 174 beads and covers 58 confirmed EXPs through EXP-093, but
      the current 60-confirmed registry also contains EXP-094 and EXP-095.
      A complete bead graph needs a Phase-9 refresh adding R/T/D triplets for
      EXP-094 and EXP-095.
    - Reframed Phase 11 SOAK from "closing OPEN entries" to post-convergence
      regression / broad-coverage soak campaigns, since the current registry
      has 0 `OPEN` and 0 `NEEDS_REFINEMENT` entries.
    - No registry verdict changed; registry lint still passes and the source
      tree remains untouched outside audit artifacts.

26. **Registry stale-witness wording cleanup**
    - Removed internal contradictions where settled Phase-5 registry entries
      still said "TBD", "placeholder", or "no loom model on file" even though
      the same entries already contained Miri/loom result notes.
    - Updated EXP-030, EXP-031, EXP-032, EXP-040, EXP-044, EXP-052, and
      EXP-053 to name their actual implemented witness/model paths.
    - Reframed deferred remediation-design EXPs (EXP-061, EXP-063, EXP-064,
      EXP-066, EXP-069, EXP-070, EXP-071) as "design witness to build" while
      preserving the linter-required `Minimal reproducer` field.
    - Corrected the EXP-037 on-disk witness path to `experiments/EXP-038/`
      and `phase5_experiment_results/EXP-038.log`, matching the existing
      Phase-5 executor numbering note.
    - Reworded "3 open holes" in the EXP-012 propagation cluster to
      "3 remaining callback holes" in the registry, final report, Phase 4,
      and Phase 6 idea-wizard notes. These are unresolved remediation targets,
      not `OPEN` registry verdicts.
    - Normalized initial seeded witness comments from `repro.rs` to
      `src/main.rs`, matching the actual cargo project layout under
      `experiments/EXP-*`.
    - Corrected the final-report evidence count from "110 EXP logs plus 3
      Codex layout-check logs" to "110 total `.log` files, including 3 Codex
      layout-check logs"; the directory count is exactly 110.
    - No registry verdict changed; registry lint still passes.

27. **Backticked path-reference audit**
    - Ran a path checker over UB artifact Markdown to validate concrete
      backticked references against the current checkout and the run
      workspace: 2,630 concrete references checked, 0 missing after cleanup.
    - Corrected Section C shorthand paths from the old test/parallel
      prefix and test/Scanner.rs to the actual current locations under
      `src/runtime/cli/test/...`.
    - Corrected Section T / provenance wording that referenced absent local
      vendor trees. The current checkout has the libuv binding header at
      `src/jsc/bindings/libuv/uv.h`, and no local vendor/libuv or
      vendor/mimalloc tree.
    - Reworded future or upstream-only paths (EXP-010-fix, N-API layout-dump
      helper, upstream compiler_builtins chkstk source files) so they are not
      presented as local files that should already exist.
    - No registry verdict changed. This is artifact hygiene / click-through
      defensibility only.

28. **Strict-provenance / libuv function-pointer demotion alignment**
    - Aligned `phase2_findings_02_provenance.md` with the later Phase-4/5
      result for F-P-14 / the Section-T libuv function-pointer round-trip.
      Earlier Phase-2 wording still called the witness `LIKELY-UB`; the
      current evidence supports portability / typed-callback hardening only
      on supported targets.
    - Updated the Phase-2 count sentence from "16 strict-provenance failures"
      to "15 strict-provenance failures plus 1 portability-hardening site",
      explicitly excluding F-P-14 from counted live UB.
    - No registry verdict changed; this removes stale severity language from
      an intermediate artifact.
    - Also removed a contradictory "5-site int-to-pointer reconstruction
      family" label from the final report. The paragraph listed EXP-029 /
      EXP-048 / EXP-049 / EXP-050 plus ten F-P sites, so it now describes the
      strict-provenance reconstruction family without an impossible site
      count.

29. **EXP-051 remediation-plan supersession**
    - Fixed public-facing artifacts that still recommended the v1 "Grok
      coexistence" plan for EXP-051 (`output_loader` marked `unsafe fn` plus
      `try_output_loader`). Phase 8 v2 overturned that plan because
      safe-fn-to-unsafe-fn is also a source-compatibility break and still does
      not eliminate misuse of the legacy method.
    - `FINAL_UB_REPORT.md` and `UB_RUNBOOK.md` now recommend option D:
      keep `output_loader(&self) -> BunLoader` source-compatible, remove the
      transmute via checked conversion, add
      `try_output_loader -> Result<BunLoader, InvalidLoader>`, and deprecate
      the legacy method with migration guidance.
    - Added a superseded note to `phase8_triangulation_v1.md` so readers know
      its EXP-051 recommendation is historical and overridden by
      `phase8_triangulation.md`.

30. **EXP-013 signal-safety wording precision**
    - Re-read the current POSIX signal path (`handle_segfault_posix` directly
      calls `crash_handler`) and the source-callgraph log. The contract claim
      remains defensible: the installed handler reaches locks, output
      flushing/formatting, stack/report setup, path lookup, and reload
      machinery.
    - Tightened registry wording from standalone `format_args!` to `write!` /
      formatting call sites. The macro itself is not the operation at issue;
      the non-async-signal-safe behavior is the formatting/write path reached
      from the handler.

31. **EXP-094 remediation wording precision**
    - Re-read `src/bun_core/deprecated.rs:114-410` and the Phase-11 Miri log.
      The finding remains confirmed: Bun's in-tree
      `basic_doubly_linked_list_test` fails under Miri because the list stores
      raw links whose SB tags are invalidated when the test re-mints `&mut`
      references to already-linked stack nodes.
    - Tightened the registry remediation text. The previous wording said an
      `intrusive-collections` replacement would have the "same C ABI shape";
      this type is an internal Rust intrusive-list API, not a C ABI contract.
      The corrected claim is "same intrusive-list role" with pinned/list-owned
      aliasing discipline.

32. **OPEN-row wording precision**
    - Re-ran the registry and convergence counts from the experiment registry:
      91 entries = 60 `CONFIRMED_UB`, 14 `NO_EVIDENCE`, 16 `DEFERRED`,
      1 `RESOLVED`; round 61 agrees with `OPEN=0` and
      `NEEDS_REFINEMENT=0`.
    - Tightened wording from "literal `OPEN` rows" to "status-column `OPEN`
      rows" in the final report / registry / phase-4 table. The old phrase
      was semantically intended but grep-hostile because
      `OPENSSL_memory_free` appears in a reviewed row.

33. **EXP-085 safe-constructor citation precision**
    - Re-read `src/bun_core/fmt.rs:724-731` and `:3744-3749`.
      The finding remains confirmed: safe `fmt::s(bytes)` returns `Raw(bytes)`,
      and `Raw`'s safe `Display` implementation calls
      `core::str::from_utf8_unchecked` on those bytes.
    - Expanded citations in the registry, Phase-4 table, and Phase-8
      remediation plan to include both the `Raw` display body and the safe
      `fmt::s` constructor. This makes the safe-API boundary explicit instead
      of forcing reviewers to infer it from the title.

34. **Phase-4 unified-row count correction**
    - Mechanically re-counted the live `phase4_unified_findings.md` unified
      table between `## Unified findings table` and `## Structural fix points`.
      This intermediate pass counted 167 data rows; item 40 supersedes that
      count after a later row was added/clarified.
    - Corrected then-stale `168` / `156 F-* + 12 NEW-*` references in the final
      report and Phase-4 footer. Item 40 restores the current 168-row count.
      Registry verdict counts did not change: 91 EXP entries = 60
      `CONFIRMED_UB`, 14 `NO_EVIDENCE`, 16 `DEFERRED`, and 1 `RESOLVED`.
    - Added `phase7_convergence_round_62.json` to record the post-correction
      quiet state explicitly.

35. **EXP-091 / EXP-092 late allocator-pairing source re-check**
    - Re-read `src/jsc/bindgen.rs:10-18, 235-353`, the EXP-091 Miri witness,
      and the remediation plan. The finding remains confirmed: the second
      conversion branch can return a `Vec<ZigType>` over storage allocated as
      `Vec<ExternType>` when element size fits but deallocation alignment
      differs. The source's mimalloc-layout rationale is insufficient for the
      Rust `Vec::from_raw_parts` safety contract.
    - Re-read `src/runtime/webcore.rs:404` and
      `src/runtime/webcore/streams.rs:2533-2597`. The EXP-092 safe-API claim
      remains correctly scoped: `streams` is a `pub mod`, and both
      `ReadResult` and `to_stream` are `pub`; the audit does not rely on a
      private helper being maliciously called from one function.

36. **EXP-072 caller-citation expansion**
    - Ran a mechanical simple-citation health check over the live source-of-truth
      artifacts. It found no real out-of-range source line drift, but the
      HiveArray row used brace-compressed paths such as
      `src/install/PackageManager/{...}` that are not literal file paths.
    - Expanded those citations in Phase 4, the final report, and the runbook to
      concrete files:
      `src/install/PackageManager/PackageManagerEnqueue.rs`,
      `src/install/PackageManager/runTasks.rs`,
      `src/runtime/server/server_body.rs`, `src/runtime/server/mod.rs`,
      `src/runtime/bake/DevServer.rs`, and
      `src/runtime/api/bun/h2_frame_parser.rs`.

37. **Phase-4 EXP cross-reference status clarity**
    - Ran a registry-vs-Phase-4 status check for rows with a single direct
      `EXP-*` token. Five apparent mismatches were cross-references or
      subcases, not verdict errors.
    - Tightened the affected rows so reviewers can see the ownership of each
      verdict: F-A-8 is partial (`EXP-045` confirmed, SendPtr siblings reviewed);
      F-DR-12 and F-S-30 are contextual cross-references; F-L-5 feeds the
      deferred EXP-070 tooling vehicle; F-21-4 is a follow-up subcase under
      the already-confirmed EXP-060 raw-handle bug.

38. **EXP-072 raw-slot owner/type precision**
    - Re-checked all 8 EXP-072 caller lines against source field/type
      definitions, not just `.get()` text. The callers resolve through
      `PackageManager.rs:320-321`, `PackageManager.rs:427-428`,
      `RequestContext.rs:115-123`, `DevServer.rs:448-449`, and
      `h2_frame_parser.rs:1233-1236` to `HiveArrayFallback` pools.
    - Corrected over-compressed wording that described all 4 deprecated methods
      as `HiveArrayFallback` methods. The exact method set is
      `HiveArray::get` plus `HiveArrayFallback::{get, try_get,
      get_and_see_if_new}`.
    - Replaced install-specific `preallocated_*` labels on runtime rows with
      the real pool names/types (`request_pool`, `deferred_request_pool`,
      `H2FrameParserHiveAllocator`).

39. **EXP-013 signal-safety count and formatter wording**
    - Re-read `src/crash_handler/lib.rs:878-1342`, the Section-U step table,
      `src/bun_core/output.rs:2069-2077`, and the EXP-013 source audit.
      The POSIX signal-safety finding remains confirmed and correctly scoped
      to the POSIX signal path, not the Rust panic hook or Windows VEH.
    - Corrected brittle wording that said "8 of 14 dispatched steps" violate
      the whitelist. Section U's table has 9 non-whitelisted audited steps;
      the registry now says "at least 8 distinct operation classes" and notes
      the 9-of-14 step count.
    - Removed over-specific "allocating String" wording for
    `Output::pretty_fmt_args`; the source builds a `Cow::Owned` runtime
    template and then runs ordinary `Display` formatting, which is enough
    to be non-async-signal-safe without relying on a `String` claim.

40. **Phase-4 row-count consistency**
    - Re-ran a mechanical parse of `phase4_unified_findings.md`: 168 data
      rows total, made of 156 `F-*` rows and 12 `NEW-*` rows.
    - Corrected stale `155 + 12 = 167` wording left over from an earlier
      convergence round. The final report headline already said 168; this
      change aligns the Phase-4 footer, the v2 note, and the convergence
      appendix with the current parse.
    - Verdict counts did not change: 60 `CONFIRMED_UB`, 14 `NO_EVIDENCE`,
      16 `DEFERRED`, 1 `RESOLVED`, 0 `OPEN`, 0 `NEEDS_REFINEMENT`.

41. **Post-round-68 strict-provenance and late-finding wording audit**
    - Re-checked the strict-provenance cluster against source and Miri logs:
      `src/url/lib.rs:340-351` (EXP-020), `src/runtime/shell/EnvStr.rs:188-200`
      (EXP-029), `src/ptr/tagged_pointer.rs:53-64` (EXP-048),
      `src/bun_core/string/immutable.rs:1076` (EXP-049), and
      `src/bun_alloc/lib.rs:925-946` (EXP-050). All strict-provenance
      witnesses remain real, but the prior "one helper closes F-P-1..F-P-12"
      language was too broad. `TaggedPtr::{get,to}` directly covers EXP-048 /
      F-P-4 and the true `TaggedPtrUnion` callers; the other F-P rows are
      separate custom packers, FFI numeric-pointer boundaries, or layout-only
      rows requiring their own remediation.
    - Reworded EXP-048/049/050 severity labels from generic
      `MUST-BE-UB (strict-provenance)` / `LIKELY-UB (strict-provenance)` to
      `STRICT_PROVENANCE_FAIL`, preserving the release-gate importance without
      overclaiming today's default Rust execution model.
    - Corrected late stale phrases across the final report, runbook, Phase 4,
      Phase 6, Phase 8, Phase 9, and Phase 11 artifacts: `as_stream_mut` is
      actually `UvStream::as_stream(&mut self)`, libuv `HandleType` has 18
      discriminants (`UV_UNKNOWN_HANDLE`, 16 map entries, `UV_FILE`), and the
      crash-handler finding is "9 of 14 audited steps / at least 8 operation
      classes" rather than a brittle "8 of 14 dispatched steps" claim.
    - Tightened source-specific wording for EXP-038, EXP-040, EXP-053,
      EXP-059, and EXP-095 so the artifacts describe the exact witness:
      AnyTaskJob uses the on-disk EXP-039 harness; the S3 trip hazard occurs
      during `AsyncHTTP::init` argument evaluation before the write into
      `task.http`; UvHandle bypasses are through `as_handle_mut`, `as_stream`,
      and direct casts; the leaked mutex guard witness fires at construction /
      return; and the Mach-O mutation witness is the `MachoFile::init -> Vec`
      edit path, not `MachoSigner::init(&[u8])`.
    - Re-ran the registry linter and stale-phrase grep after these changes.
      Registry counts did not change: 91 EXP entries, 60 `CONFIRMED_UB`, 14
      `NO_EVIDENCE`, 16 `DEFERRED`, 1 `RESOLVED`, 0 `OPEN`, 0
      `NEEDS_REFINEMENT`.

42. **EXP-033 source-faithfulness repair and EXP-037 current-source demotion**
    - Re-read `src/threading/channel.rs:81-142, 208-242` and found the
      original EXP-033 Miri witness was not source-faithful enough: Bun's
      `Channel` impl is currently `T: Copy`, while the witness used a
      non-`Copy` `DropCounter` to prove drop-on-uninitialized-slot UB. The
      underlying issue remains real, but the correct source-faithful witness is
      validity-bearing `Copy`, not `Drop`. Rewrote the EXP-033 harness to use
      `T = bool`; Miri now reports uninitialized memory at
      `std::hint::black_box(slice[0])` after the code materializes
      `&mut [bool; 1]` over `[MaybeUninit<bool>; 1]`.
    - Re-read `src/watcher/WindowsWatcher.rs:55, 196-211` and found EXP-037
      was stale against current `origin/main`: the live code already matches
      `FILE_NOTIFY_INFORMATION.Action` as a raw `DWORD` and skips unknown
      action codes. The old `transmute` Miri witness is a valid negative
      pattern, but not evidence of current Bun UB. Demoted EXP-037 from
      `CONFIRMED_UB` to `RESOLVED` and converted downstream references to
      regression-guard language.
    - Updated the final report, runbook, soak designs, Phase-2 validity sweep,
      Phase-4 unified table, Phase-6 idea-wizard, Phase-9 bead log, and
      convergence tracker totals. Live registry totals are now 91 EXP entries:
      59 `CONFIRMED_UB`, 14 `NO_EVIDENCE`, 16 `DEFERRED`, 2 `RESOLVED`, 0
      `OPEN`, 0 `NEEDS_REFINEMENT`.
    - Tightened EXP-042 / Bucket-14 wording that had claimed a forged `&mut`
      was "instant UB even if never written." The source/log-backed claim is
      now narrower and defensible: the function hands out a mutable-reference
      capability derived from `&T`, and Miri Tree Borrows rejects the first
      mutation through that forged reference.

43. **Phase-1 JSC Send/Sync inventory supersession notes**
    - Re-read the high-risk generic safe-API entries EXP-046, EXP-056,
      EXP-057, EXP-060, EXP-072, EXP-077, EXP-082, EXP-083, EXP-084, and
      EXP-087 against current `origin/main` source and their Miri / compile
      logs. The current registry wording is appropriately caveated: several
      findings are confirmed generic safe-API or unsafe-contract defects, not
      proven production races at every call site.
    - Found stale Phase-1 JSC inventory language that still labeled
      `WorkTask<C>` and `ConcurrentPromiseTask<C>` as `PRESENT_STRONG` /
      "raw ptrs are inert." Later EXP-046 evidence overturned that Phase-1
      confidence: the context traits lack `Send`, the owned
      `ConcurrentPromiseTask` wrapper is the stronger shape, and the
      `WorkTask` raw-pointer wrapper remains per-context unsafe-contract debt.
      The same stale Phase-1 table also marked `JsCell<T>`, `VirtualMachine`,
      and `Blob` as `PRESENT_STRONG`; later EXP-045 / EXP-084 / EXP-082
      supersede those labels at the generic safe-API level. Patched
      `phase1_inventory_K.md` and `phase1_notes/K_jsc.md` with explicit
      `SUPERSEDED_BY_EXP-*` annotations.
    - Reworded historical Phase-6 / Phase-10 notes that said the registry max
      was `EXP-060` so readers understand that was true only at the time of
      those passes; the current registry extends to `EXP-095` with gaps.
    - Ran a fresh `unsafe impl<...> Send/Sync` detector against current source:
      42 generic rows, matching `phase2_findings_08_send_sync.md`. No new
      uncatalogued generic Send/Sync UB class surfaced; the live risky entries
      remain the already-mapped EXP-019 / EXP-045 / EXP-046 / EXP-047 cluster,
      with SendPtr-like siblings correctly classified as hardening unless a
      concrete bad instantiation appears.

44. **Unchecked-intrinsics inventory boundary clarification**
    - Re-ran the current-source `unreachable_unchecked` search and compared it
      to the earlier `CODEX_UNCHECKED_INTRINSICS_SWEEP_2026-05-16.md`.
      NEW-V-4 intentionally tracks the **suspicious exhaustiveness subset**
      (`dispatch.rs`, `js_bundle_completion_task.rs`, and `jsc/generated.rs`),
      not every current call to `unreachable_unchecked`.
    - Patched `phase2_findings_04_validity.md` and
      `phase4_unified_findings.md` so readers cannot mistake the nine NEW-V-4
      sites for a complete inventory. The extra live calls are now explicitly
      accounted for: `src/bun.rs:1585` is EXP-086 / NEW-V-6, while
      `atomic_cell.rs:317`, `bundler/transpiler.rs:1926`,
      `MiniEventLoop.rs:311`, `PackageManagerTask.rs:284,542`, and
      `install/lockfile/Tree.rs:1131` remain reviewed-but-not-promoted local
      assertions or hardening targets.
    - No verdict-count change: this is a defensibility/wording correction, not
      a new or demoted experiment.

45. **Evidence-location precision for EXP-094**
    - Ran a registry/evidence consistency scan over all 59 `CONFIRMED_UB`
      entries. Every confirmed entry has either a standalone experiment/log or
      an explicitly documented source-only / OS-contract evidence path.
    - The only confirmed entry without an `EXP-094*.log` under
      `phase5_experiment_results/` is EXP-094, because its evidence is stronger
      but located elsewhere: the Phase-11 full-workspace Miri path-b log
      `phase11_artifacts/miri-leaf/cargo_miri_workspace_sp_nofailfast.log`
      contains Bun's own `basic_doubly_linked_list_test` failure.
    - Patched `FINAL_UB_REPORT.md` so it no longer implies EXP-094's raw
      evidence is in the Phase-5 log directory. The source-of-truth artifact
      list already had the more precise wording; this correction aligns the
      Honesty Section with it.

46. **Phase-9 bead log live-count and TaggedPtr scope cleanup**
    - Re-read `phase9_beads_log.md` after the EXP-037 demotion and
      strict-provenance scope cleanup. Two stale phrases remained in the bead
      handoff artifact:
      - "Current registry count is 60 after EXP-094 / EXP-095 were added" was
        true before EXP-037 was resolved, but live count is now 59.
      - The S2 structural-fix row still implied a central `TaggedPtr` rewrite
        covered F-P-4/7..12. It now states the defensible scope: EXP-048 /
        F-P-4 plus true `TaggedPtrUnion` callers only; F-P-7..12 need separate
        remediation.
    - No bead files were edited and no source files were touched. This is
      artifact handoff correction only.

47. **Citation and Phase-4 cross-reference integrity pass**
    - Ran a citation-integrity script over the registry, Phase 4 table, final
      report, runbook, and remediation plan. No out-of-range Bun source line
      references were found. Apparent "missing" paths were experiment-local
      stack traces (`src/main.rs`), Rust stdlib stack traces
      (`core/src/cell.rs`), or package paths such as
      `packages/bun-native-plugin-rs/src/lib.rs` that the quick `src/...`
      extractor intentionally misclassified.
    - Cross-checked Phase-4 table `EXP-ID` cells against live registry verdicts.
      The mismatches were intentional non-owning cross-references (shape
      cousins, remediation vehicles, or "confirmed elsewhere" cluster notes),
      not stale verdicts. Patched `phase4_unified_findings.md` with an explicit
      column-contract note so future readers/scripts do not treat every
      `EXP-###` token in that column as the row's own verdict.

48. **Phase-10 current-status refresh**
    - Ran a stale-language sweep over final-facing artifacts for `TBD`,
      `pending`, `OPEN`, `NEEDS_REFINEMENT`, and related terms. Most hits are
      legitimate historical convergence-table entries or source TODO citations.
    - Found one live-status drift in `phase10_fresh_eyes_log.md`: the final
      "Current status" paragraph still called round 61 the current quiet state.
      Patched it to round 70 with live totals
      (`CONFIRMED_UB=59`, `NO_EVIDENCE=14`, `DEFERRED=16`, `RESOLVED=2`).
      Correction 50 subsequently updated those live totals to
      (`CONFIRMED_UB=58`, `NO_EVIDENCE=15`, `DEFERRED=16`, `RESOLVED=2`).

49. **Upstream main drift guard**
    - Fetched `origin/main` on 2026-05-16 and found it had advanced from the
      audited base `4d443e5402` to `e750984db6`.
    - The new commit range includes `e520065ebb Harden 36 reachable security
      findings across runtime, install, parsers, http (#30722)`, with
      substantial Rust changes (`31 files changed, 716 insertions, 118
      deletions` under `src` / `packages/bun-native-plugin-rs` for that commit).
    - Added `CODEX_MAIN_DRIFT_NOTE_2026-05-16.md` and patched the final report,
      registry, and runbook so the 59-confirmed count is explicitly scoped to
      `origin/main@4d443e5402`, not latest fetched `origin/main@e750984db6`.
      Latest-main claims require a W4 already-mature refresh pass. Also
      replaced registry phrases of the form "current main `4d443e5402`" with
      "audited base `4d443e5402`" for the seeded-anchor reconfirmation notes.
    - Added `CODEX_W4_REFRESH_TRIAGE_2026-05-16.md` to identify the first
      refresh targets. The broad diff touches 686 Rust/package files and
      intersects 77 EXP entries, but the material hardening commit
      `e520065ebb` touches 31 Rust files and intersects six confirmed entries
      first: EXP-011, EXP-039, EXP-057, EXP-072, EXP-082, and EXP-088 (plus
      EXP-017 / EXP-020 as non-confirmed guard/deferred rows).

50. **EXP-039 panic-policy + source-scope correction**
    - Re-read `src/runtime/socket/Listener.rs` on both audited base
      `4d443e5402` and latest fetched `origin/main@e750984db6`.
    - Corrected the EXP-039 four-site overclaim. Only the two `listen()` sites
      (`:235` and `:317`) perform allocation-prone `take_protos()` before
      `core::mem::forget(socket_config)`. The connect-path sites (`:1069` and
      shifted `:1296`) do `Option::take()` immediately before `mem::forget`;
      their `take_protos()` calls happen later after `socket_config` has been
      forgotten.
    - Applied the same conceptual rule already used for EXP-038: Bun's current
      dev/release/release-derived/shim profiles set `panic = "abort"`, and
      `src/bun_core/lib.rs:2701-2707` / `src/crash_handler/lib.rs:1797-1804`
      document that panics abort before unwinding. Therefore the EXP-039 Miri
      witness is a real unwind-model regression guard, but it should not be
      counted as current production UB.
    - Patched the registry, Phase-2 Bucket-11 notes, Phase-4 unified row,
      Phase-8 remediation plan, Phase-10 fresh-eyes log, runbook, W4 triage,
      and final report. Live totals are now 58 `CONFIRMED_UB`, 15
      `NO_EVIDENCE`, 16 `DEFERRED`, 2 `RESOLVED`.

51. **W4 direct-diff read for `e520065ebb` intersections**
    - Re-read the exact hunks in `e520065ebb Harden 36 reachable security
      findings across runtime, install, parsers, http (#30722)` for the six
      confirmed-entry intersections identified in the W4 triage.
    - Updated `CODEX_W4_REFRESH_TRIAGE_2026-05-16.md` with a direct-diff table:
      EXP-011, EXP-057, EXP-072, EXP-082, and EXP-088 remain live on latest
      fetched `origin/main@e750984db6`; the commit touched adjacent or
      same-file security logic but not the UB anchor. EXP-039 was not fixed by
      the commit either, but is now `NO_EVIDENCE` for current production UB due
      to correction 50.
    - Also recorded that EXP-017 remains `NO_EVIDENCE` and EXP-020 remains
      `DEFERRED` after the same diff read.

52. **Pinned-base wording cleanup after EXP-039 demotion**
    - Corrected `CODEX_MAIN_DRIFT_NOTE_2026-05-16.md` from the stale
      59-confirmed checkpoint to the live 58-confirmed registry total.
    - Replaced misleading "current main" / old prior-audit branch labels in
      Bucket-3 alignment, Bucket-4 validity, Bucket-8 Send/Sync, Bucket-17
      async-drop, and Section-E inventory notes with pinned
      `origin/main@4d443e5402` wording.
    - Tightened Section E's Listener row so the `:235/:317` panic-prone sites
      are distinguished from the connect-path move-out sites that do not share
      EXP-039's proof.
    - Normalized the remaining Phase-1 mapper headings that said "current
      main" to pinned `origin/main@4d443e5402` wording. This prevents the
      Section C/D/H/I/M/O/P/Q workload counts from being mistaken for a
      latest-`origin/main` W4 refresh.

53. **W4 refresh expansion beyond `e520065ebb`**
    - Re-read `f7c692ae9c` and recorded it as a post-run upstream fix for an
      additional `ResolvedSource` / `SourceProvider` refcount bug that was
      present at audited base but not promoted into the registry. This is a
      credibility correction: latest main is fixed, and the registry should
      not pretend the audit found that bug first.
    - Added the same W4 honesty note to `FINAL_UB_REPORT.md` so the main public
      artifact cannot be quoted as an exhaustive historical-bug count without
      the upstream-fixed miss caveat.
    - Re-read `8438ff7baa` intersections for EXP-026, EXP-044, EXP-079, and
      EXP-084. The resolver split / typed extern-Rust pointer cleanup does not
      close those entries; the source shapes remain present on latest fetched
      `origin/main@e750984db6`.

54. **EXP-019 / EXP-021 source-faithfulness recheck**
    - Re-read `src/ast/nodes.rs` around `StoreRef`, `StoreStr`, and
      `StoreSlice<T>`. The current audited source still has the source shapes
      the registry claims: safe lifetime-erasing constructors, safe reborrow
      methods with caller-chosen lifetimes, `Deref` / `AsRef` over the erased
      slice, and unconditional `unsafe impl<T> Send` / `unsafe impl<T> Sync`
      for `StoreSlice<T>`.
    - Re-ran the standalone Miri witnesses:
      `experiments/EXP-019` still reports a data race through `Cell<u32>` via
      the unbounded `Sync` impl, and `experiments/EXP-021` still reports
      `pointer not dereferenceable: alloc194 has been freed` after a safe
      `StoreSlice::new(&Vec)` escapes its backing allocation.
    - Verified the registry numbering note is still internally consistent for
      the then-current ID range: EXP-022..EXP-025 are unused in the canonical
      registry, the old DirectoryWatchStore witness was renumbered to EXP-028,
      and the stale PE alignment candidate was promoted to EXP-093. Later
      correction item 149 adds the separate EXP-105 non-canonical support-model
      slot.

55. **EXP-080 public-dispatch-handle forgery recheck**
    - Re-read `src/dispatch/lib.rs`: `link_interface!` still emits a public
      handle with `pub kind` and `pub owner`, while the surrounding crate docs
      and generated `unsafe fn new<T>` explicitly name `owner is a live *mut T
      matching kind` as the invariant-establishing boundary.
    - Re-read the concrete `CompletionHandle` analogue in
      `src/bundler/bundle_v2.rs`; that hand-written handle also exposes public
      erased owner/vtable fields and safe methods that dispatch through the
      vtable contract.
    - Re-ran `experiments/EXP-080` under Miri. The safe forged handle still
      reaches `memory access failed: attempting to access 1 byte, but got null
      pointer`, confirming the registry's "safe public fields bypass unsafe
      constructor" claim remains source-faithful.

56. **EXP-086 `unsafe_assert(false)` contract recheck**
    - Re-read `src/bun.rs:1582-1586`: `unsafe_assert(condition: bool)` remains
      a safe public function whose false branch calls
      `core::hint::unreachable_unchecked()`.
    - Re-ran the current reachability grep. The only `unsafe_assert(` hit in
      `src/` is the definition, so the registry's careful wording is correct:
      this is a safe-API contract defect and dormant helper, not a proven
      production caller.
    - Re-ran `experiments/EXP-086` under Miri. It still reports `Undefined
      Behavior: entering unreachable code` when safe code passes `false`.

57. **EXP-091 / EXP-092 allocator-ownership recheck**
    - Re-read `src/jsc/bindgen.rs:235-353`. The unsafe fast path still reuses
      an `ExternArrayList<Child::ExternType>` allocation for
      `Vec<Child::ZigType>` when `size_of::<ZigType>() <=
      size_of::<ExternType>()` and `align_of::<ZigType>() <=
      MI_MAX_ALIGN_SIZE`; it does not require the eventual `Vec` deallocation
      layout to match the original element alignment. The in-source comment
      relies on mimalloc ignoring layout, which does not satisfy Rust's
      `Vec::from_raw_parts` contract.
    - Re-ran `experiments/EXP-091`; Miri still reports `incorrect layout on
      deallocation: alloc209 has size 8 and alignment 8, but gave size 8 and
      alignment 4`.
    - Re-read `src/runtime/webcore/streams.rs:2533-2597`. `ReadResult::Read`
      remains a public safe enum variant over `*mut [u8]`, and safe
      `to_stream()` still treats pointer inequality from `buf` as ownership
      transfer into `Vec::from_raw_parts`.
    - Re-ran `experiments/EXP-092`; Miri still reports deallocation of stack
      memory via Rust heap deallocation. The registry's distinction remains
      correct: these are safe generic/API shape defects even if current
      production producers are more disciplined.

58. **EXP-089 uninitialised primitive-array construction recheck**
    - Re-read `src/bun_core/util.rs:997-1003`, `src/bun_core/util.rs:1045-1050`,
      and `src/install/lockfile/Tree.rs:87-91`. The functions still construct
      `PathBuffer([u8; N])`, `WPathBuffer([u16; N])`, and `DepthBuf([u32; N])`
      with `MaybeUninit::uninit().assume_init()`, and suppress
      `invalid_value` / `uninit_assumed_init` warnings.
    - Re-ran `experiments/EXP-089` under Miri. It emits the same compiler
      warnings and then aborts at construction time: `constructing invalid
      value of type PathBuffer: at .0[0], encountered uninitialized memory,
      but expected an integer`.
    - This confirms the registry's conceptual correction: the issue is not a
      later read-before-write hazard; creating an initialized `[u8; N]` /
      `[u16; N]` / `[u32; N]` from fresh uninitialized storage is already UB.

59. **Residual latest-main wording cleanup**
    - Replaced remaining ambiguous "current main" phrasing in the Phase-1 Q
      review, GuardedLock source-faithful audit, Send/Sync sweep, and bundler
      notes with explicit audited-base / W4 spot-check wording.
    - Directly checked `origin/main@e750984db6` for the two claims that now
      mention latest-main status: `src/url/lib.rs` still uses integer
      pointer reconstruction in `host_with_path`, and
      `src/threading/guarded.rs` still lacks the `_not_send` guard marker.
    - Directly checked the latest fetched bundler JS-chunk worker path: the
      code still forms `&mut LinkerContext` and `&mut Chunk` from raw pointers
      before calling `generate_compile_result_for_js_chunk_impl`, although
      later cleanup commits changed comments and may shift line numbers. The
      notes now say "same reborrow shape" rather than overclaiming identical
      file:line on latest main.
    - Also replaced two generic registry falsifiability references to
      "current main" with "checked source revision" / "audited-base successor"
      wording, and corrected Phase 11's setup log to call `4d443e5402` the
      audited-base main commit rather than "current main HEAD."

60. **EXP-084 VirtualMachine safe TLS trap recheck**
    - Re-read `src/jsc/VirtualMachine.rs:604-688`: `VirtualMachine` still has
      `unsafe impl Sync` and `unsafe impl Send`; safe `get()` returns
      `&'static VirtualMachine`; safe `as_mut(&self)` and `get_mut()` route
      through `get_mut_ptr()`, whose release path calls
      `get_or_null().unwrap_unchecked()` after only a debug assertion.
    - Re-ran `experiments/EXP-084` with `cargo +nightly miri run --release`.
      Miri still reports `Undefined Behavior: entering unreachable code` on
      the spawned non-VM thread at `unwrap_unchecked()`.
    - The registry wording is appropriately narrow: it proves an unsound safe
      Rust API boundary, not that a production worker currently captures
      `&VirtualMachine` and calls `as_mut()` off-thread.

61. **EXP-079 `Transpiler::env_mut(&self)` aliasing recheck**
    - Re-read `src/bundler/transpiler.rs:260-266`: safe
      `env_mut(&self) -> &'a mut dot_env::Loader<'a>` still returns `&mut`
      from a shared receiver by dereferencing the raw `self.env` pointer.
    - Re-ran `experiments/EXP-079` under Tree Borrows. The existing log
      reproduces exactly: the second `env_mut()` tag is disabled by the
      foreign write through the first borrow, then the write through the
      second borrow is rejected.
    - Also ran the default Stacked-Borrows configuration while spot-checking:
      it fails even earlier/differently, because the second unique retag
      invalidates the first tag and the first write then fails. This reinforces
      the contract finding rather than changing the registry verdict.

62. **EXP-085 `fmt::Raw` invalid UTF-8 recheck**
    - Re-read `src/bun_core/fmt.rs:724-736` and `:3744-3749`: `Raw<'a>` is a
      safe `pub` wrapper over `&[u8]`, `raw()` / `s()` are safe constructors,
      and `Display` still calls `core::str::from_utf8_unchecked(self.0)`.
    - Re-ran `experiments/EXP-085` under Miri. The invalid `0xff` byte reaches
      `core::str::next_code_point` and Miri reports `Undefined Behavior:
      entering unreachable code`.
    - The registry wording is precise: production reachability is per-call-site
      and should not be overstated, but the generic safe formatter contract is
      unsound for arbitrary caller bytes.

63. **EXP-088 UTF-16 EString narrowed-provenance recheck**
    - Re-read `src/ast/e.rs:1413-1424` and `:1449-1459`. `init_utf16` still
      casts `&[u16]` to bytes and stores only the first `data.len()` bytes in
      `Str`, while `slice16()` later calls `from_raw_parts(ptr.cast::<u16>(),
      self.data.len())`, expanding the access back to `2 * len` bytes.
    - Re-ran `experiments/EXP-088` with
      `MIRIFLAGS=-Zmiri-symbolic-alignment-check`. Miri still reports a retag
      failure at offsets `[0x0..0x4]` because the `Str` tag was created only
      for offsets `[0x0..0x2]`.
    - This confirms the corrected framing: the strongest issue is the
      lying-length/provenance expansion, not merely a generic UTF-16 alignment
      suspicion.

64. **EXP-087 `ThreadPool::get_worker(&self)` duplicate-`&mut` recheck**
    - Re-read `src/bundler/ThreadPool.rs:414-428` and
      `Worker::get(ctx)` at `:629-652`: `get_worker(&self, id)` still returns
      `&'static mut Worker` from a raw pointer stored in an internally locked
      map, and `Worker::get` exposes that detached mutable reference to safe
      callers.
    - Re-ran `experiments/EXP-087` under Tree Borrows. Miri still reports
      `write access through <245> ... is forbidden` after two safe
      `get_worker()` calls create coexisting mutable handles to the same
      worker.
    - The registry distinction remains correct: production reachability is
      per-caller, but the safe API contract is unsound while repeated calls can
      materialize duplicate `&'static mut Worker` references.

65. **EXP-014 `MultiArrayList::Slice<T>: Copy` aliasing recheck**
    - Re-read `src/collections/multi_array_list.rs:560-574`: the source still
      contains the explicit "Known soundness gap" comment, still implements
      `Copy` for `Slice<T>`, and still tells callers to avoid overlapping
      mutable views.
    - Re-ran `experiments/EXP-014` under Tree Borrows. Miri still rejects the
      second mutable column write (`right[0] = 20`) because the right view's
      tag was disabled by the earlier write through the left view.
    - This remains a counted UB-shape finding, not just a style issue, because
      the local API permits the modeled copy-and-mutably-reborrow sequence and
      the source itself names real bundler borrowck exploiters.

66. **EXP-093 / EXP-095 executable-format alignment recheck**
    - Re-read `src/exe_format/pe.rs`: the helper comments still acknowledge
      Zig's `*align(1)` model, while `get_section_headers`,
      `get_section_headers_mut`, and `init` still cast byte-buffer offsets to
      `SectionHeader` references / slices.
    - Re-ran `experiments/EXP-093` with symbolic alignment checking. Miri still
      rejects `slice::from_raw_parts(ptr.cast_const(), 1)` as constructing an
      unaligned `&[SectionHeader]`.
    - Re-read `src/exe_format/macho.rs`: the iterator reads command headers
      with `read_unaligned`, but `update_load_command_offsets` then casts the
      byte-backed command pointer to typed mutable references such as
      `&mut macho::symtab_command`.
    - Re-ran `experiments/EXP-095` with symbolic alignment checking. Miri
      still rejects `&mut *cmd_ptr.cast::<SymtabCommand>()` as an unaligned
      typed mutable reference. The registry's fix direction is right:
      mutate local read-unaligned copies and write them back unaligned, or
      prove/copy into aligned storage first.

67. **EXP-045 / EXP-047 unbounded Sync-cell recheck**
    - Re-read `src/jsc/JSCell.rs:126-128`: `JsCell<T>` still has unbounded
      `unsafe impl<T> Sync` and `unsafe impl<T> Send` while exposing safe
      `new` / `get`.
    - Re-ran `experiments/EXP-045`; Miri still reports a data race between the
      main-thread write and the worker-thread `Cell::<u32>::get`.
    - Re-read `src/bun_core/atomic_cell.rs:503-504` and
      `src/bun_core/util.rs:2276-2277`: `ThreadCell<T>` / `RacyCell<T>` still
      have unbounded `Sync` impls. The `RacyCell` comment is explicit that
      `Cell<U>` / `Rc<U>` / `RefCell<U>` payloads must not be wrapped, which
      is a contract outside the type system.
    - Re-ran `experiments/EXP-047`; Miri still reports the `Cell<u32>` data
      race admitted by the unbounded `Sync` impl. Item 133 later corrects the
      interpretation: that witness uses caller-side `unsafe` raw-pointer deref,
      so it remains hardening evidence rather than a confirmed Bun safe-API UB.

68. **EXP-082 / EXP-083 cross-thread safe-method race recheck**
    - Re-read `src/jsc/webcore_types.rs:60-96` and `:220-231`: `Blob` still
      stores `global_this: Cell<*const JSGlobalObject>`, still declares both
      `unsafe impl Send` and `unsafe impl Sync`, and still exposes safe
      `global_this(&self) -> Option<&JSGlobalObject>`.
    - Re-ran `experiments/EXP-082`; Miri still reports a data race through the
      modeled JS-thread-only `Cell` reached via safe `Blob::global_this()` from
      two threads.
    - Re-read `src/runtime/shell/IOWriter.rs:237-252,969-985` and
      `src/runtime/shell/IOReader.rs:72-100,220-268`: both types still store
      mutable state in `UnsafeCell`, still declare `Send` and `Sync`, and expose
      safe `&self` methods (`enqueue`, `start`, `add_reader`, `remove_reader`)
      that call private helpers returning `&mut State` / `&mut ReaderImpl`.
    - Re-ran `experiments/EXP-083`; Miri still rejects the concurrent safe
      `enqueue(&self)` reborrow through `UnsafeCell<State>`.
    - Registry wording remains appropriately scoped: the generic safe API
      contract is confirmed unsound; production live paths still require
      per-caller classification.

69. **Public-summary convergence/count refresh**
    - Found final-facing artifacts still referring to earlier quiet snapshots
      (`round 70` / `round 73` / `round 77`) or stale `59 CONFIRMED_UB` /
      `14 NO_EVIDENCE` counts after the EXP-039 panic-policy demotion.
    - Updated `FINAL_UB_REPORT.md`, `phase4_unified_findings.md`,
      `phase2_findings_08_send_sync.md`, and `phase10_fresh_eyes_log.md` so
      current-count statements all point at convergence round 78:
      58 `CONFIRMED_UB`, 15 `NO_EVIDENCE`, 16 `DEFERRED`, 2 `RESOLVED`,
      0 `OPEN`, 0 `NEEDS_REFINEMENT`.
    - Left historical table rows that explicitly describe older rounds intact;
      they are part of the convergence history, not current-count claims.

70. **EXP-073 / EXP-074 / EXP-075 / EXP-076 shared-provenance event-loop cluster recheck**
    - Re-read `src/runtime/webcore/blob/copy_file.rs:1005,1300,1580,1666`:
      `CopyFileWindows` still stores `event_loop: &EventLoop`, then
      `resolve_promise` / `throw` still pass `self.event_loop as *const _ as
      *mut _` to `EventLoop::enter_scope`, whose model mutates the loop-entry
      count. The sibling fix direction (store a raw event-loop pointer like the
      write path) remains source-faithful.
    - Re-read `src/runtime/timer/timer_object_internals.rs:107,856-869,970-1021`:
      `parent_ptr(&self)` still starts from `ptr::from_ref(self).cast_mut()`,
      and `set_event_loop_timer_state(&self)` still writes the plain
      `EventLoopTimer.state` field through the recovered raw parent pointer.
      The source comment says writes should go through `Cell` / `UnsafeCell`,
      but this field is not interior-mutable.
    - Re-read `src/runtime/bake/DevServer.rs:2115,3021`: the deferred request
      still stores `dev: std::ptr::from_ref(self)` and later mutates
      `(*self.dev.cast_mut()).deferred_request_pool`.
    - Re-read `src/runtime/socket/WindowsNamedPipeContext.rs:269-272` and
      `src/jsc/VirtualMachine.rs:1181-1183`: `deinit_in_next_tick` still
      recovers `*mut VirtualMachine` from an `&'static VirtualMachine` backref
      and calls an `&mut self` VM API that mutates the nested event loop.
    - Re-read stored Miri logs for all four experiments. EXP-073/074/075/076
      each have default Stacked-Borrows and Tree-Borrows failures matching the
      registry text; EXP-076's narrower event-loop-pointer fix harness remains
      Miri-clean under both modes.
    - No wording change needed. These remain source-faithful
      shared-provenance UB findings, not merely stylistic `cast_mut` lint hits.

71. **EXP-003 / EXP-006 / EXP-007 / EXP-034 / EXP-036 lockfile cluster evidence recheck**
    - Re-read `src/install/lockfile/Package.rs:3466-3478` and
      `src/install/lockfile/Package/Meta.rs:34-46`: the `Meta` column is still
      copied from lockfile bytes, then materialized as `&mut [Meta]` before
      `meta.needs_update()` can read `has_install_script`.
    - Re-read `src/install/lib.rs:1128-1135`: `Origin` is still a sparse
      `#[repr(u8)]` enum with only `0`, `1`, and `2` valid.
    - Re-read `src/install/lockfile/Tree.rs:1014-1020`: `dep_id` from the
      lockfile-backed dependency list still reaches `deps.get_unchecked(dep_id
      as usize)`.
    - Re-read `src/install/migration.rs:1492-1493`: the migration path still
      calls `set_len` over reserved-but-uninitialized cursor storage, matching
      the EXP-005 family.
    - Re-read `src/install/lockfile/Buffers.rs:104-178`,
      `src/install/lockfile.rs:3369-3378`, and
      `src/install/lockfile/bun.lockb.rs:590`: `read_array<T: Copy>` still
      reinterprets disk bytes as `T`, and `PatchedDep` still contains
      `patchfile_hash_is_null: bool`.
    - Re-read the raw Miri logs. EXP-003, EXP-006, EXP-007, EXP-034, and
      EXP-036 all still have decisive UB signals. Corrected two public summary
      references that claimed EXP-006's invalid tag was `0xa5`; the raw log and
      registry say `0x2a`.
    - The structural-fix split is correct and important: the
      `LockfileArrayElem` bound only directly closes the `read_array` /
      `PatchedDep` class (EXP-036). It does **not** close EXP-003/005/006/007,
      which land in typed `Meta` column materialization, yarn/migration
      uninitialized-slice construction, and `Tree.rs` unchecked indexing.

72. **EXP-046 task-wrapper Send-bound wording recheck**
    - Re-read `src/jsc/WorkTask.rs:23-58` and
      `src/jsc/ConcurrentPromiseTask.rs:14-55`: both wrappers still publish
      `unsafe impl Send` without requiring `Context: Send`.
    - Re-read the seven real context impl sites named by the compile
      experiment (`WriteFile`, `ReadFile`, `GetAddrInfoRequest`, `CopyFile`,
      `PipelineTask`, `TransformTask`, `WalkTask`). The split in
      `phase5_exp046_context_inventory.md` remains right: `WorkTask<C>` stores
      raw `*mut C`, while `ConcurrentPromiseTask<C>` owns `Box<C>`.
    - Re-read `EXP-046.log` / `EXP-046-rerun.log`: the Miri witness is an
      owned-wrapper laundering proof, not a verbatim `WorkTask<C>` proof.
      Re-read `EXP-046-send-bound-check.log`: temporarily adding `+ Send` to
      both context traits still fails all seven real impls with `E0277`.
    - Tightened `phase5_exp046_context_inventory.md` wording from "low-risk"
      remediation to "mechanically safe but not integration-trivial." Adding
      `+ Send` is the right compiler-wall direction, but it intentionally
      surfaces nontrivial per-context redesign work.

73. **UB_RUNBOOK drift-note count refresh**
    - Re-read final-facing `UB_RUNBOOK.md` drift note. It still warned against
      treating "59 confirmed entries" as latest-main live findings, a stale
      count from before the EXP-039 panic-policy demotion.
    - Updated the note to "58 confirmed entries" so it matches the live
      registry and `FINAL_UB_REPORT.md` headline.

74. **EXP-013 POSIX signal-safety classification recheck**
    - Re-read `src/crash_handler/lib.rs:587-590,878-910,1657-1673,1736-1737`.
      The installed POSIX signal handler still calls `crash_handler(...)`; the
      source itself still has the TODO questioning mutex lock/unlock inside a
      signal handler.
    - Re-read `phase5_exp013_signal_safety_source_audit.md` and the registry
      EXP-013 notes. The classification is correctly scoped: POSIX/libc
      async-signal-safety contract violation, not a Miri Stacked-Borrows /
      Tree-Borrows witness.
    - Verified the artifacts explicitly exclude ordinary Rust panic-hook paths
      and Windows VEH from the POSIX async-signal-safety judgment.
    - No wording change needed. This remains a confirmed OS-contract finding
      with the right evidence standard.

75. **EXP-059 `bun_alloc::Mutex` latent-API recheck**
    - Re-read `src/bun_alloc/lib.rs:540-565`: `Mutex::lock(&self)` still
      transmutes `std::sync::MutexGuard<'_, ()>` to `'static`, and the SAFETY
      comment still relies on every `bun_alloc::Mutex` living inside a
      `'static` BSS singleton.
    - Re-read source uses in `src/bun_alloc/lib.rs`: current initialization
      sites write `Mutex::new()` into BSS/lazy singleton storage, so the
      present in-tree precondition is plausibly satisfied.
    - Re-read `experiments/EXP-059/src/main.rs` and `EXP-059.log`: the witness
      deliberately stack-constructs the same mutex shape through the public
      constructor, returns the lifetime-erased guard, and Miri reports a
      dangling `MutexGuard`.
    - The registry wording is appropriately constrained: `CONFIRMED_UB
      (latent API misuse)`, not "current BSS instances are crashing." No
      wording change needed.

76. **EXP-094 intrusive-list in-tree Miri failure recheck**
    - Re-read `src/bun_core/deprecated.rs:114-410`: `DoublyLinkedList<T>`
      stores raw `prev` / `next` node pointers, and the in-tree
      `basic_doubly_linked_list_test` stack-allocates nodes, links them, then
      reuses `&mut` node variables while raw links remain stored in the list.
    - Re-read Phase-11 logs
      `phase11_artifacts/soak-results-v2-nff/miri-tb-nff.log` and
      `phase11_artifacts/soak-results-v2-nff/miri-sp-nff.log`: both record
      Bun's own `deprecated::tests::basic_doubly_linked_list_test` failing
      under Miri at `src/bun_core/deprecated.rs:391:29` with the same
      stale-tag / borrow-stack read failure.
    - Registry and public-report wording is correct: this is an in-tree unit
      test Miri failure, not merely a standalone model. The source SAFETY
      comment proves stack lifetime but not the aliasing-tag invariant.
    - Remediation should delete the deprecated list if unused or redesign it
      around pinned/list-owned nodes; caller-owned stack nodes plus raw
      intrusive links keep allowing stale Stacked-Borrows / Tree-Borrows tags.

77. **EXP-060 N-API thread-safe-function raw-handle recheck**
    - Re-read `src/runtime/napi/napi_body.rs:2937-3007`: exported C ABI
      wrappers still receive `napi_threadsafe_function = *mut
      ThreadSafeFunction` and immediately form `unsafe { &mut *func }` before
      calling `enqueue`, `acquire`, `release`, `ref_`, or `unref`.
    - Re-read `src/runtime/napi/napi_body.rs:2461-2855`: the internal
      `Mutex` / atomics are real, but the wrapper mints the exclusive Rust
      reference before any method-level lock can run.
    - Re-read `experiments/EXP-060/src/main.rs` and
      `phase5_experiment_results/EXP-060-mut-raw-handle-miri.log`: the
      witness includes the same internal `Mutex` and two copied foreign raw
      handles, and Miri reports a data race between an atomic store and the
      second `&mut ThreadSafeFunction` retag.
    - Artifact wording is correct: the primary bug is the pre-lock `&mut`
      retag at the C ABI boundary. Shuttle remains useful only for the
      finalizer/env-teardown subcase; it is not needed to prove the primary
      EXP-060 UB.

78. **EXP-051 `BunLoader` hostile-byte enum validity recheck**
    - Re-read `packages/bun-native-plugin-rs/src/sys.rs:101-117`: `BunLoader`
      is `#[repr(u32)]` with valid discriminants `0..=12`.
    - Re-read `packages/bun-native-plugin-rs/src/sys.rs:151-155` and
      `packages/bun-native-plugin-rs/src/lib.rs:635-637`:
      `OnBeforeParseResult.loader` is a raw `u8`, and the public safe
      `output_loader(&self) -> BunLoader` still transmutes
      `(*self.result_raw).loader as u32` with no validity check.
    - Re-read `experiments/EXP-051/src/main.rs` and
      `phase5_experiment_results/EXP-051.log`: Miri rejects the source-shaped
      hostile value `0xff` at the transmute itself with `constructing invalid
      value of type BunLoader`.
    - The artifact's compatibility-first remediation is the right framing:
      keep the public safe signature only if the implementation performs a
      checked conversion with documented legacy invalid-byte behavior, and add
      `try_output_loader -> Result<BunLoader, InvalidLoader>` for callers that
      want explicit error handling. Marking the safe method `unsafe` would be a
      breaking API change without fixing existing callers.

79. **EXP-054 / EXP-055 demotion recheck**
    - Re-read `src/runtime/napi/napi_body.rs:512-536,1985-2032`: the five
      audited N-API POD mirrors are still `#[repr(C)]` and still lack local
      layout asserts, but the EXP-054 C-header reflector log says the current
      LP64 C layout matches the Rust mirrors.
    - Re-read `src/libuv_sys/libuv.rs:257-292,976-989`: `HandleType` remains a
      hand-transcribed `#[repr(C)]` enum plus a range-checked transmute, and
      `write<T>` still round-trips a Rust function pointer through
      `usize`/`reserved[0]`; the EXP-055 reflector log says current vendored
      libuv discriminants match and target-width parity holds on this platform.
    - Public artifacts are correctly demoted: these are high-value
      layout-lock / portability hardening gates under EXP-063, not counted
      live UB findings unless a concrete C/Rust layout/discriminant mismatch
      or unsupported target-width/ABI case is shown.

80. **EXP-090 H3 `Vec<Header>::set_len(4)` falsification recheck**
    - Re-read `src/http/h3_client/encode.rs:58-107`: the encoder reserves
      `request.headers.len() + 4`, calls `headers.set_len(4)`, pushes user
      headers, then overwrites slots `0..=3` before passing the vector to
      `send_headers`.
    - Re-read `src/uws_sys/quic/Header.rs:9-17,43-51`: current
      `quic::Header` contains raw pointers and integer lengths only, has no
      `Drop`, and has no validity-bearing field that must be initialized before
      assignment overwrites the slot.
    - Re-read `experiments/EXP-090/src/main.rs` and `EXP-090.log`: the
      source-shaped Miri probe completes cleanly for this current no-Drop
      shape.
    - Artifact wording is correct: this remains a style/regression guard, not
      a counted UB finding. Reopen only if `Header` gains `Drop`, a validity
      invariant, a read-before-overwrite path, or if the pseudo-header slots can
      be observed before lines 96-107 initialize them. `ptr::write` would still
      make the initialization discipline clearer.

81. **EXP-081 POSIX dir-iterator `Name` lifetime-erasure recheck**
    - Re-read `src/sys/lib.rs:152-192,221-244,560-579,800-808`: POSIX
      `IteratorResult` is returned by a safe `next(&mut self)` as an owned
      value, while its `Name` is `Copy + Clone + Send + Sync` and stores a raw
      pointer into the iterator's inline buffer. Safe `Name::slice_u8()` later
      forms `&[u8]` from that pointer.
    - The source comments correctly describe the streaming contract, but the
      contract is not encoded in the type (`IteratorResult` has no iterator
      lifetime and `Name::slice_u8()` is safe).
    - Re-read `experiments/EXP-081/src/main.rs`, `EXP-081.log`, and
      `EXP-081-rerun.log`: retaining an entry, dropping the iterator, then
      calling safe `slice_u8()` produces a Miri dangling-pointer failure.
    - Artifact wording is correct: this is a safe-API contract bug. It does not
      require proving current consumers retain entries incorrectly; the safe API
      itself permits the dangling use. The right remediation is an owned result
      or a lifetime-tied `IteratorResult<'iter>` / `Name<'iter>`.

82. **EXP-085 `fmt::Raw` / `fmt::s` invalid-UTF-8 recheck**
    - Re-read `src/bun_core/fmt.rs:724-731,3744-3749`: `Raw<'a>` and the safe
      `s(bytes)` constructor still accept arbitrary `&[u8]`, and `Display`
      still calls `core::str::from_utf8_unchecked(self.0)`.
    - Re-read `experiments/EXP-085/src/main.rs` and `EXP-085.log`: the witness
      passes `[0xff]`, formats `Raw`, and Miri reports UB when the invalid
      string is consumed by `chars().next()`.
    - Artifact wording is appropriately scoped: this is a safe-API contract
      bug in a byte-display helper. Do not overclaim a specific stale argv
      exploit path without a current call graph; classify production
      reachability per call-site byte source. The core contract is still wrong
      while a safe constructor can feed non-UTF-8 bytes to `Display`.

83. **EXP-086 `unsafe_assert(false)` safe-helper recheck**
    - Re-read `src/bun.rs:1582-1586`: `pub fn unsafe_assert(condition: bool)`
      is still safe and calls `core::hint::unreachable_unchecked()` when the
      caller-controlled bool is false.
    - Re-ran the source search mentally via `rg 'unsafe_assert\(' src
      --glob '*.rs'`: current in-tree Rust reachability is still only the
      definition, so this is not a production call-path claim today.
    - Re-read `experiments/EXP-086/src/main.rs` and `EXP-086.log`: Miri
      reports `entering unreachable code` for the source-shaped safe helper
      called with `false`.
    - Artifact wording is correct: count it as a safe-API contract defect, not
      as evidence that production code currently reaches the false branch.
      Preferred remediation remains deletion while unused, or converting the
      false branch to `panic!` / `unreachable!`; making the function `unsafe`
      is acceptable only if a real internal caller needs the unchecked
      precondition.

84. **EXP-087 `ThreadPool::get_worker` duplicate-`&mut` recheck**
    - Re-read `src/bundler/ThreadPool.rs:408-424,625-652`: `get_worker(&self,
      id) -> &'static mut Worker` looks up a raw worker pointer under the
      `workers_assignments` lock, drops/ends the map borrow, then returns
      `unsafe { &mut *w }`; `Worker::get(ctx)` exposes the same escaping
      mutable worker handle for the current thread id.
    - The map lock serializes lookup and insertion only. It does not tie the
      returned reference lifetime to a guard and cannot prevent a safe caller
      from calling the accessor twice for the same id and holding both
      `&'static mut Worker` values.
    - Re-read `experiments/EXP-087/src/main.rs` and `EXP-087.log`: the
      Tree-Borrows witness confirms the duplicate-handle shape; the second
      handle's write is forbidden after a foreign write through the first
      handle.
    - Artifact wording is correct: this is a safe-API contract defect, while
      production exploitability still depends on whether current call sites
      keep two worker handles live concurrently. The fix should return a raw
      pointer / `NonNull` with local unsafe reborrow or a non-escaping
      guard/closure-scoped handle.

85. **EXP-088 `E::String::init_utf16` narrowed-range recheck**
    - Re-read `src/ast/e.rs:1413-1424,1445-1459`: `init_utf16(&[u16])` casts
      the full UTF-16 slice to bytes but stores only the first `data.len()`
      bytes in `Str`; `slice16()` later interprets that stored length as a
      count of `u16` elements and forms a slice covering `2 * data.len()`
      bytes.
    - Re-read `experiments/EXP-088/src/main.rs` and `EXP-088.log`: the model
      mirrors the current source, even with aligned `&[u16; 2]` input, and Miri
      rejects the expanded `slice16()` retag because the stored tag was created
      only for the narrowed byte range.
    - Artifact wording is correct: this is stronger than a generic alignment
      suspicion. The current constructor/accessor pair itself narrows then
      re-expands provenance. Fix by storing typed UTF-16 data, or storing a
      full-byte-length representation rather than a lying byte slice.

86. **EXP-089 primitive scratch-buffer `assume_init` recheck**
    - Re-read `src/bun_core/util.rs:997-1003,1045-1050` and
      `src/install/lockfile/Tree.rs:87-91`: `PathBuffer::uninit`,
      `WPathBuffer::uninit`, and `depth_buf_uninit` still call
      `MaybeUninit::uninit().assume_init()` for `[u8; N]`, `[u16; N]`, and
      `[u32; N]` storage.
    - The source comments still use the tempting but wrong argument that
      "every bit pattern is valid." That does not make uninitialized memory an
      initialized integer value.
    - Re-read `experiments/EXP-089/src/main.rs` and `EXP-089.log`: rustc emits
      `invalid_value` warnings for all three shapes and Miri rejects
      constructing `PathBuffer` itself before any meaningful caller read.
    - Artifact wording is correct: this is not merely read-before-write
      hardening. The constructor creates an invalid initialized value. Use
      zeroed arrays as the emergency fix, or a `MaybeUninit<[T; N]>` /
      initialized-prefix scratch-buffer wrapper for the performance-preserving
      design.

87. **EXP-091 `BindgenArray` allocator-layout recheck**
    - Re-read `src/jsc/bindgen.rs:10-18,235-353`: the identity fast path is
      now gated on `size_of`, `align_of`, and `Child::SAME_REPR`, but the
      second conversion branch can still return `Vec<Child::ZigType>` over an
      allocation originally reconstructed as `Vec<Child::ExternType>` whenever
      `size_of::<ZigType>() <= size_of::<ExternType>()` and
      `align_of::<ZigType>() <= MI_MAX_ALIGN_SIZE`.
    - Re-read Rust's local `Vec::from_raw_parts` safety docs: the element type
      must have the same alignment as the allocation, and `size_of::<T>() *
      capacity` must equal the allocated byte size. `T` having a less strict
      alignment is explicitly not enough.
    - Re-read `experiments/EXP-091/src/main.rs` and `EXP-091.log`: the witness
      uses `Extern` size 8 / align 8 and `Zig` size 8 / align 4; Miri rejects
      `Vec<Zig>` drop with `incorrect layout on deallocation`.
    - Artifact verdict remains correct: relying on mimalloc's runtime `mi_free`
      layout tolerance does not satisfy `Vec::from_raw_parts`.
    - Correction made during this recheck: changed remediation language from
      "reuse only when element size and alignment match" to the exact contract:
      reuse only when the eventual `Vec<ZigType>` deallocation layout matches
      the original allocation layout (`align_of::<ZigType>() ==
      align_of::<ExternType>()` and `size_of::<ZigType>() * new_capacity ==
      size_of::<ExternType>() * old_capacity`), or allocate fresh / carry an
      original-layout raw-allocation token.

88. **EXP-092 `ReadResult::to_stream` owned-slice recheck**
    - Re-read `src/runtime/webcore/streams.rs:2533-2597` and
      `src/runtime/webcore.rs:403-404`: `streams` is a public module,
      `ReadResult` is public, `ReadResult::Read(*mut [u8])` is a safe enum
      variant, and both `to_stream` / `to_stream_with_is_done` are safe public
      methods. The extra parameters do not invalidate the safe-API claim:
      `Pending` is public in the same module and `JSValue` is imported from the
      public JSC glue.
    - Re-read the source branch: pointer inequality from the caller-provided
      `buf` decides ownership. In the `owned` branches, the method calls
      `Vec::from_raw_parts(slice_ptr, len, len)` at `streams.rs:2589-2597`.
    - Re-read `experiments/EXP-092/src/main.rs` and `EXP-092.log`: safe Rust
      constructs a raw fat pointer to stack memory, passes a different `buf`,
      receives `StreamResult::Owned(Vec<u8>)`, and Miri rejects drop with
      `deallocating ... stack variable memory, using Rust heap deallocation
      operation`.
    - Artifact wording is defensible: this is stronger than a producer-discipline
      watchlist item. Current in-tree producers may be disciplined, but the
      safe API itself does not encode ownership, so pointer inequality is an
      unsound ownership test.

89. **EXP-093 / EXP-095 PE and Mach-O alignment recheck**
    - Re-read `src/exe_format/pe.rs:203-220,281-302,315-334,389-396,900-920`:
      the helpers are explicitly documented as unaligned byte views, but
      callers immediately materialise `&T`, `&mut T`, `&[SectionHeader]`, or
      `&mut [SectionHeader]` from format-controlled offsets. The source TODOs
      already name the Zig `*align(1)` to Rust-reference gap.
    - Re-read `experiments/EXP-093/src/main.rs` and `EXP-093.log`: the witness
      mirrors the `SectionHeader` slice path with an odd section-header offset;
      Miri rejects `slice::from_raw_parts` with an unaligned-reference error.
      Artifact wording as hostile/tampered PE bytes is correct.
    - Re-read `src/exe_format/macho_types.rs:1-12`,
      `src/sys/lib.rs:5852-5878`, and
      `src/exe_format/macho.rs:121-130,163-170,361-403`: the iterator's read
      path is sound (`read_unaligned`), and `macho.rs:163-170` uses the correct
      `write_unaligned` pattern, but later mutation paths still cast
      load-command bytes to typed mutable references.
    - Source-faithfulness correction for EXP-095: ordinary compiler-produced
      64-bit Mach-O command streams are normally aligned, so the public claim
      should stay scoped to hostile/tampered bytes unless a live producer emits
      malformed `cmdsize`s. The current iterator does not validate that each
      `cmdsize` preserves command-offset alignment; a malformed
      odd-but-in-bounds `cmdsize` can place the next command at the odd offset
      used in the witness. Added this nuance to the EXP-095 notes.

90. **EXP-077 / EXP-078 safe-API lifetime and uninit recheck**
    - Re-read `src/css/css_parser.rs:2304-2322,2678-2727,2739-2767`: the
      result structs expose `CssModuleExports<'static>` /
      `CssModuleReferences<'static>`, and the implementation explicitly
      transmutes arena-borrowed maps to `'static` at `css_parser.rs:2717-2725`.
      The source TODO names this as a placeholder until the bump lifetime is
      threaded.
    - Re-read `experiments/EXP-077/src/main.rs` and
      `EXP-077-default-miri.log`: the witness mirrors the lifetime-only widen
      and Miri rejects returning the result with a dangling reference. Artifact
      wording is correct because it already notes current reviewed in-tree
      consumers only read `result.code`, so production exploitability is
      caller-dependent.
    - Re-read `src/bun_core/util.rs:111-119,149-171,284-301`: `ArrayLike` is a
      public safe trait, `Vec<T>::set_len_and_slice` calls `Vec::set_len(n)`
      and returns `&mut [T]` before initialization, and the only in-tree caller
      immediately copies from `default`.
    - Re-read `experiments/EXP-078/src/main.rs` and
      `EXP-078-default-miri.log`: safe code can call the trait method on
      `Vec<bool>`, read `live[0]`, and Miri reports an uninitialized read. The
      current in-tree caller discipline does not make the safe trait method
      sound for arbitrary safe callers.

91. **EXP-079 / EXP-080 aliasing and handle-forgery recheck**
    - Re-read `src/bundler/transpiler.rs:256-268,546,612,710`:
      `Transpiler::env_mut(&self) -> &'a mut dot_env::Loader<'a>` derives a
      caller-lifetime `&mut` from a shared receiver and raw `self.env` pointer.
      The current in-tree calls are mostly statement-scoped, but the safe API
      itself permits two live mutable borrows from two safe calls.
    - Re-read `src/install/PackageManager.rs:1091-1107`: the same-shape API
      exists there as `pub fn env_mut<'a>(&self) -> &'a mut
      dot_env::Loader<'static>`. Added an EXP-079 note so remediation sweeps
      both entry points instead of patching only `Transpiler`.
    - Re-read `experiments/EXP-079/src/main.rs` and
      `EXP-079.log`: the witness mirrors the Bun shape with
      `Transpiler<'static>`, calls `env_mut()` twice, and Tree-Borrows rejects
      the later write (`write access through <354> ... is forbidden`). The
      artifact correctly scopes the claim to a safe-API contract bug rather
      than asserting every current Bun call site is live UB.
    - Re-read `src/dispatch/lib.rs:302-318` and
      `src/bundler/lib.rs:326-342`: `bun_dispatch::link_interface!` really
      emits public `kind` and `owner` fields while documenting `unsafe fn new`
      as the invariant-establishing gate. Corrected the registry citation from
      the stale/imprecise `src/dispatch/lib.rs:310-324` to `302-318`.
    - Re-read `experiments/EXP-080/src/main.rs` and
      `EXP-080-default-miri.log`: safe code forges a handle with
      `owner = null_mut()`, calls a safe dispatch method, and Miri reports
      `memory access failed: attempting to access 1 byte, but got null
      pointer`. The optimal fix remains private fields plus an explicit unsafe
      raw-parts constructor for legitimate low-level call sites.

92. **EXP-082 / EXP-083 / EXP-084 Send-Sync and thread-affinity recheck**
    - Re-read `src/jsc/webcore_types.rs:60-96,220-231` and
      `src/runtime/webcore/Blob.rs:1509-1513,1557-1562,1869-1873,1911-1916`:
      `Blob` is still `unsafe impl Send` + `Sync`, `global_this(&self)` still
      returns a safe `Option<&JSGlobalObject>`, and downstream paths still
      immediately call `bun_vm().as_mut().event_loop()` from the returned
      reference. Re-read `src/jsc/JSGlobalObject.rs:20-28,40-42`; the
      JS-global handle is documented as opaque, `UnsafeCell`-backed, and
      `!Send + !Sync`. EXP-082 remains correctly framed as a generic safe-API
      contract bug, not as a proven production off-thread Blob caller.
    - Re-read `src/runtime/shell/IOWriter.rs:209-252,543-555,969-985` and
      `src/runtime/shell/IOReader.rs:72-104,220-268`: both shell IO handles
      remain `unsafe impl Send` + `Sync`, and safe `&self` methods still
      mutate `UnsafeCell` state. EXP-083's stronger formulation (safe
      concurrent method calls), rather than the earlier weaker "last Arc drops
      on another thread" rationale, is correct.
    - Re-read `src/jsc/VirtualMachine.rs:604-688`: `VirtualMachine` remains
      `unsafe impl Send` + `Sync`; safe `as_mut(&self)` and safe
      `get_mut() -> &'static mut VirtualMachine` both route through TLS-backed
      `get_mut_ptr().unwrap_unchecked()`. Strengthened EXP-084: a captured
      `&VirtualMachine` on a non-VM thread is not the only proof path; direct
      safe `VirtualMachine::get_mut()` on a non-VM thread reaches the same
      unchecked TLS precondition.
    - Re-read `EXP-082.log`, `EXP-083.log`, and `EXP-084-release.log`: Miri
      respectively reports the Blob-shaped cross-thread `Cell` race, the
      shell-IO safe `UnsafeCell` reborrow violation, and release-mode
      `unwrap_unchecked(None)` as `entering unreachable code`. No demotion is
      warranted; the artifacts are defensible when kept at the generic
      safe-API-contract level.

93. **EXP-072 HiveArray raw-slot migration and EXP-017 volatile-publish demotion recheck**
    - Re-read `src/collections/hive_array.rs:205-223,523-548`:
      `HiveArray::get`, `HiveArrayFallback::get`,
      `get_and_see_if_new`, and `try_get` remain deprecated with the explicit
      message "returns *mut T to uninitialized memory; use get_init / emplace /
      claim". Re-read the replacement token API at `hive_array.rs:386-462`;
      the `HiveSlot` token encodes the initialization window the legacy raw
      methods leave to callers.
    - Re-read the eight current raw-slot call sites listed in EXP-072:
      bun_install's resolve/network-task pools, `server_body.rs:3415`,
      `server/mod.rs:705`, `DevServer.rs:2097`, and
      `h2_frame_parser.rs:7375`. The artifact correctly avoids saying all
      eight are independently proven production crashes; several immediately
      perform `write` / placement construction. The confirmed claim is the
      generic unsafe API contract plus migration obligation, backed by the
      source deprecation and `EXP-072.log`'s uninitialized-drop Miri witness.
    - Re-read `src/io/lib.rs:868-870,1018-1020,1153-1168` and
      `phase5_exp017_source_overlap_audit.md`: the primitive model is real
      (`write_volatile` + `fence(SeqCst)` is not an atomic function-pointer
      publication), but the current write sites publish before queueing or
      rewrite after the IO thread has popped and cleared `scheduled`. The
      `NO_EVIDENCE` production verdict is correct; keep the Miri race model as
      a regression guard and do not count EXP-017 as live production UB today.

94. **EXP-006 stale invalid-tag byte cleanup**
    - Re-read the EXP-006 registry block, `experiments/EXP-006/src/main.rs`,
      and `phase5_experiment_results/EXP-006.log`: the current `Meta::origin`
      witness uses attacker byte `0x2a`, matching EXP-003's witness byte.
    - Fixed `UB_RUNBOOK.md`'s rerun table, which still said
      `0x2a / 0xa5`. This contradicted the already-recorded correction item 71
      and could make the public artifact look mechanically inconsistent.

95. **Public-summary signature and citation precision sweep**
    - Replaced shorthand `Blob::global_this(&self) -> &JSGlobalObject` wording in
      the public summary/remediation tables with the exact source signature
      `Blob::global_this(&self) -> Option<&JSGlobalObject>`. The finding remains
      unchanged: when the pointer is present, a `Send + Sync` `Blob` exposes a
      JS-thread-affine `&JSGlobalObject` through a safe method.
    - Propagated the EXP-084 strengthening to `phase8_remediation_plan.md`:
      direct safe `VirtualMachine::get_mut()` on a non-VM thread reaches the
      same unchecked TLS precondition as the captured-`&VirtualMachine`
      `as_mut()` witness.
    - Corrected the Phase-4 `link_interface!` row from stale
      `src/dispatch/lib.rs:310-324` to the exact public-field / unsafe-new span
      `src/dispatch/lib.rs:302-318`, matching EXP-080 and the source re-read.
    - Re-ran the same sweep across Phase 2, Phase 4, Phase 8, and the final
      report and fixed the remaining duplicate `src/dispatch/lib.rs:310-324`
      citations. Also added EXP-084 to the Phase-4 transition footer, which had
      listed EXP-082/083 and then skipped directly to EXP-085.
    - Tightened the EXP-082 and R-EXP-082 titles from "safe
      `&JSGlobalObject`" to "safe `Option<&JSGlobalObject>`" and the EXP-084
      title from "`as_mut()`" to "`as_mut()` / `get_mut()`" so headings match
      the corrected source-faithful bodies.

96. **Phase-8 / Phase-9 public-polish correction**
    - Re-read `src/jsc/webcore_types.rs:60-96,220-231` and fixed remaining
      EXP-082 citations that had drifted to `55-96,223-231`. The old range
      was close enough to include the safety comment and function body, but
      public tables should point at the actual struct / unsafe-impl / accessor
      spans: `Blob` starts at line 60, the `Send` / `Sync` impls are lines
      95-96, and `global_this(&self)` is documented and implemented at
      220-231.
    - Also fixed the Phase-1 Section K inventory row so it uses the exact
      `Option<&JSGlobalObject>` signature instead of the earlier shorthand
      `&JSGlobalObject`.
    - Corrected the PR #30765 branch name in Phase 8 and Phase 9 from
      `claude/unsafe-exorcism-demo` to the actual GitHub head branch
      `claude/unsafe-exorcist-demo`.
    - Corrected both `phase9_beads_log.md` stale current-count statements. The
      bead graph was generated when the registry had 58 confirmed entries;
      after EXP-094/095 were added, EXP-037 resolved, and EXP-039 demoted, the
      current registry still has 58 `CONFIRMED_UB` entries but a different
      composition. The graph is therefore compositionally stale, not
      numerically one finding behind.
    - Fixed a simple arithmetic blunder in the Phase-9 graph summary: "8
      keystone beads" enumerated 7 structural beads plus 3 family roots, so
      the correct count is 10. Also replaced "with no fresh code review" with
      "with no new patch authoring" because maintainer review is still needed.
    - Replaced "no maintainer pushback" sequencing language with the more
      defensible "lowest-review-friction entry points," and tightened one
      picohttp runner-up sentence from colloquial "blows the latency budget" to
      the precise cost ("avoidable allocation/copy latency on a hot HTTP path").

97. **EXP-059 constructor-signature precision**
    - Re-read `src/bun_alloc/lib.rs:546-565`: `Mutex::new()` is a
      `pub const fn`, and the lifetime-erasing `MutexGuard` transmute lives in
      `lock()` at `550-565`.
    - Corrected public artifacts that described `Mutex::new()` as a plain
      `pub fn` or pointed to stale line 571. The finding is unchanged:
      current in-tree `bun_alloc::Mutex` instances satisfy the documented BSS
      precondition, but the public const constructor admits stack construction,
      and the EXP-059 Miri witness proves that pattern constructs a dangling
      `MutexGuard<'static>`.

98. **Phase-8 PR-sequence count correction**
    - Re-counted `phase8_remediation_plan.md`'s "Recommended PR sequencing"
      list: it currently contains entries 1 through 31.
    - Fixed `FINAL_UB_REPORT.md`'s source-of-truth artifact list, which still
      described Phase 8 as a "26-PR sequence" from an earlier snapshot. The
      correct current description is a 31-PR sequence.

99. **Late EXP evidence spot-check continuation**
    - Re-read the source and raw evidence for EXP-087, EXP-088, EXP-091,
      EXP-092, EXP-093, EXP-094, and EXP-095.
    - EXP-087 remains correctly framed as a safe-API duplicate-`&mut Worker`
      contract bug: `ThreadPool::get_worker(&self, id) -> &'static mut Worker`
      drops the worker-map lock before returning the reference, and the
      Tree-Borrows log rejects the two-call witness with a disabled-tag write.
    - EXP-088 remains source-faithful: `E::String::init_utf16` deliberately
      stores a byte slice whose length is the UTF-16 element count, and
      `slice16()` later retags twice that byte range. The Miri log rejects the
      expanded retag at offset `0x2`.
    - EXP-091 and EXP-092 remain correctly limited to safe generic/API
      contract defects: production reachability is still a separate source
      question, but the public/safe shapes can deallocate with the wrong
      layout or turn a non-owned stack slice into `Vec<u8>`.
    - EXP-093 and EXP-095 remain correctly scoped as hostile/tampered-object
      alignment findings, not claims that every ordinary PE/Mach-O file
      produced by a normal toolchain is misaligned. Their Miri witnesses reject
      typed references formed from odd byte offsets, matching the unvalidated
      byte-backed parser/mutator shapes.
    - EXP-094 remains one of the strongest evidence items: the Phase-11
      workspace Miri log contains an in-tree `bun_core` unit-test failure for
      `basic_doubly_linked_list_test`, with a stale Stacked-Borrows tag after
      re-minting `&mut` references to already-linked intrusive nodes.
    - Re-read the source/logs for EXP-077 through EXP-084. No artifact change
      was needed: the report already frames EXP-082/083/084 as generic
      safe-contract UB rather than proven production races, cites
      `EXP-084-release.log` rather than the debug-assertion panic log, and
      limits EXP-077's production exploitability to call sites that actually
      read the arena-backed `exports` / `references` fields.

100. **PR #30765 unmerged-fix drift check**
    - Re-queried GitHub for PR #30765: it is still `OPEN`, with
      `mergedAt: null`, head branch `claude/unsafe-exorcist-demo`.
    - Re-read current `origin/main` source for the three fixes carried by that
      PR. `src/errno/linux_errno.rs` still uses
      `core::mem::transmute::<u16, E>(int as u16)` in the Linux
      `impl GetErrno for usize`; `src/threading/guarded.rs` still defines
      `GuardedLock` without the `_not_send: PhantomData<*const ()>` marker;
      and `src/ast/nodes.rs` still has unbounded `unsafe impl<T> Send/Sync for
      StoreSlice<T>`.
    - The Final UB Report and Phase-8 plan are therefore still correct to
      describe PR #30765 as an already-drafted but unmerged remediation vehicle
      for EXP-002, EXP-018, and EXP-019.

101. **High-overclaim-risk evidence recheck: EXP-013 / EXP-056 / EXP-060 / EXP-046..059**
    - Re-read `src/crash_handler/lib.rs` around the POSIX signal entry and
      `crash_handler()` body. EXP-013 remains correctly classified as a
      POSIX/libc async-signal-safety contract finding, not a Miri Rust
      abstract-machine witness. The current registry / final report wording
      is the defensible form: 9 of 14 audited call-graph steps, or at least 8
      distinct non-async-signal-safe operation classes if the lock calls are
      grouped.
    - Fixed one stale secondary wording line in `phase6_idea_wizard.md` that
      still said "8 non-AS-safe steps"; it now matches the registry wording:
      "9 of 14 audited steps / at least 8 operation classes."
    - Re-read `src/runtime/server/NodeHTTPResponse.rs` and `src/ptr/lib.rs`
      for EXP-056. The narrow claim is correct: the zero-ref path reaches
      `deinit(&self)` and frees through `self.as_ctx_ptr()`, whose trait
      documentation explicitly says the pointer carries shared/read-only
      provenance. The Miri Tree-Borrows log rejects deallocation through that
      tag. The artifacts already avoid the older overclaim about `RefPtr`
      usage or cross-thread `Cell` races.
    - Re-read `src/runtime/napi/napi_body.rs` for EXP-060. The exported
      wrappers still materialize `unsafe { &mut *func }` before entering the
      internal lock (`napi_call_threadsafe_function`,
      `napi_acquire_threadsafe_function`, `napi_release_threadsafe_function`,
      `napi_ref_threadsafe_function`, `napi_unref_threadsafe_function`), so
      the raw-handle witness remains source-shaped. The artifact is correctly
      scoped to the pre-lock `&mut` aliasing bug; Shuttle/loom are only
      follow-up for finalizer/env teardown.
    - Re-read the higher-risk "shape-level" entries EXP-046, EXP-057,
      EXP-058, and EXP-059. Their current wording is appropriately limited:
      EXP-046 is an unsafe-contract defect plus Send-bound compile experiment,
      not a claim that every context has an observed crash; EXP-057 is a
      shape-level `&self -> &'a mut T` cluster whose production sites rely on
      one-call invariants; EXP-058 is strongly supported by an in-source TODO
      and Tree-Borrows two-call witness; EXP-059 is a latent API-misuse
      witness, with current BSS instances explicitly acknowledged as sound.

102. **Strict-provenance family and convergence-table completeness check**
    - Re-read the strict-provenance family in the registry, Phase 4, Phase 8,
      final report, and runbook. EXP-020, EXP-029, EXP-048, EXP-049, and
      EXP-050 are consistently marked `DEFERRED` / release-gate migration
      work, not default-runtime UB. Source spot-checks still match the family:
      `URL::host_with_path` does `ptr as usize` arithmetic, `EnvStr` rebuilds
      a pointer from integer bits, and `TaggedPtr::get/to` reconstruct typed
      pointers from stored integer payloads.
    - Verified the raw strict-provenance logs exist and still show
      `-Zmiri-strict-provenance` rejecting the integer-to-pointer
      reconstruction. No demotion/promotion was needed.
    - Fixed `FINAL_UB_REPORT.md`'s convergence table, which said formal rounds
      exist through round 78 but only displayed rows through round 73. Added
      rows 74-78 with the unchanged current totals:
      `OPEN=0`, `NEEDS_REFINEMENT=0`, `CONFIRMED_UB=58`,
      `NO_EVIDENCE=15`, `DEFERRED=16`, `RESOLVED=2`.

103. **Exploitability-language precision sweep**
    - Re-read the registry and triangulation text for phrases that implied a
      complete exploit chain where the audit has proven a UB precondition but
      not arbitrary code execution.
    - Tightened EXP-002 wording: it now says the public Linux errno trait is a
      latent public-API trap for any future raw-syscall path that feeds an
      unmapped errno into the trait. It no longer says the path is "one
      Zig-port pattern from being weaponized."
    - Tightened EXP-035 and the Phase-2 validity summary: they now say that
      once the `__BUN` / standalone module bytes are tampered, a single
      invalid enum byte is sufficient to trigger the Miri-confirmed validity
      violation. They no longer use the phrase "attacker's job is trivial."
    - Tightened the standalone-binary triangulation text: it now explicitly
      says the audit proves the validity violation and does **not** prove a
      complete arbitrary-code-execution chain. The checksum recommendation
      remains framed as defense-in-depth for the tampering vector, not as a
      substitute for byte-level validity checks.

104. **Post-wording convergence reruns**
    - Re-ran the skill's convergence tracker after the exploitability-language
      edits and the final registry-lint validation. The tracker wrote
      `phase7_convergence_round_79.json` and then
      `phase7_convergence_round_80.json` with unchanged verdict totals:
      `OPEN=0`, `NEEDS_REFINEMENT=0`, `CONFIRMED_UB=58`,
      `NO_EVIDENCE=15`, `DEFERRED=16`, `RESOLVED=2`.
    - Updated `FINAL_UB_REPORT.md` so the formal convergence table and latest
      snapshot references now point through round 80.

105. **Registry audited-base wording guard**
    - Tightened the registry's run-start policy note so "current source" inside
      individual entries is explicitly defined as the audited base
      `origin/main@4d443e5402` unless an entry names a later fetched
      `origin/main@...` commit.
    - This prevents a reader from treating pinned-base entries as refreshed
      latest-main claims after upstream advanced to `e750984db6`.

106. **EXP-035 standalone module-graph validity source spot-check**
    - Re-read `src/standalone_graph/StandaloneModuleGraph.rs:231-246` and
      `:575-580`: `CompiledModuleGraphFile` is still materialized by
      `core::ptr::read_unaligned(modules_list_base.add(i))` as a typed record.
    - Re-read the four closed enum fields:
      `FileSide` has 2 valid `u8` tags, `Encoding` has 3, `ModuleFormat` has
      3, and `bun_ast::Loader` has 21. The probability calculation in the
      validity bucket remains accurate: `(2 * 3 * 21 * 3) / 256^4`.
    - Re-read `phase5_experiment_results/EXP-035.log`: Miri reports
      `constructing invalid value of type CompiledModuleGraphFile` at
      `.loader.<enum-tag>` for byte `0xff`, exactly at the
      `read_unaligned` materialization point. No artifact change needed beyond
      the exploitability-language tightening already recorded in item 103.

107. **EXP-036 lockfile `PatchedDep` bool-validity source spot-check**
    - Re-read `src/install/lockfile/bun.lockb.rs:577-601`: the patched
      dependencies reader calls `buffers::read_array::<PatchedDep>(stream)`
      and then stores the values into `lockfile.patched_dependencies`.
    - Re-read `src/install/lockfile.rs:3369-3377`: `PatchedDep` is
      `#[repr(C)]` and contains `patchfile_hash_is_null: bool`.
    - Re-read `src/install/lockfile/Buffers.rs:104-178`: `read_array<T: Copy>`
      validates bounds/alignment/element-size but still forms `&[T]` directly
      from attacker-controlled lockfile bytes. No byte-level validity decode
      exists for `bool`.
    - Re-read `phase5_experiment_results/EXP-036.log`: Miri reports
      `constructing invalid value of type bool: encountered 0xff`. The
      registry's EXP-036 source path and fix direction remain defensible.

108. **EXP-036 remediation-plan conceptual correction**
    - Fixed `phase8_remediation_plan.md`'s R-EXP-036 plan. The old wording
      suggested "validate at impl time," which is not a runtime validation
      point and would be misleading for reviewers.
    - The plan now states the two defensible fix shapes:
      (1) make `PatchedDep` byte-open by storing the disk field as `u8` plus an
      accessor, then allow it under `LockfileArrayElem`; or
      (2) add a checked decode path that reads raw bytes, validates `bool`
      bytes as `0 | 1`, and only then constructs `PatchedDep`.
    - Explicitly recorded the key invariant: do **not** form `&[PatchedDep]`
      before validation, because materializing `bool = 0xff` is already UB.

109. **Open PR #30765 trio source-faithfulness recheck**
    - Re-read audited-base source for the three proposed PR #30765 fixes.
      `GuardedLock<'a, Value, M>` still has only `guarded:
      &'a GuardedBy<Value, M>` and no `_not_send` marker; sibling
      `MutexGuard` carries `PhantomData<*const Mutex>` with a comment naming
      Darwin `os_unfair_lock` / Windows `SRWLOCK` same-thread unlock
      requirements.
    - Re-read `StoreRef<T>` vs `StoreSlice<T>`: `StoreRef<T>` has
      `unsafe impl<T: Send> Send` / `unsafe impl<T: Sync> Sync`, while
      `StoreSlice<T>` still uses unbounded `unsafe impl<T> Send/Sync`.
    - Re-read `linux_errno.rs`: Linux `impl GetErrno for usize` still uses
      `transmute::<u16, E>(int as u16)` after only a `[0,4096)` range clamp.
      Checked alternatives already exist (`SystemErrno::init` on POSIX and
      `E::try_from_raw` on Windows), matching the proposed fix direction.
    - No artifact wording change needed; this recheck confirms the open-PR
      remediation batch remains source-faithful against the audited base.

110. **EXP-091 `BindgenArray` allocator-layout remediation check**
    - Re-read `src/jsc/bindgen.rs:235-353`: the non-`SAME_REPR` fast path
      reuses storage when `size_of::<ZigType>() <= size_of::<ExternType>()`
      and `align_of::<ZigType>() <= MI_MAX_ALIGN_SIZE`, then returns a
      `Vec<ZigType>` that will later deallocate with the `ZigType` layout.
    - Re-read the Phase-8 remediation text. It correctly says the sound reuse
      condition is allocation-layout equality, not allocator-convenience:
      deallocation through `Vec<ZigType>` must match the original allocation's
      size and alignment (or use a raw-allocation wrapper that preserves the
      original layout for deallocation).
    - No wording change needed; the plan's winner (layout-equal reuse only,
      fresh allocation otherwise) is the right fix shape.

111. **Confirmed-entry evidence-path audit**
    - Mechanically parsed all 58 `CONFIRMED_UB` registry entries and checked
      that each one cites an evidence artifact or source-callgraph witness.
      All 58 have explicit evidence references.
    - Verified referenced log paths exist for the confirmed entries. One stale
      path was found: EXP-001's invocation still wrote to
      `phase5_experiment_results/EXP-001.log`, but the actual retained log is
      `phase5_experiment_results/EXP-001_preflight.log`.
    - Updated EXP-001's invocation path to the retained `_preflight` log name.

112. **EXP-049 / `SmolStr` naming split and missed strict-provenance row**
    - Re-read `src/bun_core/string/immutable.rs:1076` and corrected the
      artifact label: EXP-049 is `StringOrTinyString::slice`, not `SmolStr`.
      That finding reconstructs a pointer from little-endian bytes with
      `usize::from_le_bytes(...) as *const u8`.
    - Re-read the actual exported `src/bun_core/string/SmolStr.rs` type and
      found a distinct packed-pointer representation: heap strings store raw
      pointer bits in the upper 64 bits of a `u128`, then recover
      `*const/*mut u8` in `ptr_const()` / `ptr()`. `slice()`, `append_char()`,
      `append_slice()`, and `Drop` all use the recovered pointer.
    - Added EXP-096 as a separate `DEFERRED` strict-provenance release-gate
      finding, with a minimal Miri reproducer under
      `experiments/EXP-096/` and raw log
      `phase5_experiment_results/EXP-096.log`. Re-ran the witness with a
      `bash`/`pipefail` wrapper after the initial zsh wrapper mishandled
      `PIPESTATUS`; the retained log now records the expected Miri rejection
      and the wrapper observed `miri_exit=1`.
    - Updated the registry, unified findings table, final report, runbook,
      strict-provenance policy note, and remediation plan. At that
      pre-EXP-097 checkpoint, the registry had one fewer EXP entry and one
      fewer confirmed item than it has now; EXP-097 later superseded those
      checkpoint totals.

113. **Safe-contract gap sweep after EXP-096**
    - Ran a targeted detector pass over generic `unsafe impl Send` / `Sync`,
      safe `&self -> &mut` / `&'static mut` APIs, and recurring
      validity/provenance primitives.
    - Added `CODEX_SAFE_CONTRACT_GAP_SWEEP_2026-05-16B.md` to record the
      outcome. The broad detector hits mapped to existing registry owners:
      F-L-1 / EXP-057, EXP-079, EXP-087, EXP-083, EXP-082, EXP-084,
      EXP-019, EXP-045, and EXP-080.
    - Reviewed suspicious but non-promoted siblings (`SendPtr<T>` wrappers,
      `DeclarationBlock<'bump>: Send + Sync`, `StringHashMapKey<A>`,
      `RacyCell<T>` / `ThreadCell<T>` production instantiations) and kept them
      as hardening/no-new-EXP items. No new default-runtime UB count changed.

114. **Fresh ast-grep round-82 triage**
    - Re-ran the skill's bundled ast-grep UB pattern suite against current
      `src/`. Raw output (`phase2_raw/codex_ast_grep_ub_patterns_round82_2026-05-16.log`,
      84,757 lines, ~6 MB) was later dropped from the PR to keep the diff
      reviewable; regenerate by rerunning the suite against `origin/main@4d443e5402`.
    - Added `CODEX_AST_GREP_ROUND82_TRIAGE_2026-05-16.md` with per-pattern
      counts and high-signal spot checks.
    - Cross-checked the small/high-signal buckets (`from_ref`/const-to-mut
      aliasing, lifetime widening, `MaybeUninit::uninit().assume_init`,
      transmutes, `get_unchecked`, UTF-8 unchecked/indexing) against the
      registry. They map to existing owners (EXP-002/007/008/009/048/051/077/
      089/093/095 plus reviewed hardening rows) or are advisory-only.
    - Corrected two stale current-summary references revealed by the rerun:
      at that checkpoint Phase 4's header said `EXP-001..EXP-096`, and the
      final report's raw Phase-5 log count said 111 and included EXP-096.
      EXP-097 later superseded both current-count claims.
    - Re-ran the convergence tracker after this triage; round 82 stayed
      converged with unchanged totals: 58 `CONFIRMED_UB`, 15 `NO_EVIDENCE`,
      17 `DEFERRED`, 2 `RESOLVED`, and zero open/refinement entries.

115. **Syn-walker round-83 triage and EXP-097 promotion**
    - Built and ran the skill's implemented `syn-walkers` against `src/`:
      `data_races` (155 manual Send/Sync impls), `escape` (0 hits),
      `pin_walker` (197 broad `mem::replace` hits, no promoted Pin finding),
      `transmute_pairs` (24 transmutes), and `safety_doc_coverage` (2,565
      documentation-hardening hits).
    - Added `CODEX_SYN_WALKER_ROUND83_TRIAGE_2026-05-16.md` and retained raw
      outputs under `phase2_raw/codex_syn_*_round83_2026-05-16.*`.
    - Promoted EXP-097 after `transmute_pairs` exposed an under-classified
      errno family: `windows_errno::E::from_raw` and
      `SystemErrno::from_raw` are safe `pub const fn`s, not `unsafe fn`
      caller-contract sites, and they transmute unchecked sparse enum
      discriminants. Release-mode Miri confirms invalid-tag UB in
      `phase5_experiment_results/EXP-097.log`.
    - Corrected `phase2_findings_06_type_punning.md` and
      `phase2_findings_04_validity.md` accordingly, added F-NF6-4 to
      `phase4_unified_findings.md`, and updated final/runbook/remediation
      counts to 93 registry entries and 59 `CONFIRMED_UB`.

116. **W4 latest-main drift refresh**
    - Fetched `origin/main` and confirmed it had advanced from the audited
      `4d443e5402` base to `e750984db6` (7 commits ahead), including the
      material hardening commit `e520065ebb`.
    - Added `CODEX_W4_LATEST_MAIN_REFRESH_2026-05-16.md` to prevent stale
      public wording. The refresh checks direct source-presence for the
      highest-priority findings and explicitly says not to quote an exact
      latest-main live count until every `CONFIRMED_UB` entry has been replayed.
    - Replayed 8 high-priority witnesses / compile witnesses and retained
      logs under `phase5_experiment_results/W4_*_origin_main*.log`:
      EXP-002, EXP-004, EXP-007, EXP-008, EXP-009, EXP-018, EXP-019, EXP-097.
      All eight still produce the expected Miri failure or compile-success
      proof signal for the source shape that remains present on latest main.
    - Updated `FINAL_UB_REPORT.md` to the then-current raw phase-5 log count
      and listed the W4 refresh artifact as a source-of-truth appendix.

117. **Round-85 aliasing-contract gap sweep**
    - Exercised the remaining `syn-walkers` binaries not covered by round 83:
      `aliasing` and `validity`. Both are still scaffolds that exit with
      `TODO: implement ... walker`, so their raw stderr was retained under
      `phase2_raw/codex_syn_{aliasing,validity}_round85_2026-05-16.stderr`.
    - Added `CODEX_ALIASING_CONTRACT_ROUND85_2026-05-16.md` and a manual
      `&self -> &mut` / `from_ref(...).cast_mut()` / `addr_of!.cast_mut()`
      sweep with 128 textual hits. The high-signal hits map to existing owners
      (EXP-057, EXP-079, EXP-083, EXP-084, EXP-087, EXP-041, EXP-056, EXP-019,
      EXP-021) or the existing lint/remediation queue.
    - No new EXP was added. The round strengthens the remediation story: Bun
      needs a lint-enforced exception list for safe shared-receiver mutation
      helpers, not duplicate counts for every syntactic variant.

118. **EXP-097 direct Bun-crate witness**
    - Added `experiments/EXP-097-bun-errno-crate/`, a source-linked harness
      depending on `bun_errno` by path.
    - Ran release-mode Miri on the harness. The witness calls safe
      `bun_errno::SystemErrno::from_raw(138)` and Miri reports invalid enum
      construction at `/data/projects/bun/src/errno/lib.rs:310`, inside Bun's
      actual `SystemErrno::from_raw` implementation.
    - Retained raw output at
      `phase5_experiment_results/EXP-097-bun-errno-crate.log` and updated the
      registry, syn-walker triage note, remediation plan, and final report.
      This strengthens EXP-097 from mirror-only evidence to mirror + direct
      Bun-crate evidence where the target platform exposes the path.
    - The direct witness raises the raw phase-5 log count from the W4
      checkpoint's 120 to 121.

119. **EXP-019 direct Bun-crate witness**
    - Added `experiments/EXP-019-bun-ast-crate/`, a source-linked harness
      depending on `bun_ast` by path.
    - Ran Miri on the harness. It constructs a real
      `bun_ast::StoreSlice::new(&[Cell<u32>])`, copies the lifetime-erased
      wrapper into two scoped threads, and Miri reports a data race in
      `Cell::set`.
    - Retained raw output at
      `phase5_experiment_results/EXP-019-bun-ast-crate.log` and updated the
      registry, Phase-4 table row, remediation plan, and final report. This
      strengthens EXP-019 from mirror-only evidence to mirror + direct
      Bun-crate evidence while keeping the registry totals unchanged.
    - The direct witness raises the raw phase-5 log count from 121 to 122.

120. **EXP-002 direct Bun-crate witness**
    - Added `experiments/EXP-002-bun-errno-crate/`, a source-linked harness
      depending on `bun_errno` by path.
    - Ran Miri on the harness. It feeds the bit pattern for `isize -134` into
      the real Linux `GetErrno for usize` implementation and Miri reports an
      invalid enum tag at `/data/projects/bun/src/errno/linux_errno.rs:192`.
    - Retained raw output at
      `phase5_experiment_results/EXP-002-bun-errno-crate.log` and updated the
      registry, Phase-4 table row, remediation plan, and final report. This
      strengthens EXP-002 from mirror-only evidence to mirror + direct
      Bun-crate evidence while keeping the registry totals unchanged.
    - The direct witness raises the raw phase-5 log count from 122 to 123.

121. **EXP-085 direct Bun-crate witness**
    - Added `experiments/EXP-085-bun-core-crate/`, a source-linked harness
      depending on `bun_core` by path.
    - Ran Miri on the harness. It calls the real safe
      `bun_core::fmt::s(&[0xff])` display adapter, then consumes the rendered
      string; Miri reports UB in `core::str::next_code_point`.
    - Retained raw output at
      `phase5_experiment_results/EXP-085-bun-core-crate.log` and updated the
      registry, Phase-4 table row, remediation plan, and final report. This
      strengthens EXP-085 from mirror-only evidence to mirror + direct
      Bun-crate evidence while keeping the registry totals unchanged.
    - The direct witness raises the raw phase-5 log count from 123 to 124.

122. **EXP-089 direct Bun-crate witness**
    - Added `experiments/EXP-089-bun-core-crate/`, a source-linked harness
      depending on `bun_core` by path.
    - Ran Miri on the harness. It calls the real `bun_core::PathBuffer::uninit()`
      constructor and Miri reports invalid uninitialized integer construction at
      `/data/projects/bun/src/bun_core/util.rs:1003`.
    - Retained raw output at
      `phase5_experiment_results/EXP-089-bun-core-crate.log` and updated the
      registry, Phase-4 table row, remediation plan, and final report. This
      strengthens EXP-089 from mirror-only evidence to mirror + direct
      Bun-crate evidence while keeping the registry totals unchanged.
    - The direct witness raises the raw phase-5 log count from 124 to 125.

123. **EXP-088 direct Bun-crate witness**
    - Added `experiments/EXP-088-bun-ast-crate/`, a source-linked harness
      depending on `bun_ast` by path.
    - Ran Miri with symbolic alignment enabled. It calls the real
      `bun_ast::E::String::init_utf16(&[u16; 2])` constructor and then
      `slice16()`. Miri rejects the expanded retag at
      `/data/projects/bun/src/ast/e.rs:1424`; the `init_utf16` call created a
      tag for `[0x0..0x2]`, while `slice16()` requested `[0x0..0x4]`.
    - Retained raw output at
      `phase5_experiment_results/EXP-088-bun-ast-crate.log` and updated the
      registry, Phase-4 table row, remediation plan, and final report. This
      strengthens EXP-088 from mirror-only evidence to mirror + direct
      Bun-crate evidence while keeping the registry totals unchanged.
    - The direct witness raises the raw phase-5 log count from 125 to 126.

124. **EXP-093 direct Bun-crate witness**
    - Added `experiments/EXP-093-bun-exe-format-crate/`, a source-linked
      harness depending on `bun_exe_format` by path.
    - The harness builds a minimal PE byte buffer and calls the real
      `PEFile::init(&data)` public API. Under
      `MIRIFLAGS="-Zmiri-symbolic-alignment-check"`, Miri rejects
      `/data/projects/bun/src/exe_format/pe.rs:317` while the parser
      materialises `&DOSHeader` from byte-backed `Vec<u8>` storage.
    - This is stronger than the original mirror-only witness: the mirror
      isolates the later `&[SectionHeader]` path with an odd
      `section_headers_offset`; the direct crate witness proves the same
      alignment class in Bun's public parser before that later path is reached.
    - Retained raw output at
      `phase5_experiment_results/EXP-093-bun-exe-format-crate.log` and updated
      the registry, Phase-4 table row, remediation plan, and final report.
    - The direct witness raises the raw phase-5 log count from 126 to 127.

125. **EXP-095 direct Bun-crate witness**
    - Added `experiments/EXP-095-bun-exe-format-crate/`, a source-linked
      harness depending on `bun_exe_format` by path.
    - The harness builds a minimal Mach-O image with a `__BUN,__bun` section,
      calls the real `MachoFile::init(&data)` and then
      `MachoFile::write_section(b"payload")`.
    - Under `MIRIFLAGS="-Zmiri-symbolic-alignment-check"`, Miri rejects
      `/data/projects/bun/src/exe_format/macho.rs:122` while the writer
      materialises `&mut [section_64]` over the byte-backed load-command
      region.
    - This strengthens EXP-095 from mirror-only evidence to mirror + direct
      Bun-crate evidence. The mirror still isolates the `&mut symtab_command`
      path at `macho.rs:366`; the direct witness proves the same alignment
      class through the real public writer at `macho.rs:121-130`.
    - Retained raw output at
      `phase5_experiment_results/EXP-095-bun-exe-format-crate.log` and updated
      the registry, Phase-4 table row, remediation plan, and final report.
    - The direct witness raises the raw phase-5 log count from 127 to 128.

126. **EXP-078 direct Bun-crate witness**
    - Added `experiments/EXP-078-bun-core-crate/`, a source-linked harness
      depending on `bun_core` by path.
    - The harness imports `bun_core::util::ArrayLike`, calls the real
      `Vec<bool>` implementation of `set_len_and_slice(1)`, and reads
      `live[0]` through the safe returned `&mut [bool]`.
    - Miri reports an uninitialized `bool` read in the harness, confirming the
      same safe-API defect without relying on the earlier mirror trait copy.
    - Retained raw output at
      `phase5_experiment_results/EXP-078-bun-core-crate.log` and updated the
      registry, Phase-4 table row, remediation plan, and final report.
    - The direct witness raises the raw phase-5 log count from 128 to 129.

127. **EXP-080 direct Bun-crate macro witness**
    - Added `experiments/EXP-080-bun-dispatch-crate/`, a source-linked harness
      depending on Bun's real `bun_dispatch` proc-macro crate by path.
    - The harness calls `bun_dispatch::link_interface!`, implements the
      generated `link_impl_Handle!` macro, then constructs the generated
      `Handle { kind, owner }` from safe code with `owner = null_mut()` and
      calls the generated safe dispatcher.
    - Miri reports `memory access failed: attempting to access 1 byte, but got
      null pointer` in the generated implementation thunk, proving EXP-080
      through Bun's actual macro expansion rather than only the minimized
      mirror.
    - Retained raw output at
      `phase5_experiment_results/EXP-080-bun-dispatch-crate.log` and updated
      the registry, Phase-4 table row, remediation plan, and final report.
    - The direct witness raises the raw phase-5 log count from 129 to 130.

128. **EXP-081 direct Bun-crate attempt recorded as tool-limited**
    - Added `experiments/EXP-081-bun-sys-crate/`, a source-linked harness
      depending on `bun_sys` by path.
    - The first version used `bun_sys::open_dir_absolute` and stopped at
      existing EXP-089 (`PathBuffer::uninit`) before reaching the iterator.
      That raw output is retained at
      `phase5_experiment_results/EXP-081-bun-sys-crate.log`.
    - The revised version opens the directory fd via `libc::open`, feeds it
      into `bun_sys::dir_iterator::iterate`, and reaches
      `src/sys/lib.rs:345` / `src/sys/linux_syscall.rs:498`.
    - Miri then stops because Linux `SYS_getdents64` (syscall 217) is
      unsupported. Raw output retained at
      `phase5_experiment_results/EXP-081-bun-sys-crate-disable-isolation.log`.
    - This does **not** upgrade EXP-081 from mirror-confirmed to direct
      Bun-crate-confirmed evidence. It is recorded so reviewers see the honest
      tool limitation and do not mistake the direct attempt logs for an extra
      UB proof.
    - The two attempt logs raise the raw phase-5 log count from 130 to 132.

129. **EXP-072 direct Bun-crate HiveArray witness**
    - Added `experiments/EXP-072-bun-collections-crate/`, a source-linked
      harness depending on `bun_collections` by path.
    - The harness calls the real deprecated
      `HiveArray::<NeedsDrop, 1>::get()`, intentionally leaves the returned
      slot uninitialized, then calls the real `HiveArray::put()`.
    - Default Miri reports a Stacked Borrows retag violation in
      `HiveArray::put` at `src/collections/hive_array.rs:347`: the pointer
      returned from `get()` was derived from the previous `&mut self` borrow
      and is invalidated by the later `&mut self` receiver retag for `put()`.
    - A second run with `-Zmiri-disable-stacked-borrows` exposes the original
      validity bug behind that aliasing failure: `NeedsDrop::drop` reads an
      uninitialized `NonZeroU32`, again called from `HiveArray::put` at
      `hive_array.rs:347`.
    - Retained raw output at
      `phase5_experiment_results/EXP-072-bun-collections-crate.log` and
      `phase5_experiment_results/EXP-072-bun-collections-crate-no-sb.log`,
      and updated the registry, Phase-4 table row, and final report.
    - The two direct witness logs raise the raw phase-5 log count from 132 to
      134.

130. **Syn-walker round 99 rerun / calibration**
    - Corrected the failed round-98 invocation that assumed local
      `target/release/...` binaries. In this environment the skill binaries are
      in `/tmp/cargo-target/release`.
    - Reran all seven syn-walker binaries against `/data/projects/bun/src` and
      retained the round-99 raw outputs under `phase2_raw/`:
      `codex_syn_data_races_round99_2026-05-16.log`,
      `codex_syn_transmute_pairs_round99_2026-05-16.jsonl`,
      `codex_syn_pin_round99_2026-05-16.log`,
      `codex_syn_safety_doc_round99_2026-05-16.log`,
      `codex_syn_escape_round99_2026-05-16.log`, plus scaffold-status logs for
      `aliasing` and `validity`.
    - Added `CODEX_SYN_WALKER_ROUND99_TRIAGE_2026-05-16.md`.
    - No registry verdict changed. The useful result is calibration: 155
      Send/Sync rows, 24 transmute rows, 197 `mem::replace` rows, and 2,565
      safety-doc rows are either already owned by EXP entries, already in the
      hardening queue, or documentation debt rather than fresh confirmed UB.
    - Reran the convergence tracker. Round 99 remains quiet with
      `OPEN=0`, `NEEDS_REFINEMENT=0`, `CONFIRMED_UB=59`, `NO_EVIDENCE=15`,
      `DEFERRED=17`, and `RESOLVED=2`.

131. **Miri mini-matrix over direct Bun-crate witnesses**
    - Added `CODEX_MIRI_MINI_MATRIX_ROUND99_2026-05-16.md`.
    - Reran selected direct Bun-crate witnesses across alternate Miri axes:
      EXP-080 under default + Tree Borrows, EXP-085 under default + strict
      provenance, and EXP-089 under default + symbolic alignment.
    - Retained raw output at
      `phase5_experiment_results/CODEX-miri-mini-matrix-round99-2026-05-16.log`.
    - The pass added no EXP entries and changed no verdicts. It confirmed that
      EXP-080, EXP-085, and EXP-089 remain source-linked direct Bun-crate UB
      witnesses under at least one supported Miri axis, with EXP-080 and
      EXP-085 surviving a second axis unchanged.
    - Tooling correction: current nightly rejects the skill script's stale
      `-Zmiri-check-number-validity` flag. The EXP-089 witness still fails
      under default Miri and under `-Zmiri-symbolic-alignment-check`; future
      matrix runs should not copy that obsolete flag blindly.
    - The mini-matrix log raises the raw phase-5 log count from 134 to 135.
      Round 100 remains quiet with `OPEN=0`, `NEEDS_REFINEMENT=0`,
      `CONFIRMED_UB=59`, `NO_EVIDENCE=15`, `DEFERRED=17`, and `RESOLVED=2`.

132. **EXP-098 AtomicCell generic safe-API promotion**
    - Re-read `src/bun_core/atomic_cell.rs:50-83` after the round-99
      Send/Sync syn-walker output re-flagged `AtomicCell<T>`.
    - Corrected the earlier F-DR-7 / F-S-4 classification. The old rationale
      said `T: Atom` method gating made `AtomicCell<T: Copy>` defensible, but
      `new()` and `into_inner()` are safe and require only `T: Copy`.
    - Added `experiments/EXP-098-bun-core-crate/`, a direct Bun-crate witness
      that constructs `bun_core::AtomicCell<&Cell<u32>>`, moves it to a scoped
      thread via the wrapper's unsafe `Send`, calls safe `into_inner()`, and
      races the returned `&Cell<u32>` against the original thread.
    - Miri default and Tree-Borrows runs both report a non-atomic `Cell` data
      race. Raw logs:
      `phase5_experiment_results/EXP-098-bun-core-crate-default.log` and
      `phase5_experiment_results/EXP-098-bun-core-crate.log`.
    - Promoted registry entry EXP-098 to `CONFIRMED_UB`, updated Phase-2,
      Phase-4, Phase-8, `FINAL_UB_REPORT.md`, and `UB_RUNBOOK.md` counts to
      94 registry entries / 60 `CONFIRMED_UB`.
    - The raw phase-5 log count increases from 135 to 137. Production
      instantiations still appear atomically valid; the promoted claim is the
      public safe abstraction contract, not an in-tree production race path.
    - W4 drift check: latest fetched `origin/main@e750984db6` still carries the
      same `AtomicCell<T: Copy>` unsafe auto-trait impls and `new()` /
      `into_inner()` signatures; there is no diff for
      `src/bun_core/atomic_cell.rs` between audited base `4d443e5402` and
      `origin/main`.
    - Registry lint passes after the promotion. Convergence round 102 is quiet
      after the W4 drift note:
      `OPEN=0`, `NEEDS_REFINEMENT=0`, `CONFIRMED_UB=60`,
      `NO_EVIDENCE=15`, `DEFERRED=17`, and `RESOLVED=2`.

133. **EXP-047 safe-boundary demotion**
    - Re-read `src/bun_core/atomic_cell.rs:476-589` (`ThreadCell<T>`) and
      `src/bun_core/util.rs:2248-2295` (`RacyCell<T>`), plus the two real
      `ThreadCell` statics: `src/io/lib.rs:674` and `src/http/lib.rs:727`.
    - Found an overclaim in the previous EXP-047 wording. The old Miri witness
      races `RacyCell<Cell<u32>>`, but the actual read/write path performs
      caller-side `unsafe { &*rc.get() }`. That proves the raw-pointer contract
      can be violated; it does not prove an unsound safe API in Bun.
    - Added `experiments/EXP-047-safe-boundary-bun-core/`, a direct Bun-crate
      boundary check. `safe_share_compiles` proves safe code can share
      `&'static RacyCell<Cell<u32>>` / `&'static ThreadCell<Cell<u32>>` and
      call `.get()` / `.get_unchecked()`. `raw_pointer_send_fails` proves the
      raw `*mut Cell<u32>` cannot be sent across threads in safe Rust.
    - Raw log:
      `phase5_experiment_results/EXP-047-safe-boundary-bun-core.log`.
    - Demoted registry entry EXP-047 to `NO_EVIDENCE`, with prose explaining
      that this is no evidence for the Bun project-UB claim.
      The issue remains in the remediation plan as hardening / payload-access
      audit debt, but it is no longer counted in the confirmed-UB headline.
    - Production payload audit: `IoRequestLoop::schedule` and
      `HTTPThread::schedule` use `get_unchecked()` only for documented
      queue/waker fields; owner-thread mutation stays on the owning thread.
      This is auditor-fragile but not a confirmed race.
    - Registry lint passes after the demotion. Convergence rounds 103 and 104
      are quiet:
      `OPEN=0`, `NEEDS_REFINEMENT=0`, `CONFIRMED_UB=59`,
      `NO_EVIDENCE=16`, `DEFERRED=17`, and `RESOLVED=2`.

134. **EXP-099 IPC singleton re-entry promotion**
    - Ran a targeted RacyCell / singleton safe-boundary sweep after EXP-047's
      demotion, specifically looking for cases where Bun's own safe API
      immediately turns a raw singleton pointer into a reference before a
      re-entrant callback.
    - Promoted `src/runtime/node/node_cluster_binding.rs:35-51,147-158` plus
      `src/jsc/ipc.rs:140-159` to EXP-099. The critical source shape is
      `child_singleton<'a>() -> &'a mut InternalMsgHolder` combined with
      `InternalMsgHolder::flush(&mut self)` running JS callbacks through
      `dispatch_unsafe`. The source comment already says callback dispatch can
      re-enter and that `black_box(ptr::from_mut(self))` is used to force
      reloads.
    - Added `experiments/EXP-099/`, a source-shaped Tree-Borrows model for the
      real `RacyCell<Option<_>>` singleton + `flush(&mut self)` + `black_box`
      pattern. Miri rejects the re-entry into `child_singleton()` while the
      `flush(&mut self)` protected tag is live:
      `phase5_experiment_results/EXP-099.log`.
    - This is deliberately separate from EXP-047. EXP-047 remains
      `NO_EVIDENCE` as a generic `RacyCell` safe-boundary finding because its
      old race needed caller-side `unsafe` raw-pointer dereference. EXP-099 is
      a stronger, source-specific safe-API defect: Bun's safe helper mints the
      `&mut`, and Bun's callback-running receiver keeps it live across
      possible re-entry.
    - Updated Phase-2 bucket 1 / 15 / 21, Phase-4 synthesis, Phase-8
      remediation, `FINAL_UB_REPORT.md`, and `UB_RUNBOOK.md`. At the time of
      the EXP-099 promotion, registry totals were 95 entries / 60
      `CONFIRMED_UB` / 16 `NO_EVIDENCE` / 17 `DEFERRED` / 2 `RESOLVED`; raw
      Phase-5 logs were 139. EXP-100 supersedes those totals below.

135. **EXP-100 UpgradedDuplex / SSLWrapper re-entry promotion**
    - Followed the same R-2 callback-receiver smoke trail into
      `src/uws/lib.rs` and its runtime consumers. `SSLWrapper` itself has
      multiple comments saying callbacks can re-enter via a fresh owner borrow,
      and `src/http/ProxyTunnel.rs` contains the correct disjoint-field pattern.
    - Found that `src/runtime/socket/UpgradedDuplex.rs` still uses the unsafe
      shape: exported/callback-facing `&mut self` methods borrow
      `&mut self.wrapper`, call `SSLWrapper::{flush,shutdown,write_data,start}`,
      and SSLWrapper synchronously calls back through `ctx: *mut UpgradedDuplex`.
      The callbacks at `UpgradedDuplex.rs:101-146` materialize
      `&mut UpgradedDuplex`; `on_close` runs `teardown()`, which writes
      `self.wrapper = None`.
    - Added `experiments/EXP-100/`, a source-shaped Tree-Borrows model. The
      first draft accidentally proved a setup-pointer invalidation artifact; the
      retained version initializes the wrapper through a raw owner first and
      then calls `close(&mut self)`, so the logged failure is the actual
      callback reborrow / write while the receiver protector is live. Raw log:
      `phase5_experiment_results/EXP-100.log`.
    - Added `CODEX_UPGRADED_DUPLEX_SSLWRAPPER_REENTRY_2026-05-16.md` to record
      the source audit and why `ProxyTunnel`'s raw-owner / disjoint-field
      callback accessors are the remediation pattern. Item 136 below corrects
      the overbroad "whole ProxyTunnel is clean" implication.
    - Updated Phase-2 bucket 1 / 15 / 21, Phase-4 synthesis, Phase-8
      remediation, `FINAL_UB_REPORT.md`, and `UB_RUNBOOK.md`. Registry totals
      are now 96 entries / 61 `CONFIRMED_UB` / 16 `NO_EVIDENCE` / 17
      `DEFERRED` / 2 `RESOLVED`; raw Phase-5 logs are now 140.
    - Post-promotion lint caught a missing `Falsifiability` field on EXP-100;
      added the explicit close/demote criteria and reran the registry lint plus
      convergence tracker.

136. **EXP-101 ProxyTunnel shutdown receiver-wrapper promotion**
    - Re-read `src/http/ProxyTunnel.rs` with fresh eyes after EXP-100. The
      callback path does contain the correct raw-owner / disjoint-field pattern:
      `wrapper_mut`, `ref_count_of`, `socket_of`, and the aliasing note at
      `ProxyTunnel.rs:222-230` explicitly forbid callbacks from materialising a
      whole-struct `&mut ProxyTunnel`.
    - Found one leftover stale wrapper: `ProxyTunnel::shutdown(&mut self)` at
      `src/http/ProxyTunnel.rs:707-711` calls `SSLWrapper::shutdown(true)` while
      the whole-struct receiver borrow is live. Live callers exist at
      `src/http/lib.rs:1347-1355` and `src/http/HTTPContext.rs:692-700`.
    - Added `experiments/EXP-101/`, a source-shaped Tree-Borrows model with a
      bad path and a good raw-owner control. The bad path mirrors
      `shutdown(&mut self)` and Miri rejects a callback raw-field write while the
      receiver's protected tag is live (`phase5_experiment_results/EXP-101.log`).
      The good path performs the same callback writes through raw-owner
      projection and runs clean (`phase5_experiment_results/EXP-101-good.log`).
    - Updated `CODEX_UPGRADED_DUPLEX_SSLWRAPPER_REENTRY_2026-05-16.md` so it
      now says "ProxyTunnel contains the fix pattern", not "ProxyTunnel is
      entirely the fix model." This is the key defensibility correction:
      maintainers can accept the good pattern without the report pretending the
      whole type was already clean.
    - Updated Phase-2 bucket 1 / 15 / 21, Phase-4 synthesis, Phase-8
      remediation, `FINAL_UB_REPORT.md`, `UB_RUNBOOK.md`, and added
      `CODEX_PROXY_TUNNEL_SHUTDOWN_REENTRY_2026-05-16.md`. Registry totals are
      now 97 entries / 62 `CONFIRMED_UB` / 16 `NO_EVIDENCE` / 17 `DEFERRED` /
      2 `RESOLVED`; raw Phase-5 logs are now 142.

137. **EXP-102 ProxyTunnel write receiver-wrapper promotion**
    - Continued the ProxyTunnel receiver-wrapper sweep with the request
      body/header write path. `ProxyTunnel::write(&mut self, buf)` at
      `src/http/ProxyTunnel.rs:768-775` calls `SSLWrapper::write_data` while the
      whole-struct receiver borrow is live.
    - Verified live callers at `src/http/lib.rs:2876-2888`
      (`RequestStage::ProxyBody`) and `src/http/lib.rs:2913-2947`
      (`RequestStage::ProxyHeaders`).
    - Re-read `src/uws/lib.rs:739-785, 1069-1128` and
      `src/http/ProxyTunnel.rs:483-497`: `write_data` can synchronously enter
      `handle_traffic`, `write_encrypted`, and close callbacks while the
      receiver protector is still live.
    - Added `experiments/EXP-102/`, a source-shaped Tree-Borrows model with a
      bad path and a raw-owner `write_raw` control. The bad path reports a
      forbidden callback raw-field write in
      `phase5_experiment_results/EXP-102.log`; the raw-owner control passes in
      `phase5_experiment_results/EXP-102-good.log`.
    - Updated the registry, Phase-2 bucket 1 / 15 / 21, Phase-4 synthesis,
      Phase-8 remediation, `FINAL_UB_REPORT.md`, `UB_RUNBOOK.md`,
      `CODEX_PROXY_TUNNEL_SHUTDOWN_REENTRY_2026-05-16.md`, and
      `CODEX_UPGRADED_DUPLEX_SSLWRAPPER_REENTRY_2026-05-16.md`. Registry totals
      are now 98 entries / 63 `CONFIRMED_UB` / 16 `NO_EVIDENCE` / 17
      `DEFERRED` / 2 `RESOLVED`; raw Phase-5 logs are now 144.

138. **EXP-103 ProxyTunnel raw-capture-first receiver-wrapper promotion**
    - Continued the ProxyTunnel sweep past `shutdown` and `write`, specifically
      testing the local belief that `let this = NonNull::from(&mut *self)` at
      the top of a method is enough to avoid receiver-protector UB.
    - Re-read `src/http/ProxyTunnel.rs:714-749` and `:752-765`: both
      `on_writable(&mut self, ...)` and `receive(&mut self, ...)` capture a raw
      pointer first, then call `SSLWrapper::flush` / `receive_data`, which can
      synchronously invoke callbacks that write tunnel fields through raw
      projections.
    - Verified live callers at `src/http/lib.rs:2754-2755` (`on_writable`) and
      `src/http/lib.rs:3254-3258` (`receive`).
    - Added `experiments/EXP-103/`, a four-mode Tree-Borrows model:
      `on-writable-bad`, `receive-bad`, `on-writable-good`, and `receive-good`.
      The two bad paths fail with protected receiver tags created at the
      `&mut self` methods; both raw-owner controls pass.
    - Defensibility correction: the source comment's raw-capture-first argument
      is insufficient under Tree Borrows. The fix is not "capture raw earlier";
      the entry point itself must be raw-owner / `NonNull<Self>`.
    - Updated registry, Phase-2 bucket 1 / 15 / 21, Phase-4 synthesis, Phase-8
      remediation, `FINAL_UB_REPORT.md`, `UB_RUNBOOK.md`,
      `CODEX_PROXY_TUNNEL_SHUTDOWN_REENTRY_2026-05-16.md`, and
      `CODEX_UPGRADED_DUPLEX_SSLWRAPPER_REENTRY_2026-05-16.md`. Checkpoint
      metrics at EXP-103 were entries=99, confirmed=64,
      no-evidence=16, deferred=17, resolved=2, and raw Phase-5 logs=148. The
      current totals after EXP-104 are recorded in the next entry.

139. **EXP-104 WindowsNamedPipe / SSLWrapper receiver-protector promotion**
    - Re-read `src/runtime/socket/WindowsNamedPipe.rs` with the ProxyTunnel
      receiver-wrapper lesson in mind. The important correction is subtle:
      `WRAPPER_BUSY` is a valid wrapper-lifetime guard, but it does not remove
      the protected `&mut self` receiver created by the generated
      `#[bun_uws::uws_callback]` thunk in `src/jsc_macros/lib.rs:828-843`.
    - Confirmed callback-driving source shapes at
      `WindowsNamedPipe.rs:261-315,394-407,554-610,1038-1052,1127-1152,1166-1238`.
      The direct Miri model covers representative `flush` and receive paths;
      the remaining listed paths are same-shape remediation scope. The common
      property is that `SSLWrapper::flush`, `receive_data`, `write_data`,
      `start`, or `shutdown` can run while a whole-struct receiver borrow is
      live, and those calls can synchronously re-enter generated callbacks such
      as `ssl_write`, `ssl_on_data`, and `ssl_on_close`.
    - Added `experiments/EXP-104/`, a four-mode Tree-Borrows model:
      `flush-bad`, `receive-bad`, `flush-good`, and `receive-good`. The bad
      paths keep the same `WRAPPER_BUSY` lifetime guard and still fail with a
      forbidden callback reborrow under the protected receiver. The raw-owner
      controls keep the guard and pass, which is the remediation direction.
    - Updated the registry, Phase-2 bucket 1 / 21, Phase-4 synthesis, Phase-8
      remediation, `FINAL_UB_REPORT.md`, `UB_RUNBOOK.md`, and added
      `CODEX_WINDOWS_NAMED_PIPE_SSLWRAPPER_REENTRY_2026-05-16.md`. At that
      checkpoint, registry totals were 100 entries / 65 `CONFIRMED_UB` / 16
      `NO_EVIDENCE` / 17 `DEFERRED` / 2 `RESOLVED`; raw Phase-5 logs were 152.

140. **`#[uws_callback]` receiver sweep guardrail**
    - Ran a whole-workspace source sweep for real `#[uws_callback]` attributes:
      `rg -n '^\s*#\[(bun_uws::)?uws_callback' src -g '*.rs'`.
    - Counted 38 real attributes, split across `UpgradedDuplex` (11),
      `WindowsNamedPipe` (13), `Request` (6), `CryptoHasher` (3),
      `FetchTasklet` (2), `WTFTimer` (2), and `Subprocess` (1).
    - Added `CODEX_UWS_CALLBACK_RECEIVER_SWEEP_2026-05-16.md` to prevent a
      bad overgeneralization: the macro is not globally unsound. The confirmed
      defect class is narrower: a generated or internal whole-struct receiver
      remains protected while the method enters SSLWrapper / uSockets / JS /
      another subsystem that can synchronously call back into the same object.
    - No new EXP was created from this sweep; it reinforces EXP-100 and
      EXP-104 and keeps accessors / non-re-entrant callbacks out of the bug
      count.

141. **`LaunderedSelf` / `black_box(ptr::from_mut(self))` guardrail**
    - Added `CODEX_LAUNDERED_SELF_BLACK_BOX_GUARDRAIL_2026-05-16.md` and a
      supporting model under `experiments/EXP-105/`.
    - Important source-faithfulness correction: the model is **not** a new
      registry finding and should not inflate the headline count. A direct
      `black_box(ptr::from_mut(self))` followed by `LaunderedSelf::r(this)`
      ran clean under Tree Borrows. The failing path requires the receiver to
      enter callback-capable code that performs an intervening foreign write or
      re-entry before the stale raw pointer is used again.
    - The three support logs are:
      `phase5_experiment_results/EXP-105-direct.log` (clean),
      `phase5_experiment_results/EXP-105-callback.log` (Tree-Borrows UB), and
      `phase5_experiment_results/EXP-105-good.log` (raw-owner control clean).
    - This reinforces EXP-099/100/101/102/103/104 and the Phase-8 remediation
      choice "remove the protected receiver from callback-running paths." It
      does **not** prove that every `LaunderedSelf` implementation or every
      `Self::r(this)` call is independently UB. PipeWriter call sites need a
      source-specific callback/re-entry audit before any additional promotion.
    - At that checkpoint, raw Phase-5 log count rose to 155, but registry
      totals remained 100 entries / 65 `CONFIRMED_UB` / 16 `NO_EVIDENCE` / 17
      `DEFERRED` / 2 `RESOLVED`.

142. **EXP-106 `PipeWriter` / `FileSink` re-entry promotion**
    - Added `CODEX_PIPEWRITER_LAUNDERED_SELF_REENTRY_2026-05-16.md`, a
      source-shaped Tree-Borrows model under `experiments/EXP-106/`, and raw
      logs `phase5_experiment_results/EXP-106-bad.log` /
      `phase5_experiment_results/EXP-106-good.log`.
    - Corrected source framing: `FileSink`'s parent-side `borrow = ptr`
      discipline is sound and should be preserved. The defect is one layer
      lower: `PipeWriter` completion/error entry points still receive
      `&mut self` and then call `Parent::on_write`, whose concrete `FileSink`
      parent can re-enter the same intrusive writer via `writer.with_mut`.
    - The bad-path model fails under Tree Borrows; the raw-owner control passes.
      This promotes EXP-106 to `CONFIRMED_UB` without generalizing the
      non-counted EXP-105 `LaunderedSelf` guardrail into a blanket claim.
    - The then-current Phase-5 log and registry counts were later superseded by
      correction 146. The live totals after correction 146 are 103 entries /
      68 `CONFIRMED_UB` / 16 `NO_EVIDENCE` / 17 `DEFERRED` / 2 `RESOLVED`.

143. **W4 latest-main wording refresh after EXP-106**
    - Fetched `origin/main`; latest remains `e750984db6`, seven commits ahead
      of audited base `4d443e5402`.
    - Verified the EXP-106 source shape remains live on latest main:
      `src/io/PipeWriter.rs:426-451,1572-1619,2105-2185` still uses
      `&mut self` completion/error entry points plus
      `black_box(ptr::from_mut(self))`, and `FileSink::on_write` can still
      re-enter `writer.with_mut` at current `src/runtime/webcore/FileSink.rs`
      lines 522 / 524. The upstream drift commits changed only
      comments/formatting in these files.
    - Updated `CODEX_MAIN_DRIFT_NOTE_2026-05-16.md`,
      `CODEX_W4_LATEST_MAIN_REFRESH_2026-05-16.md`, and
      `CODEX_W4_REFRESH_TRIAGE_2026-05-16.md` so public wording says 66
      pinned-base confirmed findings, while still forbidding an exact
      latest-main live count until every EXP is replayed.

144. **Ast-grep round-115 refresh**
    - Reran the UB skill's bundled ast-grep pack against `src/`. Raw output
      (`phase2_raw/codex_ast_grep_ub_patterns_round115_2026-05-16.log`,
      84,757 lines, ~6 MB) was later dropped from the PR to keep the diff
      reviewable; regenerate by rerunning the pack against `origin/main@4d443e5402`.
    - Added `CODEX_AST_GREP_ROUND115_REFRESH_2026-05-16.md` to prevent raw
      detector counts from becoming public finding counts.
    - High-signal aliasing hits were unchanged from the prior ast-grep review:
      `copy_file.rs:1580/1666` are EXP-073; `Sink.rs:1232` is
      EXP-048-adjacent strict-provenance work; `util.rs:747`,
      `doStep5.rs:694`, `TopExceptionScope.rs:497`, and `ini/lib.rs:1361`
      remain reviewed/demoted; `HostProcess.rs:94`, `JSGlobalObject.rs:183`,
      and `bun_string_jsc.rs:77` remain FFI constness / thread-affinity
      hardening queues without C++ side-effect proof.
    - No registry count change.

145. **Syn-walker round-115 refresh**
    - Reran the bundled syn walkers and saved raw outputs under
      `phase2_raw/codex_syn_*_round115_2026-05-16.{stdout,stderr}`.
    - Added `CODEX_SYN_WALKER_ROUND115_REFRESH_2026-05-16.md`.
    - Results: `transmute_pairs` produced 24 inventory rows, `data_races`
      produced 155 unsafe-impl warning rows, `escape` produced 0 rows, and
      `safety_doc_coverage` produced 2,565 documentation-coverage rows.
      `aliasing` / `validity` are scaffolded; `pin` was not available as a bin
      target in this build.
    - Mapped the high-signal outputs to existing EXP owners or documentation /
      hardening queues. No registry count change.

146. **Callback-receiver shape sweep — EXP-107/EXP-108**
    - Swept all `black_box(ptr::from_mut(self))` / receiver-derived raw pointer
      sites after EXP-106 showed this family remained undercounted.
    - Promoted **EXP-107** for `RareData::close_all_watchers_for_isolation`:
      the source comment explicitly says watcher close callbacks re-enter JS
      and can push into the same watcher vectors. The bad Tree-Borrows model
      fails; the raw-owner cleanup-loop control passes.
    - Promoted **EXP-108** for `EventLoop::run_callback` /
      `run_callback_with_result`: the source comment explicitly says JS can
      re-enter the same event loop through `vm.event_loop()` and run nested
      `enter()/exit()` pairs. The bad Tree-Borrows model fails; the raw-owner
      callback-runner control passes.
    - Added `CODEX_CALLBACK_RECEIVER_SHAPE_SWEEP_2026-05-16.md` to document
      reviewed-but-not-promoted suspects (`PipeReader`, S3 multipart,
      HTTPServerWritable, h2 stream queue, `VirtualMachine::wait_for`) and to
      prevent the inaccurate blanket claim that all `black_box`/`LaunderedSelf`
      sites are UB.
    - Updated `FINAL_UB_REPORT.md`, `UB_RUNBOOK.md`, `phase4_unified_findings.md`,
      and `phase8_remediation_plan.md` to the then-live counts: 103 registry
      entries, 68 `CONFIRMED_UB`, 179 unified rows, and 161 raw logs. Item 147 supersedes these totals.

147. **Late deep-pass normalization and root-graph correction — EXP-109/EXP-110/EXP-111**
    - Promoted **EXP-110** for `h2_frame_parser::Stream::queue_frame(&mut self)`:
      source comments at `h2_frame_parser.rs:1859-1867` explicitly describe
      JS write-callback re-entry through `client.streams` into the same
      `Stream`. The bad Tree-Borrows model fails; the raw-owner control passes.
    - Demoted **EXP-109** to `NO_EVIDENCE` for the original production
      `JSCallback` GC-root-loss hypothesis. Source review found the missing
      root: `JSCallback.#ctx` owns a heap `Function`, that `Function` owns
      `FFICallbackFunctionWrapper`, and the wrapper owns `JSC::Strong<JSFunction>`
      plus `JSC::Strong<GlobalObject>`. The standalone stale-handle Miri model is
      retained only as a non-source-faithful guard.
    - Promoted **EXP-111** for the bundler part-range worker path that still
      materializes concurrent whole-owner `&mut Chunk` / `&mut LinkerContext`
      references from shared raw pointers. The mutable `Renamer<'r>` view is
      a real subproblem, but a renamer-only patch is incomplete. The
      default-Miri retag/data-race witness fails at the concurrent
      `&mut Chunk` construction; the Tree-Borrows read-only model is clean
      and is recorded as a model-boundary note, not failure evidence.
    - Updated `FINAL_UB_REPORT.md`, `UB_RUNBOOK.md`,
      `phase4_unified_findings.md`, and `phase8_remediation_plan.md` to the
      live counts: 106 registry entries, 70 `CONFIRMED_UB`, 0
      `NEEDS_REFINEMENT`, 17 `NO_EVIDENCE`, 182 unified rows, and 171 raw logs.
    - Re-ran the formal tracker twice after EXP-109 demotion. Round 118 is
      **CONVERGED**: `OPEN=0`, `NEEDS_REFINEMENT=0`, `CONFIRMED_UB=70`,
      `NO_EVIDENCE=17`, `DEFERRED=17`, `RESOLVED=2`.

148. **Post-convergence artifact hygiene — stale wording and remediation-scope fixes**
    - Corrected stale EXP-109 prose in `DEEP_PASS_2_SYNTHESIS_2026-05-16.md`,
      `DIFFERENTIAL_RUST_VS_ZIG_2026-05-16.md`,
      `ADVERSARIAL_REAUDIT_2026-05-16.md`, and
      `DEEP_PASS_3_SYNTHESIS_2026-05-16.md`. The production `JSCallback`
      root-loss claim stays demoted to `NO_EVIDENCE`; the quarantined
      `ffi-bare-jsvalue-regression.test.ts` is explicitly a rejected artifact,
      not a valid Bun regression test.
    - Corrected stale EXP-111 rows in Phase-2/Phase-4 artifacts. The current
      claim is the broad bundler part-range fan-out defect: concurrent
      whole-owner `&mut Chunk` / `&mut LinkerContext` worker entries are the
      default-Miri failure point, while the mutable renamer view is a real
      subproblem. A renamer-only patch does not close EXP-111.
    - Split R-EXP-020 from R-EXP-029 in `phase8_remediation_plan.md`.
      `URL::host_with_path` has a local provenance-preserving subslice fix;
      `EnvStr` remains a packed-address representation migration. The old
      combined "`ptr::with_exposed_provenance` wins" recommendation was too
      weak for EXP-020.
    - Normalized EXP-047's verdict label in the registry to plain
      `NO_EVIDENCE` so mechanical counts and human counts match exactly.
    - Hardened the staged `scripts/regression-runner.sh`: negative-control
      mode now refuses to run instead of checking out commits inside the
      active Bun worktree. A future negative-control runner must use a
      disposable git worktree.
    - Re-ran the formal tracker through round 123. Round 123 is
      **CONVERGED**: `OPEN=0`, `NEEDS_REFINEMENT=0`, `CONFIRMED_UB=70`,
      `NO_EVIDENCE=17`, `DEFERRED=17`, `RESOLVED=2`.

149. **Fresh-eyes pass — staged-helper safety and provenance wording**
    - Hardened `scripts/audit/bootstrap-vendor.sh`: `--force` now refuses in
      the staged helper, `--only` validates its argument, non-empty non-git
      vendor directories are not overwritten, unknown source repositories are
      skipped, and checkout uses `git checkout --detach` instead of
      a forced checkout.
    - Corrected Phase-11 soak templates to run against the audited base
      `origin/main@4d443e5402` in a disposable worker clone, not the prior
      `claude/unsafe-exorcist-audit` branch.
    - Made quarantined rejected helper scripts inert so accidental execution
      exits immediately with a "do not run" message.
    - Tightened EXP-047 presentation across Phase-2 / Phase-4 / final report
      tables: the verdict is plain `NO_EVIDENCE`, with prose noting that the
      project-UB claim was demoted.
    - Restored EXP-109's literal registry `Hypothesis` field after the source
      root-graph demotion, so the experiment registry passes the UB-exorcist
      structural linter again.
    - Tightened tagged-pointer remediation wording: `ptr::with_exposed_provenance`
      is an interim annotation only; strict-provenance closure requires a typed
      provenance-carrying representation.
    - Corrected the registry numbering note and all current-count summaries to
      name EXP-105 as a non-canonical support-model slot. Mechanical EXP-ID
      scan now agrees with the prose: 106 canonical registry blocks across
      EXP-001..EXP-111, with only EXP-022..025 and EXP-105 absent by design.
    - Re-counted `phase5_experiment_results/**/*.log` recursively and corrected
      the current raw-log total from stale `166` wording to `171` (167
      top-level logs plus 4 Loom logs under `phase5_experiment_results/loom/`).
    - Added supersession wording to W4/latest-main drift artifacts: older
      68-confirmed snapshots predate final EXP-109/110/111 normalization; the
      final pinned-base registry is 70 confirmed entries, while latest-main
      still needs per-EXP replay before quoting an exact live count.
    - Tightened the final report's verdict-count heading from ambiguous
      "live count from the registry" to "mechanical pinned-base registry
      count" and clarified that parenthetical raw verdict labels are normalized
      by leading verdict class.
    - Split a W4 latest-main lockfile row that had collapsed EXP-003/006 and
      EXP-036 into one `Buffers::read_array<T>` fix point. The corrected W4
      table now says EXP-003/006 close at `Package::load_fields` / checked
      `Meta` decoding, while EXP-036 closes at the `Buffers::read_array<T>`
      / `LockfileArrayElem` boundary.
    - Aligned EXP-064 summaries in the final report and UB runbook with the
      registry: the checked-bit-pattern / `ConstValidate` vehicle covers
      EXP-003 and EXP-006 too, even though S6 `read_array<T>` itself does not.
    - Corrected the EXP-064 taxonomy label from "enum transmutes" to
      "validity-bearing disk / FFI bytes" where EXP-036 is included. EXP-036 is
      a `bool` bit-pattern bug, not an enum transmute.

## Validation Commands

Registry lint:

```sh
python3 /home/ubuntu/.codex/skills/rust-undefined-behavior-exorcist/scripts/lint-experiment-designs.py \
  /data/projects/bun/.ub-exorcism/2026-05-15-exhaustive/UNDEFINED_BEHAVIOR_EXPERIMENT_DESIGNS.md
```

Result:

```text
[OK] /data/projects/bun/.ub-exorcism/2026-05-15-exhaustive/UNDEFINED_BEHAVIOR_EXPERIMENT_DESIGNS.md — all blocks well-formed
```

Convergence tracker:

```sh
/home/ubuntu/.codex/skills/rust-undefined-behavior-exorcist/scripts/convergence-tracker.sh \
  /data/projects/bun/.ub-exorcism/2026-05-15-exhaustive
```

Latest stored result after the post-convergence hygiene pass:
`phase7_convergence_round_123.json`.

```text
Round 123 CONVERGED (open=0, new_findings=0, new_needs=0, prev_quiet=true).
OPEN=0 CONFIRMED_UB=70 NO_EVIDENCE=17 NEEDS_REFINEMENT=0 DEFERRED=17 RESOLVED=2
```

Source-tree cleanliness:

```sh
git diff --stat -- . ':!.ub-exorcism' ':!.unsafe-audit'
```

Result: only the pre-existing unrelated `.gitignore` edit outside audit
directories; no Bun Rust/C++/TS source diffs.

Current local-worktree note (2026-05-16): the validation command now shows an
unrelated `.gitignore` edit (`.bv/` ignore entry) outside the UB audit tree.
No Bun Rust/C++/TS source files were touched by these defensibility corrections.

150. **Final-report process-wording cleanup**
    - Changed the opening line from "full application" to
      "broad Standard+/Exhaustive-intent application" so it cannot conflict
      with the explicit note that Phase 11 multi-day soak execution was
      designed but not run.
    - Merged the duplicated Phase-11 SOAK non-execution bullets in
      `FINAL_UB_REPORT.md` into one caveat that also preserves the latest
      quiet/converged tracker status.
    - Updated `phase10_fresh_eyes_log.md` to refer to the recursive
      `phase5_experiment_results/**/*.log` evidence set and to mark its
      58-confirmed count as a historical checkpoint, not the current source of
      truth.
    - Corrected `UB_RUNBOOK.md`'s W4 drift note from stale "68 confirmed
      entries" wording to the final pinned-base total: 70 confirmed entries.
    - Converted several older sweep artifacts from stale "live
      source-of-truth total" wording to checkpoint wording:
      `phase2_findings_08_send_sync.md`,
      `CODEX_PE_ALIGNMENT_FOLLOWUP_2026-05-16.md`,
      `CODEX_ALIGNMENT_PROVENANCE_SWEEP_2026-05-16.md`,
      `CODEX_SAFE_API_CONTRACT_SWEEP_2026-05-16.md`, and
      `CODEX_UNINIT_SCRATCH_SWEEP_2026-05-16.md`.
    - Reworded round-99 syn-walker and Miri mini-matrix documents so their
      59-confirmed convergence numbers are clearly checkpoint counts, not
      current totals.
    - Added `CURRENT_STATUS_README.md` as the front-door citation guard for
      current pinned-base counts, safe public wording, and common
      misreadings.
