[package]
name = "exp_002_linux_errno_transmute"
version = "0.0.1"
edition = "2021"
[[bin]]
name = "repro"
path = "src/main.rs"

# Detach from Bun's parent workspace so cargo treats this as standalone.
[workspace]
