[package]
name = "loom-imminent-gc-timer-publish"
version = "0.0.0"
edition = "2021"

[workspace]

[lib]
path = "src/main.rs"

[target.'cfg(loom)'.dependencies]
loom = "0.7"

[profile.release]
debug = true
