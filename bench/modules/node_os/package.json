{
  "name": "bench",
  "scripts": {
    "deps": "exit 0",
    "build": "exit 0",
    "bench:bun": "bun bun.js",
    "bench:node": "node node.mjs",
    "bench": "bun run bench:bun && bun run bench:node"
  }
}
