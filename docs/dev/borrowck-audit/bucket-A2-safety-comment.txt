src/bundler/bundle_v2.rs:1132 | ffi-provenance | keep: self_ptr is the *mut c_void context handed to JSBundlerPlugin__matchOnResolve; the JS completion callback mutates/consumes self, so *mut Self provenance is required
src/bundler/bundle_v2.rs:1265 | ffi-provenance | keep: self_ptr is the *mut c_void context handed to JSBundlerPlugin__matchOnLoad; the JS completion callback mutates/consumes self, so *mut Self provenance is required
src/bundler/bundle_v2.rs:6457 | reentrant-callback | keep: self_ptr is a ParentRef<BundleV2> stored into ParseTask.ctx and scheduled on the thread pool; the task calls back into BundleV2, so *mut Self write provenance is required
src/install/PackageManager/PackageManagerLifecycle.rs:234 | reentrant-callback | keep: event_loop.tick_once holds &mut self.event_loop while task.run(ctx) reborrows the enclosing PackageManager; raw *mut c_void ctx is the correct provenance carrier — replace comment with SAFETY note
src/io/PipeWriter.rs:509 | ffi-provenance | keep: *mut Self is stored as the poll Owner backref for later on_poll/on_close dispatch (may free self); required provenance — replace 'reshaped for borrowck' with a SAFETY comment
src/io/PipeWriter.rs:759 | ffi-provenance | keep: identical to line 509 — *mut Self stored as poll Owner backref for reentrant callback dispatch; replace comment with SAFETY note
src/js_parser_jsc/Macro.rs:222 | reentrant-callback | keep: closure is run through JSC__VM__holdAPILock and executes the JS macro, which may re-enter MacroContext::call and mutate self.macros/self.bump; holding &-refs across that would alias
src/jsc/VirtualMachine.rs:3781 | ffi-provenance | keep: the `*mut VirtualMachine` is stored as `RefString.ctx` for the WTF external-string finalizer (`clear_ref_string`) invoked later from C; a `&self`-derived pointer would carry SharedReadOnly provenance
src/runtime/api/bun/h2_frame_parser.rs:4014 | reentrant-callback | keep: handler calls self.adjust_window_size/self.dispatch_* which run JS that can re-enter the parser and touch self.streams; *mut Stream + dispatch_depth guard is the designed-for pattern (see enter_stream_dispatch)
src/runtime/api/bun/h2_frame_parser.rs:4399 | reentrant-callback | keep: dispatch_with_extra(onStreamEnd/onStreamError) runs JS that may re-enter and mutate self.streams; raw *mut Stream avoids holding a map borrow across re-entry
src/runtime/api/bun/h2_frame_parser.rs:4561 | reentrant-callback | keep: self.send_go_away/self.handle_incomming_payload dispatch JS that can re-enter the parser; *mut Stream from self.streams is required to avoid aliased &mut across re-entry
src/runtime/api/bun/h2_frame_parser.rs:4610 | reentrant-callback | keep: decode_header_block dispatches onStreamHeaders → JS re-entry into parser; stream ptr is re-derived after dispatch and frees are deferred by dispatch_depth, a safe &mut would be UB/panic here
src/runtime/api/bun/h2_frame_parser.rs:4734 | reentrant-callback | keep: handle_headers_frame calls decode_header_block/dispatch which fire JS that can re-enter and touch self.streams; *mut Stream is the intentional escape for the dispatch_depth-guarded re-entry model
src/runtime/jsc_hooks.rs:1066 | reentrant-callback | keep: same as auto_tick — event-loop tick re-enters JS which mutates VM/EventLoop; raw per-field deref is required
src/runtime/jsc_hooks.rs:670 | reentrant-callback | keep: wait_for_promise/event_loop().tick() re-enter JS which may mutate VM fields; holding &mut VM across that would alias
src/runtime/jsc_hooks.rs:903 | reentrant-callback | keep: tick_immediate_tasks/uws loop dispatch JS callbacks that re-enter the VM and its EventLoop; no stable &mut can span the tick
src/runtime/server/RequestContext.rs:2036 | reentrant-callback | keep: sink field is Option<NonNull> by design (JS GC-owned); assign_to_stream may synchronously fire on_first_write→handle_first_stream_write(&mut *this) while the outer frame still holds &mut *response_stream, so Box-in-self + safe borrow would be aliasing UB
src/runtime/webcore/FileSink.rs:1446 | ffi-provenance | keep: JSSink::assign_to_stream FFI takes both `*mut self` and a `void**` into self.signal.ptr that C++ writes the controller JSValue back through; cannot express as two safe &mut without changing the generated FFI signature
src/runtime/webcore/blob/read_file.rs:289 | ffi-provenance | keep: `ctx` is the async io-close callback's *mut Self (on_done re-enters/mutates self); the hoist before `&mut this.io_poll` is already the minimal safe ordering — replace comment with a SAFETY note on callback provenance
src/runtime/webcore/blob/write_file.rs:194 | ffi-provenance | keep: identical to read_file.rs:289 — `ctx` is the async io-close callback's *mut Self; hoisting before `&mut this.io_poll` is the correct ordering, replace comment with SAFETY note
