src/bundler/LinkerContext.rs:2479 | iterate by index + re-borrow per iteration: let n=chunks[index].cross_chunk_imports.len(); for i in 0..n { let ci=chunks[index].cross_chunk_imports[i].chunk_index; recurse } — the recursion never mutates chunks (alt: change param to &[Chunk])
src/bundler/LinkerContext.rs:2499 | iterate by index + re-borrow per iteration: compute pieces.len() up front, then each iter re-match &chunks[index].intermediate_output to read (kind,idx) in a short scope before the recursive &mut chunks call — recursion never mutates chunks (alt: change param to &[Chunk])
src/bundler/bundle_v2.rs:2440 | delete key_text boxing and pass path.text directly to both .put() calls (path is a local Fs::Path<'static>, path.text does not borrow self; line 2421 already does this)
src/bundler/defines.rs:209 | restructure: in the `get_mut` Some arm just `existing.push(DotDefine { parts, data: value_define.clone() })` instead of building a new Vec + extend_from_slice + assign-back
src/bundler/linker_context/computeChunks.rs:88 | iterate by index + re-borrow per iteration: set the bit in a scoped `&mut` block with no return, then at each use (lines 117/153) re-borrow `this.graph.files.items_entry_bits()[source_index as usize].bytes(...)` instead of holding a cloned `AutoBitSet`
src/bundler/linker_context/computeCrossChunkDependencies.rs:338 | take by &mut, use mem::take on chunk_metas[chunk_index].imports, iterate its keys, restore it after the loop (body only mutates chunk_metas[other].exports where other != chunk_index)
src/css/properties/flex.rs:879 | KEEP: alloc is required for correctness
src/dotenv/env_loader.rs:1238 | restructure: compute result first, mutate second — drop `value_owned` and write `*entry.value_ptr = HashTableValue { value: Box::from(value) }` directly at line 1251; `map` is disjoint from `self` so the borrow survives `get_or_put`
src/install/PackageManager/PackageManagerEnqueue.rs:111 | iterate by index + re-borrow per iteration: read dependencies[dep_i].behavior/.name_hash directly instead of cloning into `dep`; replace the clone-assign with dependencies.swap(peer_i, begin as usize)
src/install/PackageManager/PackageManagerEnqueue.rs:995 | KEEP: alloc is required for correctness
src/install/PackageManager/install_with_manager.rs:1325 | pass &[u8] instead of cloning: drop `.to_vec()` and keep `let path = manager.lockfile.str(&realname);` — `log_mut()` takes `&self` (raw-ptr deref), so it does not conflict with the immutable `manager.lockfile` borrow
src/install/PackageManager/patchPackage.rs:143 | pass &[u8] instead of cloning: drop `.to_vec()` on the name slices (lines 244, 281) — `lockfile` here is a local `Box<Lockfile>` disjoint from `&mut manager`, so the slice can be passed directly to `compute_cache_dir_and_subpath`
src/install/yarn.rs:1381 | iterate by index + re-borrow per iteration: store the matching entry index (Option<usize>) instead of cloning e.specs, then borrow &yarn_lock.entries[found_idx].specs in the second loop (all borrows are shared)
src/js_parser/lib.rs:539 | restructure: in the `get_mut` Some arm, after the merge-loop just `existing.push(DotDefine { data: value, parts }); return Ok(())` — only call `put_assume_capacity(vec![...])` in the None arm; drop `initial_values` and the extend_from_slice Vec
src/js_parser/visit/visit_stmt.rs:186 | wrap the entry in bun_ptr::BackRef::new(r) to detach the &self.options lifetime across the &mut self call, matching the existing pattern at visit/mod.rs:284
src/runtime/api/bun/h2_frame_parser.rs:2288 | use SmallVec<[(u32, u64); 8]> for `updates` — collecting before dispatch must stay, but inline storage covers the common stream count
src/runtime/api/filesystem_router.rs:445 | delete the .to_vec(); JsCell::get() returns &T from &self so `dir` is a shared borrow and bust_dir_cache_recursive(&self, ...) is also shared — they coexist, and the recursive walk never touches self.router
src/runtime/bake/production.rs:551 | pass &[u8] with explicit lifetime instead of cloning: delete the Box::from() and keep root_dir_path: &[u8] borrowing root_dir_buf — root_dir_buf is never reused and &*root_dir_path at line 1204 works unchanged
src/runtime/bake/production.rs:95 | pass &[u8] with explicit lifetime instead of cloning: delete the Box::from() and keep cwd: &[u8] borrowing cwd_buf for the function — cwd_buf is never reused (adjust `&cwd` → `cwd` at the build_with_vm call)
src/runtime/cli/repl.rs:2093 | drop both .to_vec() and use disjoint-field borrows: `if let Some(prev) = self.history.prev(self.line_editor.get_line()) { self.line_editor.set(prev); }` — history and line_editor are separate fields
src/runtime/cli/repl.rs:2139 | take by &mut, use mem::take on the field: `let line = mem::take(&mut self.line_editor.buffer); self.line_editor.cursor = 0;` — every path calls line_editor.clear() anyway, so taking upfront yields an owned Vec with zero alloc and unblocks `(cmd.handler)(self, args)`
src/runtime/cli/repl.rs:2275 | delete .to_vec() and keep `let line = self.line_editor.get_line();` — print()/refresh_line() take &self, and on every path NLL ends the line borrow before any &mut self.line_editor call (set/insert/backspace)
src/runtime/server/server_body.rs:993 | take by &mut, use mem::take on the field: `let ServePluginsState::Unqueued(plugin_list) = mem::replace(&mut self.state, ServePluginsState::Err) else { unreachable!() };` to own the Box<[Box<[u8]>]>, iterate it directly, then overwrite self.state with Pending as before
src/runtime/shell/IOReader.rs:318 | take by &mut, use mem::take on the field: `let readers = std::mem::take(&mut s.readers);` — moves the Vec out (no alloc) and preserves snapshot semantics against re-entrant add_reader/remove_reader; this is a terminal error path so leaving s.readers empty is correct
src/runtime/shell/IOWriter.rs:833 | use SmallVec<[ChildPtr; 4]> for `targets` — the dedup'd snapshot must exist because run_yield/cancel_chunks re-derive state() and mutate s.writers, but typical unique-writer count per IOWriter is tiny so it stays on-stack
src/runtime/shell/builtin/seq.rs:196 | restructure: compute result first, mutate second — call `Builtin::of_mut(...).stdout.enqueue(child, &out, safeguard)` with the local `out` directly (enqueue copies into IOWriter's buffer), then `Self::state_mut(...).buf = out;` afterward; drop the intermediate store+clone
src/runtime/test_runner/diff/diff_match_patch.rs:985 | restructure: track only the length — change to `last_equality_len: Option<usize>`, store `diffs[p].text.len()` instead of `dupe(...)`, and at the insert site use `dupe(&diffs[eq_idx].text)` (eq_idx already indexes the same equality) before mutating diffs
src/runtime/test_runner/snapshot.rs:371 | use `for (&file_id, ils_info) in self.inline_snapshots_to_write.iter_mut()` — the loop body touches no other `self` field and never inserts/removes map keys, so iter_mut yields (&FileId, &mut Vec) directly with no key Vec needed
src/shell_parser/braces.rs:626 | split_at_mut per iteration: out[out_key][..length] is only appended to by the j==0 recursion, so for j>0 split out at new_key (new_key > out_key invariant) and extend_from_slice(&lo[out_key][..length]) without cloning
src/shell_parser/braces.rs:676 | split_at_mut per iteration: same as line 626 — re-borrow out[out_key][..length] and &mut out[new_key] disjointly each j>0 instead of snapshotting to Vec
src/shell_parser/braces.rs:745 | split_at_mut per iteration: for i>0 split out at new_key and extend_from_slice(&lo[out_key][..starting_len]); expand_flat only appends so the prefix bytes are stable
src/watcher/KEventWatcher.rs:130 | drop the watchevents &mut borrow, then borrow disjoint fields in place: let deduped=&mut this.watch_events[..out_len]; call watcher_trace::write_events(&this.watchlist,deduped,changed) free fn instead of the &self method (mirror INotifyWatcher.rs:532-535)
src/watcher/WindowsWatcher.rs:525 | drop the all_events &mut borrow, re-slice let deduped=&mut this.watch_events[..=last_event_index] and call watcher_trace::write_events(&this.watchlist,..) free fn so all borrows are disjoint struct fields (mirror INotifyWatcher.rs:532-535)
