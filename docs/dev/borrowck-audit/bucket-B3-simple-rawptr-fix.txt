src/bundler/bundle_v2.rs:2135 | replace get_or_put slot-hold with a two-step contains-check then insert(key, idx) after enqueue_parse_task returns, avoiding the *mut u32 across &mut self calls
src/install/PackageInstaller.rs:2119 | this branch never reads `destination_dir` (only no-op `close()`); delete the variable (and its close) or use a borrowless placeholder variant so `self.node_modules` isn't aliased
src/install/PackageManager/runTasks.rs:347 | copy name.slice() into a local SmallVec/Vec<u8> before the match-arm body (names are short) instead of detach_lifetime, so later &mut task / task_ptr uses are safe
src/js_parser/lexer.rs:2404 | use the already-Copy'd local `contents` (line 2378): `let chunk = &contents[self.current..];` — it does not borrow `self`, so `scan_pragma(&mut self, ..)` compiles and the StoreStr arena alloc goes away
src/jsc/AsyncModule.rs:342 | the name is loop-invariant — compute it once before `retain_mut` as an owned `Vec<u8>` (`lockfile.str(&dependency.name).to_vec()`) and pass `&name[..]`; drops the RawSlice and the per-iteration `&vm` vs `&mut vm` conflict
src/jsc/VirtualMachine.rs:4313 | redundant re-derive — just reuse the `jsc_vm` binding from line 4225; NLL ends that borrow after `_resolve` returns (line 4324), well before `RestoreLog`'s Drop runs, so no overlap with the guard
src/jsc/VirtualMachine.rs:5527 | Holder's fields are pub and disjoint: call `exception_holder.zig_exception();` once for the lazy-init side-effect, then pass `unsafe { exception_holder.zig_exception.assume_init_mut() }` and `&mut exception_holder.need_to_clear_parser_arena_on_deinit` as inline disjoint field borrows at each call site
src/paths/resolve_path.rs:1435 | Capture buf.as_ptr() as usize (address, not *mut) for the offset math; after the inner borrow ends, index buf[start_offset+len]=0 and build ZStr from buf[start_offset..].as_ptr() — no raw-ptr launder needed
src/paths/resolve_path.rs:1511 | Same as line 1435: compute start_offset via usize address diff, then re-index buf after the join_string_buf_t borrow ends; derive the ZStr pointer from the live buf slice
src/runtime/api/BunObject.rs:1899 | Reorder: call (*vm.transpiler.env).get_s3_credentials() before let rare = vm.rare_data(); the env deref is a separate process-lifetime-ptr unsafe unrelated to the borrowck reshape
src/runtime/api/bun/js_bun_spawn_bindings.rs:1094 | defer! non-move closure place-capture: use scopeguard::guard(jsc_vm_ptr, |p| ...) so the pointer is the guard payload, eliminating the jsc_vm_ptr_cleanup copy
src/runtime/api/bun/js_bun_spawn_bindings.rs:1707 | defer! non-move closure place-capture: use scopeguard::guard(subprocess_ptr, |p| ...) so the pointer is the guard payload, eliminating the subprocess_ptr_exit copy
src/runtime/cli/publish_command.rs:718 | hoist use_system_shell and log_level into locals first, then reborrow &mut *context.command_ctx directly; remaining context.* reads are disjoint fields
src/runtime/jsc_hooks.rs:2372 | move should_close/input_file_fd INTO the scopeguard payload and access via DerefMut (guard.0/guard.1); drop closure receives them by value — no raw ptrs needed
src/runtime/server/mod.rs:884 | match on `&mut req` instead of `&req` so the Stack arm binds `r: &mut &mut uws::Request` and pass `&mut **r` directly to prepare_js_request_context — no from_ref().cast_mut() launder needed
src/runtime/shell/interpreter.rs:1921 | already safe Rust (no `unsafe` at this site; EnvStr erases lifetime by design) — just delete the 'reshaped for borrowck' comment, the two-statement hoist is the idiomatic form
src/runtime/webcore/Blob.rs:607 | borrow cred/path/payer from `self.store().unwrap().data.as_s3()` instead of `t.blob` — `self` is live through the download() call and shares the same Rc-backed store as the duped `t.blob`, eliminating the *const [u8] round-trip
src/sql_jsc/mysql/MySQLConnection.rs:1705 | take rb = self.read_buffer() once, save head, bump rb.head += count, then build the slice from rb.byte_list[head..head+count] via disjoint field borrows (RawSlice remains only because Data::Temporary's API requires it)
src/sql_jsc/postgres/PostgresSQLConnection.rs:1743 | save head index, call self.skip(count) first (dropping the JsCell borrow), then re-borrow read_buffer().byte_list[head..head+count] for the returned slice (RawSlice stays only for the Data::Temporary API)
src/sql_jsc/postgres/PostgresSQLConnection.rs:2407 | call statement.structure(..) for its side effect and drop the return, then bind Some(&statement.cached_structure) and &statement.fields as ordinary disjoint shared borrows; pass the Option<&_> straight to to_js (no ParentRef needed)
