src/bundler/LinkerContext.rs:1060 | self-field-alias | change rename_symbols_in_chunk to take &Chunk (or just the fields it reads) since caller already assigns chunk.renamer from the return value; drop the separate files *const [u32]
src/bundler/LinkerContext.rs:2230 | self-field-alias | make print_code_for_file_in_chunk_js an associated fn receiving &ts_enums/&line_offset_table/&mangled_props and the RequireOrImportMeta callback ctx as disjoint params from the caller instead of &mut self
src/bundler/ParseTask.rs:2224 | self-field-alias | restructure run_with_source_code/get_ast to thread &mut Transpiler and &mut Resolver as explicit disjoint params per target, eliminating the worker_raw/transpiler/resolver raw-ptr chain
src/bundler/bundle_v2.rs:1999 | self-field-alias | move drain_deferred_tasks onto BundleV2 (or make it a free fn taking &mut BundleV2) so it accesses self.graph.deferred_* and the rest of self via normal disjoint field borrows
src/bundler/bundle_v2.rs:2115 | self-field-alias | split run_resolver into an associated fn taking (&mut Transpiler, &mut Graph, file_map, arena, ...) as disjoint params so transpiler.resolver/options and graph are borrowed independently
src/bundler/bundle_v2.rs:2249 | self-field-alias | hoist the self.graph.* record/source reads and self.transpiler.options flags into locals first, then call log_for_resolution_failures last so &mut Log does not overlap other self borrows
src/exe_format/macho.rs:93 | other | ptr-as-usize subslice offset math (already no `unsafe`); have `LoadCommandIterator` yield each entry's byte offset into `data` so callers don't need `entry.data.as_ptr() as usize - base_addr`
src/glob/GlobWalker.rs:473 | self-field-alias | change `handle_sys_err_with_path` to take only the disjoint fields it mutates (or read the path from `self.path_buf` internally) so the ZStr arg doesn't overlap `&mut self.walker`
src/http/lib.rs:1579 | self-field-alias | build `HTTPClientResult` directly from the caller-owned `NonNull<MutableString>` (lifetime independent of `&mut self`) and split `state.reset()` so it doesn't write `body_out_str` while `result.body` is live
src/http/lib.rs:3663 | self-field-alias | track `(is_buffered: bool, consumed: usize)` and re-derive the read slice from `incoming_data` / `self.state.response_message_buffer` at each use instead of caching a `RawSlice` across `&mut self` mutations
src/http_jsc/websocket_client.rs:1619 | self-field-alias | change `RareData::ws_client_group` to take the concrete loop/event-loop handle instead of `&VirtualMachine`, then call `let h = vm.uws_loop(); vm.rare_data().ws_client_group::<SSL>(h)` with disjoint borrows
src/install/PackageInstaller.rs:1164 | self-field-alias | since `self.lockfile` is `*mut Lockfile`, make `lockfile()`/a `string_buf()` helper return `&'a [u8]` tied to the struct lifetime (not `&self`) so the slice can be held across `&mut self` calls without a RawSlice
src/install/PackageInstaller.rs:1288 | self-field-alias | stop storing `&NodeModulesFolder`/`&Lockfile`/progress inside `PackageInstall`; pass them to its methods at call time (or split `PackageInstaller` so the borrowed fields are disjoint from later `&mut self` access)
src/install/PackageManager/PackageManagerEnqueue.rs:1102 | self-field-alias | change get_or_put_resolved_package_with_find_result to take disjoint &mut Lockfile + needed PM fields instead of &mut PackageManager so a stack-owned manifest borrow can coexist
src/install/PackageManager/PackageManagerEnqueue.rs:1211 | self-field-alias | pass SemverString handles for alias/url into enqueue_git_clone/checkout and resolve inside, avoiding the pre-sliced &[u8] that locks string_bytes
src/install/PackageManager/PackageManagerEnqueue.rs:1378 | self-field-alias | same as line 216: return NonNull<NetworkTask> from generate_network_task_for_tarball so enqueue_network_task(this, nt) doesn't alias
src/install/PackageManager/PackageManagerEnqueue.rs:1510 | self-field-alias | pass the tarball Uri's SemverString into the enqueue callees and resolve there, or .to_vec() the url before the mut reborrow
src/install/PackageManager/PackageManagerEnqueue.rs:2001 | self-field-alias | change Lockfile::is_root_dependency to take only the disjoint PM fields it reads (root_dependency_list, workspace_package_json_cache) instead of &mut PackageManager
src/install/PackageManager/PackageManagerEnqueue.rs:2061 | self-field-alias | change Package::from_npm to accept the disjoint PM fields it reads (options, updating_packages, log) instead of both &mut PackageManager and &mut Lockfile
src/install/PackageManager/PackageManagerEnqueue.rs:216 | self-field-alias | change generate_network_task_for_tarball to return Option<NonNull<NetworkTask>> (matching the pool's *mut API) so it doesn't borrow *this
src/install/PackageManager/PackageManagerEnqueue.rs:2368 | self-field-alias | introduce an associated fn taking (&mut manifests, &options, &lockfile) so disjoint field borrows replace the this_ptr projections
src/install/PackageManager/PackageManagerEnqueue.rs:237 | self-field-alias | pass the SemverString handle (Copy) into enqueue_local_tarball and resolve against string_bytes inside the callee (or .to_vec() for a simple allocating fix)
src/install/PackageManager/PackageManagerEnqueue.rs:2532 | self-field-alias | same as line 1102: split get_or_put_resolved_package_with_find_result signature so &manifest (from this.manifests) coexists with the other mut borrows
src/install/PackageManager/PackageManagerEnqueue.rs:2558 | self-field-alias | pass the folder SemverString into FolderResolution::get_or_put and resolve against string_bytes inside (or .to_vec() the path)
src/install/PackageManager/PackageManagerEnqueue.rs:2670 | self-field-alias | pass workspace_path_raw: SemverString into FolderResolution::get_or_put and resolve inside, avoiding the detached &[u8]
src/install/PackageManager/PackageManagerEnqueue.rs:2715 | self-field-alias | make global_link_dir_path return &'static [u8] (set-once Box never freed) and pass symlink SemverString into get_or_put to resolve internally
src/install/PackageManager/PackageManagerEnqueue.rs:284 | self-field-alias | pass Repository/SemverString handles into enqueue_git_clone/checkout and slice string_bytes inside the callee instead of pre-slicing
src/install/PackageManager/PackageManagerEnqueue.rs:425 | self-field-alias | same as line 216: make generate_network_task_for_tarball return Option<NonNull<NetworkTask>>
src/install/PackageManager/PopulateManifestCache.rs:66 | self-field-alias | restructure get_network_task/for_manifest/enqueue_network_task to take &mut preallocated_network_tasks + the disjoint manager fields they need, so the pool slot borrow does not overlap &mut manager
src/install/PackageManager/install_with_manager.rs:1107 | self-field-alias | change Tree::print to take only the mutable fields it touches (track_installed_bin, updating_packages, workspace_name_hash) instead of &mut PackageManager, letting Printer hold safe &lockfile/&options/&update_requests
src/install/PackageManager/install_with_manager.rs:67 | self-field-alias | change Lockfile::load_from_cwd to accept the specific manager sub-fields it reads (options, allocator) instead of Option<&mut PackageManager>, so &mut manager.lockfile / &mut manager.log are disjoint
src/install/PackageManager/runTasks.rs:618 | self-field-alias | convert NetworkTask::reset_streaming_for_retry / discard_unused_streaming_state into associated fns taking the disjoint task fields (not &mut self), so the &mut task.callback (extract) borrow from the match arm can coexist
src/install/PackageManager/updatePackageJSONAndInstall.rs:125 | self-field-alias | refactor PackageJSONEditor::edit / do_patch_commit to take disjoint manager fields (log, options, etc.) so the &mut MapEntry borrow from workspace_package_json_cache can be held safely across them
src/install/PackageManager/updatePackageJSONAndInstall.rs:437 | self-field-alias | same refactor as line 125: callers after get_with_path should take disjoint manager fields instead of &mut manager, letting the cache entry stay a safe &mut MapEntry
src/js_printer/lib.rs:3942 | self-field-alias | thread the arena lifetime `'a` through `symbols()`/Renamer so the returned `&'a Symbol` does not borrow `&self` (the table is arena-backed — see existing note at line 2378), or copy the few fields used (import_item_status, namespace_alias, original_name) before the `&mut self` print calls
src/js_printer/lib.rs:5288 | self-field-alias | same as 3942: make `symbols()` return an arena-lifetime `&'a Symbol` not tied to `&self`, or copy `original_name`/`namespace_alias` out before calling `self.print_*`
src/js_printer/lib.rs:5359 | self-field-alias | same as 3942: make `symbols()` return an arena-lifetime `&'a Symbol` not tied to `&self`, or copy `namespace_alias` out before the `self.print_*` calls
src/jsc/VirtualMachine.rs:1563 | reentrant-callback | close_all_socket_groups already does `let _ = self` and only needs `vm.uws_loop()`; change its signature to take `loop_: *mut uws::Loop` (Copy) and drop both the `&mut self` and `&VirtualMachine` params so no VM borrow is held across the JS on_close callbacks
src/jsc/VirtualMachine.rs:6377 | self-field-alias | `spawn_ipc_group`/`lazy_group` only read `vm.uws_loop()` (returns Copy `*mut uws::Loop`); change those to take `loop_: *mut uws::Loop` and have the caller snapshot `let loop_ = self.uws_loop();` before `self.rare_data()` — removes the `&mut self.rare_data` + `&self` overlap
src/jsc/hot_reloader.rs:863 | self-field-alias | Copy the BackRef (self.ctx) before Task::init_empty(self) and deref it directly in the scopeguards instead of going through &mut self.get_context(); preserves the documented drop order without the *mut Watcher launder
src/jsc/web_worker.rs:1279 | self-field-alias | Change RareData::close_all_socket_groups to take the disjoint VM fields it actually needs (e.g. global/event_loop) instead of &VirtualMachine so it no longer conflicts with the &mut vm.rare_data borrow
src/runtime/bake/dev_server/incremental_graph.rs:526 | self-field-alias | pass &mut DirectoryWatchStore as a parameter to disconnect_and_delete_file so it doesn't reach through owner(); the key borrow then coexists with the sibling &mut
src/runtime/bake/production.rs:221 | self-field-alias | drop the separate vm_ptr param from build_with_vm and derive vm from pt.vm (BackRef) inside, so there is one access path to the VM
src/runtime/cli/pack_command.rs:1907 | self-field-alias | change run_lifecycle_script/iterate_bundled_deps to take disjoint ctx fields (manager, command_ctx, bundled_deps, stats) instead of &mut ctx whole
src/runtime/cli/test/Scanner.rs:134 | self-field-alias | make read_dir_with_name/is_test_file associated fns taking only the disjoint fields they touch (open_dir_buf, test_files, fs) so scan_dir_buf's borrow survives
src/runtime/server/mod.rs:2099 | self-field-alias | destructure at top of set_routes: `let Self { dev_server, config, user_routes, app, h3_app, global_this, .. } = self;` so dev_server/config/user_routes are disjoint borrows instead of snapshotting *mut DevServer
src/runtime/server/mod.rs:2163 | self-field-alias | same destructure as line 2099 — with `config` and `user_routes` bound as disjoint fields, `config.websocket.as_ref()` coexists with `user_routes.iter_mut()` without the BackRef snapshot
src/runtime/webcore/blob/read_file.rs:808 | self-field-alias | change do_read to an associated fn taking the disjoint fields it actually touches (opened_fd, file_store, could_block, &mut read_eof/errno/system_error) plus a safe `&mut [u8]`, so the spare-capacity slice never coexists with `&mut self`
src/sql_jsc/mysql/MySQLConnection.rs:232 | reentrant-callback | change MySQLRequestQueue::advance to take &JSMySQLConnection (all queue/conn state is Cell/JsCell so &self suffices) and have the wrapper call self.js_connection_ref() instead of the raw get_js_connection() ptr
src/sql_jsc/mysql/MySQLConnection.rs:531 | self-field-alias | mirror the Postgres Reader: wrap read_buffer/last_message_start in JsCell/Cell and make Reader hold BackRef<MySQLConnection> instead of *mut, so buffered_reader() and process_packets(&mut self) no longer alias
