src/install/PackageInstaller.rs:719 | change spawn_package_lifecycle_scripts to return the ScriptsList (or package_name) back on Err so the error path borrows instead of pre-cloning; requires signature change touching other callers
src/install/PackageManager/patchPackage.rs:1205 | pass &[u8] with explicit lifetime: add a `relative_path(&self) -> &[u8]` accessor on `tree::Iterator`, restructure the loop to drop `IteratorNext` (continue on no-match) then `return Some(iterator.relative_path())` after the scope, change return type to `Option<&[u8]>` and propagate through the 4 callers and `pkg_info_for_name_and_version`
src/runtime/cli/pm_view_command.rs:32 | change PackageManager::http_proxy/tls_reject_unauthorized to take &self (they only call env_mut() which already takes &self), then `break 'brk &manager.root_package_json_name_at_time_of_init` with no bump copy
src/runtime/cli/pm_view_command.rs:75 | change PackageManager::http_proxy/tls_reject_unauthorized to take &self (env_mut already takes &self), then drop .clone() and hold `scope: &Scope` across lines 122/134 (scope is used through line 164)
src/watcher/INotifyWatcher.rs:388 | hoist to scratch buffer field on self (Vec<EventListIndex> on Watcher/Platform), clear()+extend_from_slice under the mutex each cycle to amortize the alloc
