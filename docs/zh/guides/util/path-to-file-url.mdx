---
title: 将绝对路径转换为文件 URL
sidebarTitle: 路径到文件 URL
mode: center
---

使用 `Bun.pathToFileURL()` 将绝对路径转换为 `file://` URL。

```ts
Bun.pathToFileURL("/path/to/file.txt");
// => "file:///path/to/file.txt"
```

---

请参阅 [文档 > API > 工具](/runtime/utils) 了解更多的实用工具。
