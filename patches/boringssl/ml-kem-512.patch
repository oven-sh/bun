diff -ruN a/crypto/evp/evp_ctx.cc b/crypto/evp/evp_ctx.cc
--- a/crypto/evp/evp_ctx.cc
+++ b/crypto/evp/evp_ctx.cc
@@ -100,6 +100,9 @@
     case EVP_PKEY_ML_DSA_87:
       alg = EVP_pkey_ml_dsa_87();
       break;
+    case EVP_PKEY_ML_KEM_512:
+      alg = EVP_pkey_ml_kem_512();
+      break;
     case EVP_PKEY_ML_KEM_768:
       alg = EVP_pkey_ml_kem_768();
       break;
diff -ruN a/crypto/evp/internal.h b/crypto/evp/internal.h
--- a/crypto/evp/internal.h
+++ b/crypto/evp/internal.h
@@ -410,6 +410,7 @@
       EVP_pkey_ml_dsa_44(),
       EVP_pkey_ml_dsa_65(),
       EVP_pkey_ml_dsa_87(),
+      EVP_pkey_ml_kem_512(),
       EVP_pkey_ml_kem_768(),
       EVP_pkey_ml_kem_1024(),
       // TODO(crbug.com/438761503): Remove DSA from this set, after callers that
diff -ruN a/crypto/evp/p_mlkem.cc b/crypto/evp/p_mlkem.cc
--- a/crypto/evp/p_mlkem.cc
+++ b/crypto/evp/p_mlkem.cc
@@ -38,6 +38,7 @@
 
 constexpr CBS_ASN1_TAG kSeedTag = CBS_ASN1_CONTEXT_SPECIFIC | 0;
 
+constexpr uint8_t kMLKEM512OID[] = {OBJ_ENC_ML_KEM_512};
 constexpr uint8_t kMLKEM768OID[] = {OBJ_ENC_ML_KEM_768};
 constexpr uint8_t kMLKEM1024OID[] = {OBJ_ENC_ML_KEM_1024};
 
@@ -63,6 +64,7 @@
                   "PublicKey type must be trivially copyable.");               \
   };
 
+MAKE_MLKEM_TRAITS(512)
 MAKE_MLKEM_TRAITS(768)
 MAKE_MLKEM_TRAITS(1024)
 
@@ -468,6 +470,10 @@
 
 }  // namespace
 
+const EVP_PKEY_ALG *EVP_pkey_ml_kem_512() {
+  return &MLKEMImplementation<MLKEM512Traits>::pkey_alg;
+}
+
 const EVP_PKEY_ALG *EVP_pkey_ml_kem_768() {
   return &MLKEMImplementation<MLKEM768Traits>::pkey_alg;
 }
@@ -476,6 +482,10 @@
   return &MLKEMImplementation<MLKEM1024Traits>::pkey_alg;
 }
 
+const EVP_KEM *EVP_kem_ml_kem_512() {
+  return &MLKEMImplementation<MLKEM512Traits>::evp_kem;
+}
+
 const EVP_KEM *EVP_kem_ml_kem_768() {
   return &MLKEMImplementation<MLKEM768Traits>::evp_kem;
 }
diff -ruN a/crypto/fipsmodule/bcm_interface.h b/crypto/fipsmodule/bcm_interface.h
--- a/crypto/fipsmodule/bcm_interface.h
+++ b/crypto/fipsmodule/bcm_interface.h
@@ -606,6 +606,10 @@
 // decapsulating party.
 #define BCM_MLKEM_ENCAP_ENTROPY 32
 
+// BCM_MLKEM512_PRIVATE_KEY_BYTES is the length of the data produced by
+// `BCM_mlkem512_marshal_private_key`.
+#define BCM_MLKEM512_PRIVATE_KEY_BYTES 1632
+
 // BCM_MLKEM768_PRIVATE_KEY_BYTES is the length of the data produced by
 // `BCM_mlkem768_marshal_private_key`.
 #define BCM_MLKEM768_PRIVATE_KEY_BYTES 2400
@@ -614,6 +618,58 @@
 // `BCM_mlkem1024_marshal_private_key`.
 #define BCM_MLKEM1024_PRIVATE_KEY_BYTES 3168
 
+OPENSSL_EXPORT bcm_infallible BCM_mlkem512_generate_key(
+    uint8_t out_encoded_public_key[MLKEM512_PUBLIC_KEY_BYTES],
+    uint8_t optional_out_seed[MLKEM_SEED_BYTES],
+    MLKEM512_private_key *out_private_key);
+
+OPENSSL_EXPORT bcm_status
+BCM_mlkem512_private_key_from_seed(MLKEM512_private_key *out_private_key,
+                                   const uint8_t *seed, size_t seed_len);
+
+OPENSSL_EXPORT bcm_infallible
+BCM_mlkem512_public_from_private(MLKEM512_public_key *out_public_key,
+                                 const MLKEM512_private_key *private_key);
+
+OPENSSL_EXPORT const MLKEM512_public_key *BCM_mlkem512_public_of_private(
+    const MLKEM512_private_key *private_key);
+
+OPENSSL_EXPORT bcm_infallible
+BCM_mlkem512_encap(uint8_t out_ciphertext[MLKEM512_CIPHERTEXT_BYTES],
+                   uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+                   const MLKEM512_public_key *public_key);
+
+OPENSSL_EXPORT bcm_status
+BCM_mlkem512_decap(uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+                   const uint8_t *ciphertext, size_t ciphertext_len,
+                   const MLKEM512_private_key *private_key);
+
+OPENSSL_EXPORT bcm_status BCM_mlkem512_marshal_public_key(
+    CBB *out, const MLKEM512_public_key *public_key);
+
+int BCM_mlkem512_public_keys_equal(const MLKEM512_public_key *a,
+                                   const MLKEM512_public_key *b);
+
+OPENSSL_EXPORT bcm_status
+BCM_mlkem512_parse_public_key(MLKEM512_public_key *out_public_key, CBS *in);
+
+OPENSSL_EXPORT bcm_status
+BCM_mlkem512_parse_private_key(MLKEM512_private_key *out_private_key, CBS *in);
+
+OPENSSL_EXPORT bcm_infallible BCM_mlkem512_generate_key_external_seed(
+    uint8_t out_encoded_public_key[MLKEM512_PUBLIC_KEY_BYTES],
+    MLKEM512_private_key *out_private_key,
+    const uint8_t seed[MLKEM_SEED_BYTES]);
+
+OPENSSL_EXPORT bcm_infallible BCM_mlkem512_encap_external_entropy(
+    uint8_t out_ciphertext[MLKEM512_CIPHERTEXT_BYTES],
+    uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+    const MLKEM512_public_key *public_key,
+    const uint8_t entropy[BCM_MLKEM_ENCAP_ENTROPY]);
+
+OPENSSL_EXPORT bcm_status BCM_mlkem512_marshal_private_key(
+    CBB *out, const MLKEM512_private_key *private_key);
+
 OPENSSL_EXPORT bcm_infallible BCM_mlkem768_generate_key(
     uint8_t out_encoded_public_key[MLKEM768_PUBLIC_KEY_BYTES],
     uint8_t optional_out_seed[MLKEM_SEED_BYTES],
diff -ruN a/crypto/fipsmodule/mlkem/mlkem.cc.inc b/crypto/fipsmodule/mlkem/mlkem.cc.inc
--- a/crypto/fipsmodule/mlkem/mlkem.cc.inc
+++ b/crypto/fipsmodule/mlkem/mlkem.cc.inc
@@ -94,6 +94,9 @@
 const uint16_t kInverseDegree = 3303;
 
 // Rank-specific constants.
+#define RANK512 2
+const int kDU512 = 10;
+const int kDV512 = 4;
 #define RANK768 3
 const int kDU768 = 10;
 const int kDV768 = 4;
@@ -101,6 +104,18 @@
 const int kDU1024 = 11;
 const int kDV1024 = 5;
 
+constexpr int du_for_rank(int rank) {
+  return rank == RANK1024 ? kDU1024 : (rank == RANK768 ? kDU768 : kDU512);
+}
+
+constexpr int dv_for_rank(int rank) {
+  return rank == RANK1024 ? kDV1024 : (rank == RANK768 ? kDV768 : kDV512);
+}
+
+// eta1 (FIPS 203 Table 2) is 3 for ML-KEM-512 and 2 for the larger sets; eta2
+// is 2 for all ranks.
+constexpr int eta1_for_rank(int rank) { return rank == RANK512 ? 3 : 2; }
+
 constexpr size_t encoded_vector_size(int rank) {
   return (kLog2Prime * DEGREE / 8) * static_cast<size_t>(rank);
 }
@@ -109,20 +124,21 @@
   return encoded_vector_size(rank) + /*sizeof(rho)=*/32;
 }
 
+static_assert(encoded_public_key_size(RANK512) == MLKEM512_PUBLIC_KEY_BYTES);
 static_assert(encoded_public_key_size(RANK768) == MLKEM768_PUBLIC_KEY_BYTES);
 static_assert(encoded_public_key_size(RANK1024) == MLKEM1024_PUBLIC_KEY_BYTES);
 
 constexpr size_t compressed_vector_size(int rank) {
-  // `if constexpr` isn't available in C++17.
-  return (rank == RANK768 ? kDU768 : kDU1024) * static_cast<size_t>(rank) *
+  return static_cast<size_t>(du_for_rank(rank)) * static_cast<size_t>(rank) *
          DEGREE / 8;
 }
 
 constexpr size_t ciphertext_size(int rank) {
   return compressed_vector_size(rank) +
-         (rank == RANK768 ? kDV768 : kDV1024) * DEGREE / 8;
+         static_cast<size_t>(dv_for_rank(rank)) * DEGREE / 8;
 }
 
+static_assert(ciphertext_size(RANK512) == MLKEM512_CIPHERTEXT_BYTES);
 static_assert(ciphertext_size(RANK768) == MLKEM768_CIPHERTEXT_BYTES);
 static_assert(ciphertext_size(RANK1024) == MLKEM1024_CIPHERTEXT_BYTES);
 
@@ -504,6 +520,32 @@
 }
 #endif
 
+// Algorithm 8 from the spec, with eta fixed to three. Samples 6 bits per
+// coefficient and sets the coefficient to the count of the first three bits
+// minus the count of the last three, giving a centered binomial distribution in
+// [-3, 3]. Only ML-KEM-512 uses eta = 3.
+void scalar_centered_binomial_distribution_eta_3_with_prf(
+    scalar *out, const uint8_t input[33]) {
+  uint8_t entropy[192];
+  static_assert(sizeof(entropy) == 2 * /*kEta=*/3 * DEGREE / 8);
+  prf(entropy, sizeof(entropy), input);
+
+  for (int i = 0; i < DEGREE; i += 4) {
+    uint32_t bits = (uint32_t)entropy[3 * (i / 4)] |
+                    ((uint32_t)entropy[3 * (i / 4) + 1] << 8) |
+                    ((uint32_t)entropy[3 * (i / 4) + 2] << 16);
+    for (int j = 0; j < 4; j++) {
+      uint16_t value = (bits & 1) + ((bits >> 1) & 1) + ((bits >> 2) & 1);
+      value -= ((bits >> 3) & 1) + ((bits >> 4) & 1) + ((bits >> 5) & 1);
+      // Add `kPrime` if `value` underflowed (same reduction as the eta = 2
+      // path).
+      uint16_t mask = 0u - (value >> 15);
+      out->c[i + j] = ((value + kPrime) & mask) | (value & ~mask);
+      bits >>= 6;
+    }
+  }
+}
+
 // Generates a secret vector by using
 // `scalar_centered_binomial_distribution_eta_2_with_prf`, using the given seed
 // appending and incrementing `counter` for entry of the vector.
@@ -528,6 +570,30 @@
   }
 }
 
+// Like vector_generate_secret_eta_2, but with eta = 3.
+template <int RANK>
+void vector_generate_secret_eta_3(vector<RANK> *out, uint8_t *counter,
+                                  const uint8_t seed[32]) {
+  uint8_t input[33];
+  OPENSSL_memcpy(input, seed, 32);
+  for (int i = 0; i < RANK; i++) {
+    input[32] = (*counter)++;
+    scalar_centered_binomial_distribution_eta_3_with_prf(&out->v[i], input);
+  }
+}
+
+// Generates a secret vector using eta1 for the given RANK: eta = 3 for
+// ML-KEM-512 and eta = 2 for ML-KEM-768 / ML-KEM-1024.
+template <int RANK>
+void vector_generate_secret_eta1(vector<RANK> *out, uint8_t *counter,
+                                 const uint8_t seed[32]) {
+  if constexpr (eta1_for_rank(RANK) == 3) {
+    vector_generate_secret_eta_3(out, counter, seed);
+  } else {
+    vector_generate_secret_eta_2(out, counter, seed);
+  }
+}
+
 template <int RANK>
 void matrix_expand_step(scalar *out, uint8_t input[34], int i) {
   input[32] = i / RANK;
@@ -926,8 +992,8 @@
 template <int RANK>
 inline void decrypt_cpa(uint8_t out[32], const private_key<RANK> *priv,
                         const uint8_t ciphertext[MLKEM768_CIPHERTEXT_BYTES]) {
-  constexpr int du = RANK == RANK768 ? kDU768 : kDU1024;
-  constexpr int dv = RANK == RANK768 ? kDV768 : kDV1024;
+  constexpr int du = du_for_rank(RANK);
+  constexpr int dv = dv_for_rank(RANK);
 
   vector<RANK> u;
   vector_decode<du>(&u, ciphertext);
@@ -981,10 +1047,10 @@
   OPENSSL_memcpy(priv->pub.rho, hashed, sizeof(priv->pub.rho));
   matrix_expand(&priv->pub.m, rho);
   uint8_t counter = 0;
-  vector_generate_secret_eta_2(&priv->s, &counter, sigma);
+  vector_generate_secret_eta1(&priv->s, &counter, sigma);
   vector_ntt(&priv->s);
   vector<RANK> error;
-  vector_generate_secret_eta_2(&error, &counter, sigma);
+  vector_generate_secret_eta1(&error, &counter, sigma);
   vector_ntt(&error);
   matrix_mult_transpose(&priv->pub.t, &priv->pub.m, &priv->s);
   vector_add(&priv->pub.t, &error);
@@ -1018,12 +1084,12 @@
 template <int RANK>
 void encrypt_cpa(uint8_t *out, const mlkem::public_key<RANK> *pub,
                  const uint8_t message[32], const uint8_t randomness[32]) {
-  constexpr int du = RANK == RANK768 ? mlkem::kDU768 : mlkem::kDU1024;
-  constexpr int dv = RANK == RANK768 ? mlkem::kDV768 : mlkem::kDV1024;
+  constexpr int du = mlkem::du_for_rank(RANK);
+  constexpr int dv = mlkem::dv_for_rank(RANK);
 
   uint8_t counter = 0;
   mlkem::vector<RANK> secret;
-  vector_generate_secret_eta_2(&secret, &counter, randomness);
+  vector_generate_secret_eta1(&secret, &counter, randomness);
   vector_ntt(&secret);
   mlkem::vector<RANK> error;
   vector_generate_secret_eta_2(&error, &counter, randomness);
@@ -1147,6 +1213,22 @@
   return 1;
 }
 
+static_assert(sizeof(MLKEM512_public_key) == sizeof(public_key<RANK512>));
+static_assert(alignof(MLKEM512_public_key) == alignof(public_key<RANK512>));
+
+const public_key<RANK512> *public_key_512_from_external(
+    const MLKEM512_public_key *external) {
+  return reinterpret_cast<const public_key<RANK512> *>(external);
+}
+public_key<RANK512> *public_key_512_from_external(
+    MLKEM512_public_key *external) {
+  return reinterpret_cast<public_key<RANK512> *>(external);
+}
+const MLKEM512_public_key *public_key_512_to_external(
+    const public_key<RANK512> *public_key) {
+  return reinterpret_cast<const MLKEM512_public_key *>(public_key);
+}
+
 static_assert(sizeof(MLKEM768_public_key) == sizeof(public_key<RANK768>));
 static_assert(alignof(MLKEM768_public_key) == alignof(public_key<RANK768>));
 
@@ -1180,6 +1262,18 @@
   return reinterpret_cast<const MLKEM1024_public_key *>(public_key);
 }
 
+static_assert(sizeof(MLKEM512_private_key) >= sizeof(private_key<RANK512>));
+static_assert(alignof(MLKEM512_private_key) >= alignof(private_key<RANK512>));
+
+const private_key<RANK512> *private_key_512_from_external(
+    const MLKEM512_private_key *external) {
+  return reinterpret_cast<const private_key<RANK512> *>(external);
+}
+private_key<RANK512> *private_key_512_from_external(
+    MLKEM512_private_key *external) {
+  return reinterpret_cast<private_key<RANK512> *>(external);
+}
+
 static_assert(sizeof(MLKEM768_private_key) >= sizeof(private_key<RANK768>));
 static_assert(alignof(MLKEM768_private_key) >= alignof(private_key<RANK768>));
 
@@ -1692,6 +1786,144 @@
   if (!mlkem_parse_private_key(priv, in)) {
     return bcm_status::failure;
   }
+  return bcm_status::approved;
+}
+
+bcm_infallible bssl::BCM_mlkem512_generate_key(
+    uint8_t out_encoded_public_key[MLKEM512_PUBLIC_KEY_BYTES],
+    uint8_t optional_out_seed[MLKEM_SEED_BYTES],
+    MLKEM512_private_key *out_private_key) {
+  uint8_t seed[MLKEM_SEED_BYTES];
+  BCM_rand_bytes(seed, sizeof(seed));
+  CONSTTIME_SECRET(seed, sizeof(seed));
+  if (optional_out_seed) {
+    OPENSSL_memcpy(optional_out_seed, seed, sizeof(seed));
+  }
+  BCM_mlkem512_generate_key_external_seed(out_encoded_public_key,
+                                          out_private_key, seed);
+  return bcm_infallible::not_approved;
+}
+
+bcm_status bssl::BCM_mlkem512_private_key_from_seed(
+    MLKEM512_private_key *out_private_key, const uint8_t *seed,
+    size_t seed_len) {
+  if (seed_len != MLKEM_SEED_BYTES) {
+    return bcm_status::failure;
+  }
+  uint8_t public_key_bytes[MLKEM512_PUBLIC_KEY_BYTES];
+  BCM_mlkem512_generate_key_external_seed(public_key_bytes, out_private_key,
+                                          seed);
+  return bcm_status::not_approved;
+}
+
+bcm_infallible bssl::BCM_mlkem512_generate_key_external_seed(
+    uint8_t out_encoded_public_key[MLKEM512_PUBLIC_KEY_BYTES],
+    MLKEM512_private_key *out_private_key,
+    const uint8_t seed[MLKEM_SEED_BYTES]) {
+  mlkem::private_key<RANK512> *priv =
+      mlkem::private_key_512_from_external(out_private_key);
+  mlkem_generate_key_external_seed(out_encoded_public_key, priv, seed);
+  return bcm_infallible::approved;
+}
+
+bcm_infallible bssl::BCM_mlkem512_public_from_private(
+    MLKEM512_public_key *out_public_key,
+    const MLKEM512_private_key *private_key) {
+  mlkem::public_key<RANK512> *const pub =
+      mlkem::public_key_512_from_external(out_public_key);
+  const mlkem::private_key<RANK512> *const priv =
+      mlkem::private_key_512_from_external(private_key);
+  *pub = priv->pub;
+  return bcm_infallible::approved;
+}
+
+const MLKEM512_public_key *bssl::BCM_mlkem512_public_of_private(
+    const MLKEM512_private_key *private_key) {
+  const mlkem::private_key<RANK512> *const priv =
+      mlkem::private_key_512_from_external(private_key);
+  return mlkem::public_key_512_to_external(&priv->pub);
+}
+
+bcm_infallible bssl::BCM_mlkem512_encap(
+    uint8_t out_ciphertext[MLKEM512_CIPHERTEXT_BYTES],
+    uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+    const MLKEM512_public_key *public_key) {
+  uint8_t entropy[BCM_MLKEM_ENCAP_ENTROPY];
+  BCM_rand_bytes(entropy, BCM_MLKEM_ENCAP_ENTROPY);
+  CONSTTIME_SECRET(entropy, BCM_MLKEM_ENCAP_ENTROPY);
+  BCM_mlkem512_encap_external_entropy(out_ciphertext, out_shared_secret,
+                                      public_key, entropy);
+  return bcm_infallible::approved;
+}
+
+bcm_infallible bssl::BCM_mlkem512_encap_external_entropy(
+    uint8_t out_ciphertext[MLKEM512_CIPHERTEXT_BYTES],
+    uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+    const MLKEM512_public_key *public_key,
+    const uint8_t entropy[BCM_MLKEM_ENCAP_ENTROPY]) {
+  const mlkem::public_key<RANK512> *pub =
+      mlkem::public_key_512_from_external(public_key);
+  mlkem_encap_external_entropy(out_ciphertext, out_shared_secret, pub, entropy);
+  return bcm_infallible::approved;
+}
+
+bcm_status bssl::BCM_mlkem512_decap(
+    uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+    const uint8_t *ciphertext, size_t ciphertext_len,
+    const MLKEM512_private_key *private_key) {
+  if (ciphertext_len != MLKEM512_CIPHERTEXT_BYTES) {
+    BCM_rand_bytes(out_shared_secret, MLKEM_SHARED_SECRET_BYTES);
+    return bcm_status::failure;
+  }
+  const mlkem::private_key<RANK512> *priv =
+      mlkem::private_key_512_from_external(private_key);
+  mlkem_decap(out_shared_secret, ciphertext, priv);
+  return bcm_status::approved;
+}
+
+bcm_status bssl::BCM_mlkem512_marshal_public_key(
+    CBB *out, const MLKEM512_public_key *public_key) {
+  return mlkem_marshal_public_key(
+      out, mlkem::public_key_512_from_external(public_key));
+}
+
+int bssl::BCM_mlkem512_public_keys_equal(const MLKEM512_public_key *a,
+                                         const MLKEM512_public_key *b) {
+  const auto *pub_a = mlkem::public_key_512_from_external(a);
+  const auto *pub_b = mlkem::public_key_512_from_external(b);
+  if (mlkem_public_keys_equal(pub_a, pub_b)) {
+    return 1;
+  }
+  return 0;
+}
+
+bcm_status bssl::BCM_mlkem512_parse_public_key(MLKEM512_public_key *public_key,
+                                               CBS *in) {
+  mlkem::public_key<RANK512> *pub =
+      mlkem::public_key_512_from_external(public_key);
+  if (!mlkem_parse_public_key(pub, in)) {
+    return bcm_status::failure;
+  }
+  return bcm_status::approved;
+}
+
+bcm_status bssl::BCM_mlkem512_marshal_private_key(
+    CBB *out, const MLKEM512_private_key *private_key) {
+  const mlkem::private_key<RANK512> *const priv =
+      mlkem::private_key_512_from_external(private_key);
+  if (!mlkem_marshal_private_key(out, priv)) {
+    return bcm_status::failure;
+  }
+  return bcm_status::approved;
+}
+
+bcm_status bssl::BCM_mlkem512_parse_private_key(
+    MLKEM512_private_key *out_private_key, CBS *in) {
+  mlkem::private_key<RANK512> *const priv =
+      mlkem::private_key_512_from_external(out_private_key);
+  if (!mlkem_parse_private_key(priv, in)) {
+    return bcm_status::failure;
+  }
   return bcm_status::approved;
 }
 
diff -ruN a/crypto/mlkem/mlkem.cc b/crypto/mlkem/mlkem.cc
--- a/crypto/mlkem/mlkem.cc
+++ b/crypto/mlkem/mlkem.cc
@@ -19,6 +19,50 @@
 
 using namespace bssl;
 
+void MLKEM512_generate_key(
+    uint8_t out_encoded_public_key[MLKEM512_PUBLIC_KEY_BYTES],
+    uint8_t optional_out_seed[MLKEM_SEED_BYTES],
+    struct MLKEM512_private_key *out_private_key) {
+  BCM_mlkem512_generate_key(out_encoded_public_key, optional_out_seed,
+                            out_private_key);
+}
+
+int MLKEM512_private_key_from_seed(struct MLKEM512_private_key *out_private_key,
+                                   const uint8_t *seed, size_t seed_len) {
+  return bcm_success(
+      BCM_mlkem512_private_key_from_seed(out_private_key, seed, seed_len));
+}
+
+void MLKEM512_public_from_private(
+    struct MLKEM512_public_key *out_public_key,
+    const struct MLKEM512_private_key *private_key) {
+  (void)BCM_mlkem512_public_from_private(out_public_key, private_key);
+}
+
+void MLKEM512_encap(uint8_t out_ciphertext[MLKEM512_CIPHERTEXT_BYTES],
+                    uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+                    const struct MLKEM512_public_key *public_key) {
+  (void)BCM_mlkem512_encap(out_ciphertext, out_shared_secret, public_key);
+}
+
+int MLKEM512_decap(uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+                   const uint8_t *ciphertext, size_t ciphertext_len,
+                   const struct MLKEM512_private_key *private_key) {
+  return bcm_success(BCM_mlkem512_decap(out_shared_secret, ciphertext,
+                                        ciphertext_len, private_key));
+}
+
+int MLKEM512_marshal_public_key(CBB *out,
+                                const struct MLKEM512_public_key *public_key) {
+  return bcm_success(BCM_mlkem512_marshal_public_key(out, public_key));
+}
+
+int MLKEM512_parse_public_key(struct MLKEM512_public_key *out_public_key,
+                              CBS *in) {
+  return bcm_success(BCM_mlkem512_parse_public_key(out_public_key, in));
+}
+
+
 void MLKEM768_generate_key(
     uint8_t out_encoded_public_key[MLKEM768_PUBLIC_KEY_BYTES],
     uint8_t optional_out_seed[MLKEM_SEED_BYTES],
diff -ruN a/crypto/obj/obj_dat.h b/crypto/obj/obj_dat.h
--- a/crypto/obj/obj_dat.h
+++ b/crypto/obj/obj_dat.h
@@ -16,7 +16,7 @@
 
 
 BSSL_NAMESPACE_BEGIN
-#define NUM_NID 973
+#define NUM_NID 974
 
 static const uint8_t kObjectData[] = {
     /* NID_rsadsi */
@@ -7146,6 +7146,16 @@
     0x04,
     0x04,
     0x02,
+    /* NID_ML_KEM_512 */
+    0x60,
+    0x86,
+    0x48,
+    0x01,
+    0x65,
+    0x03,
+    0x04,
+    0x04,
+    0x01,
 };
 
 static const ASN1_OBJECT kObjects[NUM_NID] = {
@@ -8802,6 +8812,8 @@
      0},
     {NULL, NULL, NID_undef, 0, NULL, 0},
     {"X-Wing", "X-Wing", NID_X_Wing, 0, NULL, 0},
+    {"id-alg-ml-kem-512", "ML-KEM-512", NID_ML_KEM_512, 9, &kObjectData[6241],
+     0},
 };
 
 static const uint16_t kNIDsInShortNameOrder[] = {
@@ -9241,6 +9253,7 @@
     326 /* id-alg-dh-pop */,
     325 /* id-alg-dh-sig-hmac-sha1 */,
     966 /* id-alg-ml-kem-1024 */,
+    973 /* id-alg-ml-kem-512 */,
     970 /* id-alg-ml-kem-768 */,
     324 /* id-alg-noSignature */,
     907 /* id-camellia128-wrap */,
@@ -9825,6 +9838,7 @@
     968 /* ML-DSA-65 */,
     969 /* ML-DSA-87 */,
     966 /* ML-KEM-1024 */,
+    973 /* ML-KEM-512 */,
     970 /* ML-KEM-768 */,
     388 /* Mail */,
     383 /* Management */,
@@ -11445,6 +11459,7 @@
     967 /* 2.16.840.1.101.3.4.3.17 (OBJ_ML_DSA_44) */,
     968 /* 2.16.840.1.101.3.4.3.18 (OBJ_ML_DSA_65) */,
     969 /* 2.16.840.1.101.3.4.3.19 (OBJ_ML_DSA_87) */,
+    973 /* 2.16.840.1.101.3.4.4.1 (OBJ_ML_KEM_512) */,
     970 /* 2.16.840.1.101.3.4.4.2 (OBJ_ML_KEM_768) */,
     966 /* 2.16.840.1.101.3.4.4.3 (OBJ_ML_KEM_1024) */,
     71 /* 2.16.840.1.113730.1.1 (OBJ_netscape_cert_type) */,
diff -ruN a/include/openssl/evp.h b/include/openssl/evp.h
--- a/include/openssl/evp.h
+++ b/include/openssl/evp.h
@@ -124,6 +124,7 @@
 #define EVP_PKEY_ML_DSA_44 NID_ML_DSA_44
 #define EVP_PKEY_ML_DSA_65 NID_ML_DSA_65
 #define EVP_PKEY_ML_DSA_87 NID_ML_DSA_87
+#define EVP_PKEY_ML_KEM_512 NID_ML_KEM_512
 #define EVP_PKEY_ML_KEM_768 NID_ML_KEM_768
 #define EVP_PKEY_ML_KEM_1024 NID_ML_KEM_1024
 #define EVP_PKEY_XWING NID_X_Wing
@@ -203,6 +204,7 @@
 // can be done programmatically with OpenSSL's
 // `OSSL_PROVIDER_add_conf_parameter` function, or by passing "-provparam" to
 // the command-line tool.
+OPENSSL_EXPORT const EVP_PKEY_ALG *EVP_pkey_ml_kem_512(void);
 OPENSSL_EXPORT const EVP_PKEY_ALG *EVP_pkey_ml_kem_768(void);
 OPENSSL_EXPORT const EVP_PKEY_ALG *EVP_pkey_ml_kem_1024(void);
 
@@ -1174,6 +1176,7 @@
 // ciphertexts and secrets.
 
 // EVP_kem_ml_kem_* implement ML-KEM, defined in FIPS 203.
+OPENSSL_EXPORT const EVP_KEM *EVP_kem_ml_kem_512(void);
 OPENSSL_EXPORT const EVP_KEM *EVP_kem_ml_kem_768(void);
 OPENSSL_EXPORT const EVP_KEM *EVP_kem_ml_kem_1024(void);
 
diff -ruN a/include/openssl/mlkem.h b/include/openssl/mlkem.h
--- a/include/openssl/mlkem.h
+++ b/include/openssl/mlkem.h
@@ -22,12 +22,109 @@
 #endif
 
 
-// ML-KEM-768.
+// ML-KEM-512.
 //
 // This implements the Module-Lattice-Based Key-Encapsulation Mechanism from
 // https://csrc.nist.gov/pubs/fips/203/final
 
 
+// MLKEM512_public_key contains an ML-KEM-512 public key. The contents of this
+// object should never leave the address space since the format is unstable.
+struct MLKEM512_public_key {
+  union {
+    uint8_t bytes[512 * (2 + 4) + 32 + 32];
+    uint16_t alignment;
+  } opaque;
+};
+
+// MLKEM512_private_key contains an ML-KEM-512 private key. The contents of this
+// object should never leave the address space since the format is unstable.
+struct MLKEM512_private_key {
+  union {
+    uint8_t bytes[512 * (2 + 2 + 4) + 32 + 32 + 32];
+    uint16_t alignment;
+  } opaque;
+};
+
+// MLKEM512_PUBLIC_KEY_BYTES is the number of bytes in an encoded ML-KEM-512
+// public key.
+#define MLKEM512_PUBLIC_KEY_BYTES 800
+
+// MLKEM_SEED_BYTES is the number of bytes in an ML-KEM seed.
+#define MLKEM_SEED_BYTES 64
+
+// MLKEM512_generate_key generates a random public/private key pair, writes the
+// encoded public key to `out_encoded_public_key` and sets `out_private_key` to
+// the private key. If `optional_out_seed` is not NULL then the seed used to
+// generate the private key is written to it.
+OPENSSL_EXPORT void MLKEM512_generate_key(
+    uint8_t out_encoded_public_key[MLKEM512_PUBLIC_KEY_BYTES],
+    uint8_t optional_out_seed[MLKEM_SEED_BYTES],
+    struct MLKEM512_private_key *out_private_key);
+
+// MLKEM512_private_key_from_seed derives a private key from a seed that was
+// generated by `MLKEM512_generate_key`. It fails and returns 0 if `seed_len` is
+// incorrect, otherwise it writes `*out_private_key` and returns 1.
+OPENSSL_EXPORT int MLKEM512_private_key_from_seed(
+    struct MLKEM512_private_key *out_private_key, const uint8_t *seed,
+    size_t seed_len);
+
+// MLKEM512_public_from_private sets `*out_public_key` to the public key that
+// corresponds to `private_key`. (This is faster than parsing the output of
+// `MLKEM512_generate_key` if, for some reason, you need to encapsulate to a key
+// that was just generated.)
+OPENSSL_EXPORT void MLKEM512_public_from_private(
+    struct MLKEM512_public_key *out_public_key,
+    const struct MLKEM512_private_key *private_key);
+
+// MLKEM512_CIPHERTEXT_BYTES is number of bytes in the ML-KEM-512 ciphertext.
+#define MLKEM512_CIPHERTEXT_BYTES 768
+
+// MLKEM_SHARED_SECRET_BYTES is the number of bytes in an ML-KEM shared secret.
+#define MLKEM_SHARED_SECRET_BYTES 32
+
+// MLKEM512_encap encrypts a random shared secret for `public_key`, writes the
+// ciphertext to `out_ciphertext`, and writes the random shared secret to
+// `out_shared_secret`.
+OPENSSL_EXPORT void MLKEM512_encap(
+    uint8_t out_ciphertext[MLKEM512_CIPHERTEXT_BYTES],
+    uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+    const struct MLKEM512_public_key *public_key);
+
+// MLKEM512_decap decrypts a shared secret from `ciphertext` using `private_key`
+// and writes it to `out_shared_secret`. If `ciphertext_len` is incorrect it
+// returns 0, otherwise it returns 1. If `ciphertext` is invalid (but of the
+// correct length), `out_shared_secret` is filled with a key that will always be
+// the same for the same `ciphertext` and `private_key`, but which appears to be
+// random unless one has access to `private_key`. These alternatives occur in
+// constant time. Any subsequent symmetric encryption using `out_shared_secret`
+// must use an authenticated encryption scheme in order to discover the
+// decapsulation failure.
+OPENSSL_EXPORT int MLKEM512_decap(
+    uint8_t out_shared_secret[MLKEM_SHARED_SECRET_BYTES],
+    const uint8_t *ciphertext, size_t ciphertext_len,
+    const struct MLKEM512_private_key *private_key);
+
+
+// Serialisation of ML-KEM-512 keys.
+
+// MLKEM512_marshal_public_key serializes `public_key` to `out` in the standard
+// format for ML-KEM-512 public keys. It returns one on success or zero on
+// allocation error.
+OPENSSL_EXPORT int MLKEM512_marshal_public_key(
+    CBB *out, const struct MLKEM512_public_key *public_key);
+
+// MLKEM512_parse_public_key parses a public key, in the format generated by
+// `MLKEM512_marshal_public_key`, from `in` and writes the result to
+// `out_public_key`. It returns one on success or zero on parse error or if
+// there are trailing bytes in `in`.
+OPENSSL_EXPORT int MLKEM512_parse_public_key(
+    struct MLKEM512_public_key *out_public_key, CBS *in);
+
+
+// ML-KEM-768.
+
+
 // MLKEM768_public_key contains an ML-KEM-768 public key. The contents of this
 // object should never leave the address space since the format is unstable.
 struct MLKEM768_public_key {
@@ -50,9 +147,6 @@
 // public key.
 #define MLKEM768_PUBLIC_KEY_BYTES 1184
 
-// MLKEM_SEED_BYTES is the number of bytes in an ML-KEM seed.
-#define MLKEM_SEED_BYTES 64
-
 // MLKEM768_generate_key generates a random public/private key pair, writes the
 // encoded public key to `out_encoded_public_key` and sets `out_private_key` to
 // the private key. If `optional_out_seed` is not NULL then the seed used to
@@ -80,9 +174,6 @@
 // MLKEM768_CIPHERTEXT_BYTES is number of bytes in the ML-KEM-768 ciphertext.
 #define MLKEM768_CIPHERTEXT_BYTES 1088
 
-// MLKEM_SHARED_SECRET_BYTES is the number of bytes in an ML-KEM shared secret.
-#define MLKEM_SHARED_SECRET_BYTES 32
-
 // MLKEM768_encap encrypts a random shared secret for `public_key`, writes the
 // ciphertext to `out_ciphertext`, and writes the random shared secret to
 // `out_shared_secret`.
diff -ruN a/include/openssl/nid.h b/include/openssl/nid.h
--- a/include/openssl/nid.h
+++ b/include/openssl/nid.h
@@ -5511,6 +5511,12 @@
 #define SN_X_Wing "X-Wing"
 #define NID_X_Wing 972
 
+#define SN_ML_KEM_512 "id-alg-ml-kem-512"
+#define LN_ML_KEM_512 "ML-KEM-512"
+#define NID_ML_KEM_512 973
+#define OBJ_ML_KEM_512 2L, 16L, 840L, 1L, 101L, 3L, 4L, 4L, 1L
+#define OBJ_ENC_ML_KEM_512 0x60, 0x86, 0x48, 0x01, 0x65, 0x03, 0x04, 0x04, 0x01
+
 #define SN_sha512_224 "SHA512-224"
 #define LN_sha512_224 "sha512-224"
 #define NID_sha512_224 1094
diff -ruN a/include/openssl/prefix_symbols.h b/include/openssl/prefix_symbols.h
--- a/include/openssl/prefix_symbols.h
+++ b/include/openssl/prefix_symbols.h
@@ -1313,6 +1313,7 @@
 #pragma redefine_extname EVP_hpke_x25519_hkdf_sha256 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_hpke_x25519_hkdf_sha256)
 #pragma redefine_extname EVP_hpke_xwing BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_hpke_xwing)
 #pragma redefine_extname EVP_kem_ml_kem_1024 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_kem_ml_kem_1024)
+#pragma redefine_extname EVP_kem_ml_kem_512 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_kem_ml_kem_512)
 #pragma redefine_extname EVP_kem_ml_kem_768 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_kem_ml_kem_768)
 #pragma redefine_extname EVP_kem_xwing BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_kem_xwing)
 #pragma redefine_extname EVP_marshal_digest_algorithm BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_marshal_digest_algorithm)
@@ -1336,6 +1337,7 @@
 #pragma redefine_extname EVP_pkey_ml_dsa_65 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_pkey_ml_dsa_65)
 #pragma redefine_extname EVP_pkey_ml_dsa_87 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_pkey_ml_dsa_87)
 #pragma redefine_extname EVP_pkey_ml_kem_1024 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_pkey_ml_kem_1024)
+#pragma redefine_extname EVP_pkey_ml_kem_512 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_pkey_ml_kem_512)
 #pragma redefine_extname EVP_pkey_ml_kem_768 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_pkey_ml_kem_768)
 #pragma redefine_extname EVP_pkey_rsa BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_pkey_rsa)
 #pragma redefine_extname EVP_pkey_rsa_pss_sha256 BORINGSSL_ADD_USER_LABEL_AND_PREFIX(EVP_pkey_rsa_pss_sha256)
@@ -1455,6 +1457,13 @@
 #pragma redefine_extname MLKEM1024_parse_public_key BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM1024_parse_public_key)
 #pragma redefine_extname MLKEM1024_private_key_from_seed BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM1024_private_key_from_seed)
 #pragma redefine_extname MLKEM1024_public_from_private BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM1024_public_from_private)
+#pragma redefine_extname MLKEM512_decap BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM512_decap)
+#pragma redefine_extname MLKEM512_encap BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM512_encap)
+#pragma redefine_extname MLKEM512_generate_key BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM512_generate_key)
+#pragma redefine_extname MLKEM512_marshal_public_key BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM512_marshal_public_key)
+#pragma redefine_extname MLKEM512_parse_public_key BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM512_parse_public_key)
+#pragma redefine_extname MLKEM512_private_key_from_seed BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM512_private_key_from_seed)
+#pragma redefine_extname MLKEM512_public_from_private BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM512_public_from_private)
 #pragma redefine_extname MLKEM768_decap BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM768_decap)
 #pragma redefine_extname MLKEM768_encap BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM768_encap)
 #pragma redefine_extname MLKEM768_generate_key BORINGSSL_ADD_USER_LABEL_AND_PREFIX(MLKEM768_generate_key)
@@ -4430,6 +4439,7 @@
 #define EVP_hpke_x25519_hkdf_sha256 BORINGSSL_ADD_PREFIX(EVP_hpke_x25519_hkdf_sha256)
 #define EVP_hpke_xwing BORINGSSL_ADD_PREFIX(EVP_hpke_xwing)
 #define EVP_kem_ml_kem_1024 BORINGSSL_ADD_PREFIX(EVP_kem_ml_kem_1024)
+#define EVP_kem_ml_kem_512 BORINGSSL_ADD_PREFIX(EVP_kem_ml_kem_512)
 #define EVP_kem_ml_kem_768 BORINGSSL_ADD_PREFIX(EVP_kem_ml_kem_768)
 #define EVP_kem_xwing BORINGSSL_ADD_PREFIX(EVP_kem_xwing)
 #define EVP_marshal_digest_algorithm BORINGSSL_ADD_PREFIX(EVP_marshal_digest_algorithm)
@@ -4453,6 +4463,7 @@
 #define EVP_pkey_ml_dsa_65 BORINGSSL_ADD_PREFIX(EVP_pkey_ml_dsa_65)
 #define EVP_pkey_ml_dsa_87 BORINGSSL_ADD_PREFIX(EVP_pkey_ml_dsa_87)
 #define EVP_pkey_ml_kem_1024 BORINGSSL_ADD_PREFIX(EVP_pkey_ml_kem_1024)
+#define EVP_pkey_ml_kem_512 BORINGSSL_ADD_PREFIX(EVP_pkey_ml_kem_512)
 #define EVP_pkey_ml_kem_768 BORINGSSL_ADD_PREFIX(EVP_pkey_ml_kem_768)
 #define EVP_pkey_rsa BORINGSSL_ADD_PREFIX(EVP_pkey_rsa)
 #define EVP_pkey_rsa_pss_sha256 BORINGSSL_ADD_PREFIX(EVP_pkey_rsa_pss_sha256)
@@ -4572,6 +4583,13 @@
 #define MLKEM1024_parse_public_key BORINGSSL_ADD_PREFIX(MLKEM1024_parse_public_key)
 #define MLKEM1024_private_key_from_seed BORINGSSL_ADD_PREFIX(MLKEM1024_private_key_from_seed)
 #define MLKEM1024_public_from_private BORINGSSL_ADD_PREFIX(MLKEM1024_public_from_private)
+#define MLKEM512_decap BORINGSSL_ADD_PREFIX(MLKEM512_decap)
+#define MLKEM512_encap BORINGSSL_ADD_PREFIX(MLKEM512_encap)
+#define MLKEM512_generate_key BORINGSSL_ADD_PREFIX(MLKEM512_generate_key)
+#define MLKEM512_marshal_public_key BORINGSSL_ADD_PREFIX(MLKEM512_marshal_public_key)
+#define MLKEM512_parse_public_key BORINGSSL_ADD_PREFIX(MLKEM512_parse_public_key)
+#define MLKEM512_private_key_from_seed BORINGSSL_ADD_PREFIX(MLKEM512_private_key_from_seed)
+#define MLKEM512_public_from_private BORINGSSL_ADD_PREFIX(MLKEM512_public_from_private)
 #define MLKEM768_decap BORINGSSL_ADD_PREFIX(MLKEM768_decap)
 #define MLKEM768_encap BORINGSSL_ADD_PREFIX(MLKEM768_encap)
 #define MLKEM768_generate_key BORINGSSL_ADD_PREFIX(MLKEM768_generate_key)
