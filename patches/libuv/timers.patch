--- a/src/timer.c
+++ b/src/timer.c
@@ -170,7 +170,7 @@ void uv__run_timers(uv_loop_t* loop) {
       break;
 
     handle = container_of(heap_node, uv_timer_t, heap_node);
-    if (handle->timeout > loop->time)
+    if (handle->timeout > loop->time)
       break;
 
     uv_timer_stop(handle);
