#!/usr/bin/env node
// The macOS test steps still run the runner under this name: see the darwin
// branch of the test step's command in .buildkite/ci.ts. The runner is
// runner.node.ts.
import "./runner.node.ts";
