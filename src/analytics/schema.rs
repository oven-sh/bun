// GENERATED: re-run the analytics schema generator (peechy) with .rs output
// source: src/analytics/schema.zig
// TODO(port): regenerate Reader/Writer + analytics::* types for Rust

// TODO(b1): stub surface for `analytics::*` until peechy regen lands.
#[allow(dead_code)]
pub mod analytics {
    pub struct Architecture(());
    pub struct OperatingSystem(());
    pub struct Platform(());
}

// ──────────────────────────────────────────────────────────────────────────
// PORT STATUS
//   source:     src/analytics/schema.zig (526 lines)
//   confidence: high
//   todos:      1
//   notes:      generated schema file — stub per PORTING.md "Don't translate"; regenerate via peechy
// ──────────────────────────────────────────────────────────────────────────
