#include "CryptoDhJob.h"

using namespace Bun;
using namespace JSC;

JSC_DEFINE_HOST_FUNCTION(jsDiffieHellman, (JSGlobalObject * globalObject, CallFrame* callFrame))
{

    return JSValue::encode(jsUndefined());
}
