// GENERATED: re-run build-prefixes.js with .rs output
// source: src/css/compat.zig (5397 lines) — autogenerated browser feature compat tables
// exports: pub enum Feature { ... } with is_compatible(&self, Browsers) / is_partially_compatible(&self, Browsers); uses bun_css::targets::Browsers
//
// B-2: stub surface so dependents type-check until build-prefixes.js emits the
// real Rust tables. The generated `Feature` enum has ~200 variants; only the
// shape (variant + two predicates over Browsers) is needed here.

use crate::targets::Browsers;

/// Placeholder — real variants are autogenerated by build-prefixes.js.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[non_exhaustive]
pub enum Feature {
    // TODO(port): autogenerated variant list (build-prefixes.js --rs)
    __Placeholder,
}

impl Feature {
    #[inline]
    pub fn is_compatible(self, _targets: Browsers) -> bool {
        // TODO(port): autogenerated compat tables (build-prefixes.js --rs)
        // Conservatively report "incompatible" so callers fall back to safe paths.
        false
    }

    #[inline]
    pub fn is_partially_compatible(self, _targets: Browsers) -> bool {
        // TODO(port): autogenerated compat tables (build-prefixes.js --rs)
        false
    }
}
