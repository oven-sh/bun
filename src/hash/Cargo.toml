[package]
name = "bun_hash"
version.workspace = true
edition.workspace = true

[lib]
path = "lib.rs"

[lints]
workspace = true

[dependencies]
# XxHash32 / XxHash64 / XxHash3 are implemented in C++/Highway (runtime-dispatched
# SIMD for XXH3); the FFI + safe wrappers live in bun_highway.
bun_highway.workspace = true
