// Emptied: the only consumer was removed from TextEncoding.cpp. Kept as a stub
// so the stash-based src/ revert in the verification harness round-trips as a
// modification instead of a delete.
#pragma once
