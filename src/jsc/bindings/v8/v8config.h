// This only exists so that files from the real V8 implementation can include it, without us having
// to add vendor/nodejs/include/node to the include path (as that would conflict with our V8 functions)
#include "node/v8config.h"
