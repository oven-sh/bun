#include "config.h"
// EventDispatcher was removed (no callers in Bun). This file is kept as an
// empty translation unit so the unified-source bundle composition
// (scripts/build/unified.ts) does not shift for the ~130 other webcore/*.cpp
// files.
