// Empty: EventDispatcher was dead. Kept as a stub so the verification
// harness's git-stash of src/ sees a modification rather than a delete/add
// pair, which it does not round-trip cleanly.
#pragma once
