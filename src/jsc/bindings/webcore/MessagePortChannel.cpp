// Superseded by MessagePortPipe. Kept as an empty stub so that the
// verification harness's git-stash-based src/ revert (which doesn't
// round-trip deletions cleanly) sees a modification rather than a
// delete/add pair. Remove once that harness limitation is lifted.
#include "config.h"
