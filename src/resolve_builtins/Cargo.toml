[package]
name = "bun_resolve_builtins"
version.workspace = true
edition.workspace = true

[lib]
path = "lib.rs"

[lints]
workspace = true

[dependencies]
strum.workspace = true
phf.workspace = true
bstr.workspace = true
scopeguard.workspace = true
const_format.workspace = true
enum-map.workspace = true
enumset.workspace = true
libc.workspace = true
bitflags.workspace = true
# bun_bundler intentionally not depended on — bun_bundler already depends on
# this crate, so adding it here would create a cycle.
# bun_bundler.workspace = true
bun_options_types.workspace = true
bun_ast.workspace = true
bun_core.workspace = true
