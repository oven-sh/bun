// Emptied: every re-export here was already re-exported by `mod.rs` and no
// external path resolved through `valkey_jsc::index::`. Kept as a stub so the
// stash-based src/ revert in the verification harness round-trips as a
// modification instead of a delete. Not in the mod tree, so never compiled.
