[package]
name = "bun_spawn_sys"
version.workspace = true
edition.workspace = true

[lib]
path = "lib.rs"

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
bstr.workspace = true
libc.workspace = true
bun_analytics.workspace = true
bun_core.workspace = true
bun_sys.workspace = true
bun_windows_sys.workspace = true  # tier-0 no_std leaf; unconditional like bun_sys/bun_core do — keeps the IoCounters alias valid on non-Windows so the flat re-export in lib.rs stays un-cfg'd
enumset.workspace = true
strum.workspace = true
ohos_sign = { path = "../ohos_sign" }

[target.'cfg(windows)'.dependencies]
bun_libuv_sys.workspace = true
