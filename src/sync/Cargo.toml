[package]
name = "bun_sync"
version.workspace = true
edition.workspace = true

[lib]
path = "lib.rs"

[lints]
workspace = true

# Tier-0: the futex + mutex primitives. Must stay below `bun_alloc` /
# `bun_core` (both depend on this crate), so only leaf dependencies here.
[dependencies]
bun_opaque.workspace = true
libc.workspace = true
thiserror.workspace = true
strum.workspace = true

[target.'cfg(windows)'.dependencies]
bun_windows_sys.workspace = true
