[test]
preload = "./preload.ts"

[install]
linker = "isolated"
# See ../bunfig.toml — the CI test runner points BUN_INSTALL_CACHE_DIR at a
# per-call tmpdir and deletes it between the root and test/ installs, and the
# global store's entries are absolute symlinks into that now-deleted cache.
globalStore = false
