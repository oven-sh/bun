import { runProjects } from "./run-fixtures";

// Programs in several files from other compilers' own tests: what came from where, under which licence and what was
// changed is in each suite's NOTICE.
runProjects("borrowed/projects");
