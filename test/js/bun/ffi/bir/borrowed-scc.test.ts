import { runFixtures } from "./run-fixtures";

// scc's own tests, copied from its repository: what came from where, under which licence and what was changed is in
// the directory's NOTICE.
runFixtures("borrowed/scc");
