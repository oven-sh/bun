# Complex Interpolation Test
BASE_URL=https://api.example.com
API_VERSION=v2
API_ENDPOINT=${BASE_URL}/${API_VERSION}
AUTH_TOKEN=bearer_token_12345
FULL_AUTH=Authorization: Bearer ${AUTH_TOKEN}
USER_ID=user123
RESOURCE_PATH=/users/${USER_ID}/profile
FULL_URL=${API_ENDPOINT}${RESOURCE_PATH}
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydb
DB_USER=admin
DB_CONNECTION=postgresql://${DB_USER}@${DB_HOST}:${DB_PORT}/${DB_NAME}
ENV=production
LOG_FILE=/var/log/${ENV}/app.log
BACKUP_PATH=/backups/${ENV}/${DB_NAME}
