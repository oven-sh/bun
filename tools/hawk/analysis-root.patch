diff --git a/src/runtime/Cargo.toml b/src/runtime/Cargo.toml
--- a/src/runtime/Cargo.toml
+++ b/src/runtime/Cargo.toml
@@ -9,7 +9,12 @@
 # to rustc, which is what makes it keep this crate's generic instances private and fold single-caller code before
 # the link. The build asks rustc for the crate's objects, never for the archive, and links them beside every other
 # crate's rlib (scripts/build/rust/units.ts UnitKind).
-crate-type = ["staticlib"]
+crate-type = ["staticlib", "rlib"]
+
+# hawk dead-code linter smoke test: throwaway analysis root. See ../../hawk.toml.
+[[bin]]
+name = "bun_hawk_root"
+path = "hawk_root.rs"
 
 [lints]
 workspace = true
