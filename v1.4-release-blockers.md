# Bun v1.4 release-blocker PR triage

Scanned **1683** open PRs from robobun + team authors. **625** flagged as potentially release-relevant by the first pass; this list is the **217** that survive a stricter filter (security / breaking / regression / memory-safety-severity).

Buckets: `ci_ok` = mergeable + CI passing or mostly-passing; `needs_rebase` = merge conflict or no CI run; `ci_failing` = mergeable but CI red; `draft` = still marked draft.

Full raw classifier output (all 625) is available on request.

---

## ✅ CI looks reasonable — 125

### Security (59)

- [ ] [#33990](https://github.com/oven-sh/bun/pull/33990) **dns: reject '/' '*' and other non-LDH chars in dns.lookup hostname** — @robobun  
      _security_: Body shows a `host/path` string "becomes a real QNAME on the wire" and `http.get({ host: "h.invalid/smug" ...})` reaches the origin — hos...  
      CI: buildkite fail (only darwin-14-aarch64 test lane failed, all others pass)
- [ ] [#32850](https://github.com/oven-sh/bun/pull/32850) **FileSystemRouter: do not let percent-decoding change URL structure** — @robobun  
      _security_: Body states "security-adjacent: an encoded / in a single URL segment could select a nested static route the author never exposed to that ...  
      CI: buildkite pass (Build #72004 passed)
- [ ] [#33960](https://github.com/oven-sh/bun/pull/33960) **fetch: reject non-HTTP(S) schemes before touching the network** — @robobun  
      _security_: Body states fetch of a non-HTTP(S) URL "leaks a DNS query for the scheme name and, if the resolver answers, opens a real connection" — SS...  
      CI: buildkite fail (only darwin-14-x64-test-bun lane failed, all others pass)
- [ ] [#33946](https://github.com/oven-sh/bun/pull/33946) **webcore/Blob: validate content-type in the structured-clone deserializer** — @robobun  
      _security_: Body describes that a "crafted serialized image" yields a Blob whose type "propagates unchanged into new Response(blob)'s Content-Type he...  
      CI: buildkite fail (2 test lanes: alpine-x64-baseline, darwin-14-x64) + cargo clippy fail
- [ ] [#32678](https://github.com/oven-sh/bun/pull/32678) **http(h3): reject DATA before final response HEADERS in the HTTP/3 client** — @robobun  
      _security_: Fixes "Unbounded buffering before the final HEADERS (DoS)" where "a hostile server could send HEADERS(:status 100) followed by an unbound...  
      CI: buildkite fail (darwin-14 aarch64 + x64 test lanes only; all linux/alpine/debian/darwin-26 pass)
- [ ] [#33714](https://github.com/oven-sh/bun/pull/33714) **usockets: stop reusePort from setting SO_REUSEADDR on Windows TCP listeners** — @robobun  
      _security_: On Windows SO_REUSEADDR "lets any local process bind the same port and receive the original listener's new connections" — a port-hijack v...  
      CI: buildkite fail (2 alpine test lanes only; all windows lanes pass)
- [ ] [#33865](https://github.com/oven-sh/bun/pull/33865) **Bun.password: reject argon2 cost options that verify would refuse** — @robobun  
      _security_: Body shows "out-of-i32-range options silently wrap mod 2^32 into near-minimum cost" for argon2 password hashing, and hash() can mint PHC ...  
      CI: buildkite fail (darwin-14-x64 test lane only); TypeScript types check fail
- [ ] [#32753](https://github.com/oven-sh/bun/pull/32753) **install: validate bun.lockb slice descriptors and ids at load time** — @robobun  
      _security_: "A corrupt or crafted bun.lockb crashes bun" with out-of-range panic in release builds; untrusted lockfile input reaches unchecked slice ...  
      CI: buildkite fail (only alpine-3.23 x64 + x64-baseline test lanes; all other platforms pass)
- [ ] [#33702](https://github.com/oven-sh/bun/pull/33702) **Bun.password: validate async hash/verify args like the sync forms** — @robobun  
      _security_: Without this, "await Bun.password.hash(req.body.password) with a missing field would silently store a hash of 'undefined'" — an auth foot...  
      CI: buildkite fail (aggregate only; all individual test lanes pass)
- [ ] [#33732](https://github.com/oven-sh/bun/pull/33732) **node:http: wire server socket/response setTimeout to the native inactivity timer** — @robobun  
      _security_: Per-connection setTimeout is a silent no-op so "the canonical slowloris / slow-client mitigation in Node.js" does nothing and "the stalle...  
      CI: buildkite fail (only darwin-14-aarch64-test-bun lane failed, all other platforms pass)
- [ ] [#33757](https://github.com/oven-sh/bun/pull/33757) **url: preprocess host/hostname setter input so IDNA matches the constructor** — @robobun  
      _security_: Host validation bypass vector: "a filter that validates `new URL(x)` and then assigns `u.hostname = x` validates a different host than it...  
      CI: buildkite pass (Build #70498 passed)
- [ ] [#33201](https://github.com/oven-sh/bun/pull/33201) **url: reject invalid Punycode (xn--) labels in special-scheme hosts** — @robobun  
      _security_: Body explicitly states "This is a parser differential on the host, the security-relevant component of a URL: anything that validates a UR...  
      CI: buildkite fail (only darwin-26-aarch64-test-bun failed, all other lanes pass)
- [ ] [#33717](https://github.com/oven-sh/bun/pull/33717) **http: reject requests with multiple Host header field lines (RFC 9112 5.4)** — @robobun  
      _security_: Body explicitly states the "split-brain authority is the host-header-confusion / cache-poisoning shape" that RFC 9112's MUST exists to pr...  
      CI: buildkite fail (darwin-14-aarch64 expired, darwin-14-x64 exit 2; all other lanes pass)
- [ ] [#33761](https://github.com/oven-sh/bun/pull/33761) **glob: stop bracket expressions from matching path separators in match()** — @robobun  
      _security_: Path-traversal containment bypass: "`uploads/[!.]*` is meant to be confined to `uploads/` but matches `uploads/../secret`".  
      CI: buildkite fail (2 lanes: alpine-aarch64 test, darwin-14-x64 test; rest pass)
- [ ] [#33764](https://github.com/oven-sh/bun/pull/33764) **node:vm: enforce timeout as a wall-clock deadline that interrupts Atomics.wait** — @robobun  
      _security_: Sandbox timeout bypass: "`timeout` is the whole point of running untrusted code through `node:vm`, so a guest can currently evade the bud...  
      CI: buildkite fail (3 lanes: alpine-x64, alpine-x64-baseline, darwin-14-x64; most lanes pass incl. asan/debian/aarch64)
- [ ] [#32992](https://github.com/oven-sh/bun/pull/32992) **node:http2: tie the stream receive window to what the JS readable consumes** — @robobun  
      _security_: Unbounded memory / DoS vector: "no inbound flow control... Any server can push tens of MB per second into a client that is not reading."  
      CI: buildkite fail (only darwin-26-aarch64-test-bun lane failed, all other lanes pass)
- [ ] [#33522](https://github.com/oven-sh/bun/pull/33522) **node:crypto: report DH_check() results the way OpenSSL 3 does** — @robobun  
      _security_: Diffie-Hellman validation is broken both ways: "createDiffieHellman(32) is accepted and keys off a 32-bit 'prime'" (weak params allowed) ...  
      CI: buildkite fail (1 lane: ubuntu-25.04-x64-baseline expired; all others pass)
- [ ] [#33492](https://github.com/oven-sh/bun/pull/33492) **Reject Bun.serve response header values that cannot be written as an HTTP field-value** — @robobun  
      _security_: Header-injection surface: "Bun.serve writes response header values containing C0 control octets and DEL... onto the wire verbatim" allowi...  
      CI: buildkite fail (6 lanes Expired infra, 1 darwin-26-aarch64 test fail; most lanes pass)
- [ ] [#33576](https://github.com/oven-sh/bun/pull/33576) **postgres: validate binary datum length and range before decoding** — @robobun  
      _security_: Untrusted-input hardening: "A lying peer could inject data that no query produced, indistinguishable from a real column value" via MitM/p...  
      CI: buildkite fail (darwin lanes only: 2 expired, 1 darwin-26-aarch64 test failure); all linux/windows/musl pass
- [ ] [#32832](https://github.com/oven-sh/bun/pull/32832) **node:http2: don't kill the process on a malformed HEADERS block sent to a server** — @robobun  
      _security_: Body states "Any peer that can open a TCP connection to an h2/h2c Bun server can trigger it" and "kills the whole process" — a remote DoS...  
      CI: buildkite fail (2 darwin-aarch64 lanes: one expired, one test failure; all other platforms pass)
- [ ] [#33483](https://github.com/oven-sh/bun/pull/33483) **tls: don't request client certificates just because a ca was supplied** — @robobun  
      _security_: TLS/mTLS weakness: body states a server with `ca` "broke every ordinary client while still admitting any certificate from any issuer" — e...  
      CI: buildkite fail (darwin-14-aarch64 expired, darwin-26-aarch64 test failure; all other lanes pass)
- [ ] [#33181](https://github.com/oven-sh/bun/pull/33181) **Reject interior null bytes in Bun.mmap, Glob.scan, and FileSystemRouter paths** — @robobun  
      _security_: NUL-byte path truncation: "the OS operates on a different, shorter path than the one JS-level code saw and validated" — classic path vali...  
      CI: buildkite fail (only darwin-14-aarch64 lane expired; all others pass)
- [ ] [#32749](https://github.com/oven-sh/bun/pull/32749) **install: hash patch contents with SHA-1 instead of Wyhash11** — @robobun  
      _security_: Fixes "patchedDependencies cache-poisoning" where Wyhash11 collisions let one project's patch be served as another's from the shared cache.  
      CI: buildkite fail (4 lanes expired/infra, 1 darwin-26-aarch64 test failure)
- [ ] [#33541](https://github.com/oven-sh/bun/pull/33541) **node:https: enforce server handshakeTimeout** — @robobun  
      _security_: Body states an unfinished TLS handshake "was kept open indefinitely, holding a socket and BoringSSL state bounded only by the fd limit (a...  
      CI: buildkite fail (2 darwin lanes expired, 1 darwin-26-aarch64 test fail; all linux/alpine/debian/asan pass)
- [ ] [#33539](https://github.com/oven-sh/bun/pull/33539) **node:https: fail closed when createServer has no key or cert** — @robobun  
      _security_: Body states https.createServer with no key/cert "serves plaintext HTTP on the 'https' listener" and "turns an intended https endpoint int...  
      CI: buildkite fail (2 darwin-aarch64 test lanes failed; 3 aarch64 lanes expired/infra; all other lanes pass)
- [ ] [#33570](https://github.com/oven-sh/bun/pull/33570) **net: strict canonical address parsing for SocketAddress and BlockList** — @robobun  
      _security_: BlockList bypass: "false negative on a blocked address" and "a blocklist built from non-canonical text silently guards a different addres...  
      CI: buildkite fail (darwin-26-aarch64 test fail; 3 build jobs expired/infra)
- [ ] [#33513](https://github.com/oven-sh/bun/pull/33513) **tls: apply sessionTimeout to the server's SSL context** — @robobun  
      _security_: TLS sessionTimeout is "validated and then discarded" so "tickets a server mints stay resumable for hours regardless of what the applicati...  
      CI: buildkite fail (mostly expired/infra jobs; 1 real test fail on darwin-26-aarch64, other test lanes pass)
- [ ] [#33629](https://github.com/oven-sh/bun/pull/33629) **node:tls: honor the crl option when building a SecureContext** — @robobun  
      _security_: CRL option silently dropped so "a client certificate revoked by the supplied CRL was fully authorized (authorized === true)" — revoked ce...  
      CI: buildkite fail (only darwin-14-x64-test-bun, exit status -1 infra failure; all other lanes pass)
- [ ] [#33633](https://github.com/oven-sh/bun/pull/33633) **http: reject DEL (0x7F) in request header field values** — @robobun  
      _security_: HTTP parser differential where "two hops in a chain disagree on whether the request is valid"; tests added to request-smuggling.test.ts.  
      CI: buildkite fail (only darwin-14-x64-test-bun failed, all other lanes pass)
- [ ] [#33666](https://github.com/oven-sh/bun/pull/33666) **Bun.SQL(postgres): send plaintext startup when sslmode=prefer and server declines TLS** — @robobun  
      _security_: Fixes both a connection hang ("the connection idles until connectionTimeout") and a security hole: the old `continue` dispatching trailin...  
      CI: buildkite fail (darwin-14-aarch64 expired, darwin-26-aarch64 test failure; all other lanes pass)
- [ ] [#33643](https://github.com/oven-sh/bun/pull/33643) **node:http2: handle an oversized request header list as a stream error, not a process crash** — @robobun  
      _security_: Remotely-triggerable DoS/crash: an oversized header list "kills the whole node:http2 server process with an uncatchable ERR_HTTP2_STREAM_...  
      CI: buildkite fail (only darwin-26-aarch64-test-bun lane failed; all other builds/tests passed)
- [ ] [#33671](https://github.com/oven-sh/bun/pull/33671) **sql(postgres): reject duplicate authentication requests** — @robobun  
      _security_: "A server (or active MITM...) drives Bun into an unbounded 100% CPU authentication loop, pre-auth" with "connectionTimeout never fires" —...  
      CI: buildkite fail (only darwin-14 aarch64+x64 test lanes; all other platforms pass)
- [ ] [#33426](https://github.com/oven-sh/bun/pull/33426) **node:url: treat ws: and wss: as slashed protocols** — @robobun  
      _security_: Host-confusion / allow-list bypass risk: resolving a relative reference against a ws: base "replaces the host with a segment of that rela...  
      CI: buildkite fail (all failures are Expired infra jobs; all executed jobs passed)
- [ ] [#33460](https://github.com/oven-sh/bun/pull/33460) **url: stop running url.parse() hostnames through the WHATWG host parser** — @robobun  
      _security_: Body explicitly notes "Hostname allow-lists can be bypassed or broken by the hex/octal/dword rewrite" and a "reachable-input throw" — all...  
      CI: buildkite fail (darwin-14-aarch64 expired, darwin-26-aarch64 test fail; all other lanes pass)
- [ ] [#33392](https://github.com/oven-sh/bun/pull/33392) **tls: reject secureProtocol combined with minVersion or maxVersion** — @robobun  
      _security_: TLS weakness: "the minVersion floor is silently dropped... negotiated the legacy version with no error and nothing in the logs."  
      CI: buildkite overall fail but all individual lanes pass
- [ ] [#32827](https://github.com/oven-sh/bun/pull/32827) **webcrypto: validate the OKP JWK x parameter against d on import** — @robobun  
      _security_: Missing RFC 8037 validation lets a JWK with mismatched x/d import successfully so "two consumers of the same JWK silently end up with dif...  
      CI: buildkite fail (aggregate only; all 74 sub-lanes pass)
- [ ] [#33373](https://github.com/oven-sh/bun/pull/33373) **url: validate percent-decoding and platform path rules in fileURLToPath** — @robobun  
      _security_: Path-validation bypass: "Returning \"\" instead of throwing turns a malformed URL into 'the current directory' for whatever fs call recei...  
      CI: buildkite fail (aggregate only); all individual build/test lanes pass
- [ ] [#33367](https://github.com/oven-sh/bun/pull/33367) **node:tls: apply TLS 1.3 cipher names and honorCipherOrder on servers** — @robobun  
      _security_: TLS server silently ignores cipher configuration: "honorCipherOrder was read nowhere" and TLS 1.3 cipher lists fail or are dropped, so se...  
      CI: buildkite fail (aggregate only; all 75 sub-lanes pass)
- [ ] [#33364](https://github.com/oven-sh/bun/pull/33364) **shell: scope `FOO=bar cmd` prefix assignments to that command** — @robobun  
      _security_: Prefix env assignments leak to every later command: "promoting it to a script-wide export leaks it into every later child... curl inherit...  
      CI: buildkite fail (aggregate only; all individual lanes pass)
- [ ] [#33052](https://github.com/oven-sh/bun/pull/33052) **bundler: keep a "__proto__" export name an own property in generated exports** — @robobun  
      _security_: Bundler emits bare __proto__ keys so "the key is dropped and the prototype is replaced" — a prototype-pollution vector in generated bundl...  
      CI: buildkite pass (build #68306)
- [ ] [#33272](https://github.com/oven-sh/bun/pull/33272) **ci: ignore image publish and windows signing tags on fork PRs** — @alii  
      _security_: Prevents supply-chain attack where a fork PR author controls commit subject and could "enable privileged pipeline steps: baking/publishin...  
      CI: buildkite pass (Build #68074 passed)
- [ ] [#33287](https://github.com/oven-sh/bun/pull/33287) **ws: enforce WebSocketServer maxPayload option** — @robobun  
      _security_: Resource-limit bypass / DoS vector: "`maxPayload` option was ignored under Bun: a frame larger than `maxPayload` was delivered to the `me...  
      CI: buildkite fail (only darwin-14-aarch64-test-bun lane failed, all others pass)
- [ ] [#33238](https://github.com/oven-sh/bun/pull/33238) **sql: reject a MySQL prepare-OK carrying statement_id 0** — @robobun  
      _security_: Body states "a compromised or desynced MySQL-compatible server or proxy can trigger it with a single frame" and "the very next execute ab...  
      CI: buildkite fail (only alpine-3.23-x64-test-bun lane failed, all others pass)
- [ ] [#33273](https://github.com/oven-sh/bun/pull/33273) **ci: drop gh api from the triage commands' allowed tools** — @alii  
      _security_: Hardens automated triage against prompt injection: commands "run automatically against inbound issues and PRs... with a repo-scoped token...  
      CI: buildkite fail (aggregate only; all 60 individual lanes pass)
- [ ] [#33095](https://github.com/oven-sh/bun/pull/33095) **bun:ffi: coerce JSCallback return values to the declared return type** — @robobun  
      _security_: Body states "the integer cases leak heap pointer material (ASLR-relevant) to whatever native code consumes the callback's result" and als...  
      CI: buildkite aggregate fail but all individual lanes pass
- [ ] [#33096](https://github.com/oven-sh/bun/pull/33096) **Normalize the request-target path before matching Bun.serve routes** — @robobun  
      _security_: Route/auth bypass via raw request-target path traversal: "Handlers attached to a route...can be skipped by the raw spelling" using `/admi...  
      CI: buildkite fail (overall marked failed, but all 75 individual lanes pass)
- [ ] [#33054](https://github.com/oven-sh/bun/pull/33054) **node:https: apply the full TLS option set when creating a Server** — @Jarred-Sumner  
      _security_: TLS options like minVersion/maxVersion/ciphers were silently dropped and "a pfx- or secureContext-only configuration was treated as plain...  
      CI: buildkite fail (darwin-14-aarch64 test lane) + Format check fail; all other lanes pass
- [ ] [#32677](https://github.com/oven-sh/bun/pull/32677) **patch: refuse symlink and hard link escapes when applying patches** — @robobun  
      _security_: Fixes a path-traversal / arbitrary-write vulnerability: "applying a patch writes the patch's hunk bytes through it into any file the user...  
      CI: buildkite fail (only darwin-14-aarch64 test lane, exit status 2; all other lanes pass)
- [ ] [#32910](https://github.com/oven-sh/bun/pull/32910) **node:crypto: validate OKP private JWK x matches d in createPrivateKey** — @robobun  
      _security_: Body notes "A maliciously inconsistent private JWK is supposed to be rejected at the trust boundary; silently repairing it" — crypto key ...  
      CI: buildkite fail (darwin-26-aarch64 + windows-aarch64 test fails; several lanes Expired/infra; most test lanes pass)
- [ ] [#33002](https://github.com/oven-sh/bun/pull/33002) **Bun.serve: use the absolute-form request-target's authority for request.url** — @robobun  
      _security_: Body explicitly cites "host-confusion chains (cache poisoning, vhost/origin-check bypass)" and reverts a recent regression from #31495 th...  
      CI: buildkite fail (darwin-14 aarch64/x64 expired, darwin-26-aarch64 test failure; all linux/alpine/debian/freebsd/windows-build lanes pass)
- [ ] [#32923](https://github.com/oven-sh/bun/pull/32923) **dns.lookup: return every hosts-file address family instead of only the first line's** — @robobun  
      _security_: Body highlights "an A pinned.test query was actually sent to the configured nameserver, so a hostname that was pinned locally leaked to t...  
      CI: buildkite fail (5 lanes Expired - infra; all actual test jobs passed)
- [ ] [#32721](https://github.com/oven-sh/bun/pull/32721) **patch: reject paths with embedded NUL in is_safe_patch_path** — @robobun  
      _security_: Embedded-NUL path truncation lets a crafted patch write to the wrong file; "reachable from bun install via a crafted patchedDependencies ...  
      CI: buildkite fail (darwin-14-x64-test-bun, windows-11-aarch64-test-bun; all other lanes pass)
- [ ] [#32746](https://github.com/oven-sh/bun/pull/32746) **Bun.serve(http3): fix heap use-after-free when client resets a streaming response** — @robobun  
      _security_: "Fixes a remotely-triggerable heap-use-after-free in the HTTP/3 server" when a client resets a stream mid-response.  
      CI: buildkite fail (2 lanes: alpine-x64-baseline, darwin-14-x64; rest pass incl ASAN)
- [ ] [#29959](https://github.com/oven-sh/bun/pull/29959) **node:vm: allocate dynamic-import rejection error in the vm realm** — @robobun  
      _security_: Sandbox escape: "err.constructor.constructor('return process')(); // returns the host process (escapes the vm)" via dynamic-import reject...  
      CI: buildkite fail (darwin-14/26-aarch64 test lanes only; all other platforms pass)
- [ ] [#29997](https://github.com/oven-sh/bun/pull/29997) **node:vm: throw defineProperty TypeErrors in the vm realm** — @robobun  
      _security_: Sandbox escape: "err.constructor.constructor('return process')() leaks the host process" — TypeError allocated in host realm lets vm code...  
      CI: buildkite fail (darwin-14-aarch64 + darwin-26-aarch64 test lanes only; all other platforms pass)
- [ ] [#31924](https://github.com/oven-sh/bun/pull/31924) **css: bound light-dark() origin expansion depth in relative colors** — @robobun  
      _security_: Exponential O(2^depth) CSS minifier output ("~25 levels reached gigabytes") described as "A DoS vector for anything minifying attacker-su...  
      CI: buildkite fail (3 darwin test lanes only; all linux/asan/windows pass)
- [ ] [#29998](https://github.com/oven-sh/bun/pull/29998) **node:vm: allocate prepareStackTrace call sites in the vm realm** — @robobun  
      _security_: Sandbox escape: "sites.constructor.constructor('return process')() reached the host process" — host process reachable from inside node:vm...  
      CI: buildkite fail (2 darwin test lanes: darwin-14-x64, darwin-26-aarch64); all linux/alpine/debian/ubuntu/freebsd/windows builds+tests pass
- [ ] [#31327](https://github.com/oven-sh/bun/pull/31327) **install: pin a computed sha512 for npm packages whose manifest has no usable integrity** — @Jarred-Sumner  
      _security_: Supply-chain integrity gap: npm packages with missing/unparseable integrity "were recorded in the lockfile with an empty integrity and ne...  
      CI: buildkite pass (build #58769)
- [ ] [#31581](https://github.com/oven-sh/bun/pull/31581) **Tighten ffi pointer bounds, sparse archive extraction, and the Windows default trust store** — @Jarred-Sumner  
      _security_: Three hardening fixes: ffi "negative or oversized offset produced a pointer outside the buffer", sparse-tar DoS "a small archive can no l...  
      CI: buildkite pass

### Breaking changes (20)

- [ ] [#33255](https://github.com/oven-sh/bun/pull/33255) **Reject unknown import attribute type values** — @robobun  
      _breaking-change_: Changes user-facing behavior from silently accepting to rejecting: "An unknown `type` import attribute is silently accepted... Node rejec...  
      CI: buildkite fail (only darwin-14-x64-test-bun lane failed, all others pass)
- [ ] [#28512](https://github.com/oven-sh/bun/pull/28512) **Throw ERR_IMPORT_ATTRIBUTE_UNSUPPORTED for unsupported import attributes** — @robobun  
      _breaking-change_: Intentionally changes behavior so imports that "Bun silently ignores" now "reject with TypeError [ERR_IMPORT_ATTRIBUTE_UNSUPPORTED]", whi...  
      CI: buildkite umbrella fail but all 76 individual lanes pass
- [ ] [#32953](https://github.com/oven-sh/bun/pull/32953) **Rewrite the TOML parser for v1.1.0 conformance** — @dylan-conway  
      _breaking-change_: Replaces the parser with spec-exact behavior: previously-accepted invalid docs now rejected, "Integers outside Number.MAX_SAFE_INTEGER th...  
      CI: buildkite fail (1 lane: darwin-26-aarch64 test; plus robobun TODO/FIXME hygiene gate)
- [ ] [#33438](https://github.com/oven-sh/bun/pull/33438) **require(esm): throw ERR_REQUIRE_ASYNC_MODULE instead of a bare TypeError** — @robobun  
      _breaking-change_: PR body explicitly flags "Note this is a behavior change for anyone matching `e instanceof TypeError`" — thrown error class changes from ...  
      CI: buildkite fail (4 lanes expired: debian-13-aarch64, linux-x64-baseline-build-cpp, linux-x64-musl-baseline-verify, ubuntu-25.04-aarch64 — infra expirations, no real test failures)
- [ ] [#33897](https://github.com/oven-sh/bun/pull/33897) **Bun.serve: reject unreachable route patterns (non-final '*', '?')** — @robobun  
      _breaking-change_: Route patterns that were previously "accepted silently at registration" now throw at Bun.serve() / server.reload(), a user-facing behavio...  
      CI: buildkite aggregate fail but all individual lanes pass
- [ ] [#33899](https://github.com/oven-sh/bun/pull/33899) **parser: .cjs/"type":"commonjs" rejects ESM export and top-level await** — @robobun  
      _breaking-change_: Intentionally changes behavior so files that previously "loads and runs on Bun" now emit a hard SyntaxError, only safe at a minor boundary.  
      CI: buildkite aggregate fail but all 75+ individual lanes pass
- [ ] [#33276](https://github.com/oven-sh/bun/pull/33276) **fetch: cap redirects at 20 and reject redirect failures with a TypeError** — @robobun  
      _breaking-change_: Changes default redirect cap from 127 to 20 and rejection type from Error to TypeError — "a 25-hop redirect chain resolves with a `200`" ...  
      CI: buildkite fail (2 lanes: alpine-x64-baseline, darwin-14-x64) + TypeScript types check fail
- [ ] [#33007](https://github.com/oven-sh/bun/pull/33007) **fetch: make the in-flight request cap per-origin instead of process-global** — @robobun  
      _breaking-change_: Changes the meaning of BUN_CONFIG_MAX_HTTP_REQUESTS from process-global to per-origin ("is now a per-origin cap"), a user-facing default-...  
      CI: buildkite fail (2 alpine-x64 test lanes; all other platforms pass)
- [ ] [#33842](https://github.com/oven-sh/bun/pull/33842) **process: throw ERR_WORKER_UNSUPPORTED_OPERATION for process-wide mutators in workers** — @robobun  
      _breaking-change_: Intentionally changes behavior so process.chdir()/umask()/setuid() etc. in workers now throw instead of succeeding and mutating process-w...  
      CI: buildkite fail (2 lanes: alpine-3.23-x64 + darwin-14-aarch64 test-bun; all other platforms pass)
- [ ] [#33476](https://github.com/oven-sh/bun/pull/33476) **node:tls: do not derive the SNI server_name from host in tls.connect** — @robobun  
      _breaking-change_: Changes default TLS wire behavior for every tls.connect: "Wire-visible divergence for every raw tls.connect user" — removing the implicit...  
      CI: buildkite fail (only darwin-14-aarch64 lane expired; all other lanes pass)
- [ ] [#33298](https://github.com/oven-sh/bun/pull/33298) **webcrypto: validate the view type and 65536-byte quota in getRandomValues** — @robobun  
      _breaking-change_: Adds spec-mandated throws where Bun previously "silently filled", so "code written against Bun ships patterns that throw everywhere else"...  
      CI: buildkite fail (only darwin-14-x64-test-bun failed, all other lanes pass)
- [ ] [#32434](https://github.com/oven-sh/bun/pull/32434) **Move `name` and `lastModified` from `Blob.prototype` to `File.prototype`** — @robobun  
      _breaking-change_: Changes prototype chain so `"name" in new Blob()` goes from true→false and `File.prototype === Blob.prototype` goes from true→false — an ...  
      CI: buildkite pass (Build #68407 passed)
- [ ] [#32255](https://github.com/oven-sh/bun/pull/32255) **Return Subprocess resourceUsage() as a plain object of numbers** — @robobun  
      _breaking-change_: Changes cpuTime fields from BigInt to number and replaces the native ResourceUsage class with a plain object — user-visible return-type c...  
      CI: buildkite aggregate fail but all 75 individual lanes pass
- [ ] [#32952](https://github.com/oven-sh/bun/pull/32952) **test: require an Error for .rejects.toThrow() and .resolves.toThrow()** — @robobun  
      _breaking-change_: Changes bun:test matcher verdicts so suites that currently pass will start failing: "Same code, opposite verdict across runners" — a user...  
      CI: buildkite pass (Build #66441 passed)
- [ ] [#32883](https://github.com/oven-sh/bun/pull/32883) **node:dns: reject falsy hostnames in lookup() with ERR_INVALID_ARG_VALUE** — @robobun  
      _breaking-change_: Changes dns.lookup("")/lookup(null) from resolving {address:null,family:4} to throwing ERR_INVALID_ARG_VALUE ("Node.js removed DEP0118 en...  
      CI: buildkite fail (only 2 darwin-aarch64 lanes: 1 expired, 1 exit-1; all linux/windows/x64 lanes pass)
- [ ] [#32833](https://github.com/oven-sh/bun/pull/32833) **hash: use the full 64-bit seed in Bun.hash.xxHash3** — @robobun  
      _breaking-change_: Removes seed truncation that was explicitly kept "for output stability", so xxHash3 output changes for any seed >= 2^32 — an observable o...  
      CI: buildkite aggregate fail but all individual lanes pass
- [ ] [#30284](https://github.com/oven-sh/bun/pull/30284) **Skip `dist/` and `build/` by default during `bun test` discovery** — @robobun  
      _breaking-change_: Changes default test discovery behavior ("Scanner now falls back to [\"**/dist/**\", \"**/build/**\"] when the user hasn't set pathIgnore...  
      CI: buildkite fail (2 lanes: darwin-26-aarch64, windows-11-aarch64 test-bun; all other platforms pass)
- [ ] [#30848](https://github.com/oven-sh/bun/pull/30848) **sql(mysql): add foundRows option to opt into CLIENT_FOUND_ROWS** — @robobun  
      _breaking-change_: Changes the MySQL default: "Default is `true`, matching the mysql2 / mariadb defaults" — flips affectedRows semantics from changed-rows t...  
      CI: buildkite fail (only darwin-14-aarch64-test-bun, exit 255; all other lanes pass)
- [ ] [#31718](https://github.com/oven-sh/bun/pull/31718) **Don't define `self` on the main thread** — @robobun  
      _breaking-change_: Removes the global `self` accessor from the main thread ("Bun defined `self` on every global object — main thread included"), a user-visi...  
      CI: buildkite fail (1 lane: ubuntu-25.04-x64-baseline test, exit 2)
- [ ] [#29305](https://github.com/oven-sh/bun/pull/29305) **fix(cjs): match Node.js CJS→ESM interop for __esModule default** — @robobun _(stale)_  
      _breaking-change_: Intentionally changes what `import(cjs).default` returns (from unwrapped `exports.default` to the whole `module.exports` object) — a user...  
      CI: buildkite fail (2 lanes: darwin-14-aarch64-test-bun, windows-11-aarch64-test-bun; rest pass)

### Node compat regressions (13)

- [ ] [#33808](https://github.com/oven-sh/bun/pull/33808) **Add regression test for AsyncLocalStorage context loss in .then() continuations after DFG tier-up** — @robobun  
      _node-compat-regression_: Explicit regression: "Bun 1.4.0 prints FAIL 7538/8000... Bun 1.3.14 prints PASS" — AsyncLocalStorage context lost in .then() after JIT ti...  
      CI: buildkite fail (only darwin-14-aarch64-test-bun failed, all other lanes pass)
- [ ] [#33467](https://github.com/oven-sh/bun/pull/33467) **node:stream: restore the destroyed-stream guard in Readable.prototype.pause** — @robobun  
      _node-compat-regression_: Regression: title says "restore" and body notes "The guard was removed from both pause() and resume() in 6abf57e204", causing readableFlo...  
      CI: buildkite fail (3 expired jobs + darwin-26-aarch64 test failure; most lanes pass)
- [ ] [#32987](https://github.com/oven-sh/bun/pull/32987) **node:http2: treat frames on idle streams as connection errors (RFC 9113 5.1)** — @robobun  
      _node-compat-regression_: Regression from inbound engine rewrite: "so did Bun's legacy inbound parser. The rewritten inbound engine (#31584) answers every unknown ...  
      CI: buildkite fail (aggregate only; all individual lanes pass)
- [ ] [#32300](https://github.com/oven-sh/bun/pull/32300) **os.networkInterfaces: fix IPv6 address/netmask/cidr formatting** — @robobun  
      _node-compat-regression_: Node compat regressed from Zig reference: "every IPv6 entry from `os.networkInterfaces()` had `address`/`netmask`/`cidr` set to the liter...  
      CI: buildkite pass (Build #62523 passed)
- [ ] [#33416](https://github.com/oven-sh/bun/pull/33416) **node:fs: apply cpSync errorOnExist per entry, not to the destination directory** — @robobun  
      _node-compat-regression_: Regression introduced by a prior PR: the errant check "was copied into the sync path in #31830, where only the async half belonged," so n...  
      CI: buildkite fail (overall only; all 74 individual lanes passed)
- [ ] [#29135](https://github.com/oven-sh/bun/pull/29135) **udp: only enable IP_RECVERR on connected sockets** — @robobun  
      _node-compat-regression_: "#28827 enabled IP_RECVERR... on every UDP socket" causing "Any UDP service (DNS forwarder, statsd emitter, game server) dies the moment ...  
      CI: buildkite fail (aggregate only; all 76 individual lanes pass)
- [ ] [#33184](https://github.com/oven-sh/bun/pull/33184) **module loader: fix false "require() async module" for a module racing a concurrent import** — @robobun  
      _node-compat-regression_: Body states "Node and Bun 1.3.11 both return it; 1.3.14+ throws. It regressed in the module-loader rewrite (#29393)" — explicit Node-comp...  
      CI: buildkite fail (darwin-14-x64-test-bun, darwin-26-aarch64-test-bun); all linux/alpine/debian/musl/freebsd/android lanes pass
- [ ] [#32386](https://github.com/oven-sh/bun/pull/32386) **worker: drop queued microtasks after an uncaught error or process.exit** — @robobun  
      _node-compat-regression_: Node compat behavior that worked in Zig and broke in the Rust port: "Node (and Bun's Zig reference) drop that microtask" but current Bun ...  
      CI: buildkite pass (build #64902)
- [ ] [#32373](https://github.com/oven-sh/bun/pull/32373) **worker: run process.on('exit') after uncaught error in event loop** — @robobun  
      _node-compat-regression_: Worker exit handlers skipped vs Node and prior Bun: "The Zig reference ... reaches onExit() with no termination state and the handlers ru...  
      CI: buildkite fail (aggregate failed but all individual lanes pass)
- [ ] [#32292](https://github.com/oven-sh/bun/pull/32292) **fs: treat negative position in readv/writev as current file position** — @robobun  
      _node-compat-regression_: node:fs readv/writev with position -1 now throws "EINVAL: invalid argument, preadv" where Zig 1.3.x did not error and Node treats it as c...  
      CI: buildkite fail (3 darwin test lanes expired - infra issue; all linux/alpine/debian/freebsd/android pass)
- [ ] [#32326](https://github.com/oven-sh/bun/pull/32326) **worker_threads: honor unref()/{ref:false} on nested workers** — @robobun  
      _node-compat-regression_: Body shows "Node and bun 1.3.14 (Zig) print `unrefed` / `outer-exit 0`... Current bun prints `HANG`" — nested worker unref() regressed to...  
      CI: buildkite fail (3 darwin test lanes expired/infra, all linux/alpine/debian/freebsd/musl pass)
- [ ] [#31809](https://github.com/oven-sh/bun/pull/31809) **console.dir: honor maxArrayLength in the native formatter** — @robobun  
      _node-compat-regression_: Body explicitly states "This is a regression: per the issue, maxArrayLength worked in v1.1.0 and stopped taking effect somewhere before v...  
      CI: buildkite fail (3 alpine test lanes + windows-x64-baseline-verify-baseline; darwin/debian/linux all pass)
- [ ] [#29432](https://github.com/oven-sh/bun/pull/29432) **tls: verify IP host when upgrading an existing socket** — @robobun _(stale)_  
      _node-compat-regression_: Explicitly "Regression since #29096" in TLS cert verification that "breaks the STARTTLS pattern used by pg, mysql2, ioredis, nodemailer" ...  
      CI: buildkite fail (2 lanes: alpine-aarch64, darwin-14-aarch64 test-bun; all others pass)

### Performance regressions (5)

- [ ] [#32350](https://github.com/oven-sh/bun/pull/32350) **http: pool the HTTP/1.1 request build buffer instead of reallocating per request** — @robobun  
      _perf-regression_: Explicit perf regression in a hot path: "The Rust port of RequestBodyBuffer regressed from the Zig original's zero-heap-traffic design to...  
      CI: buildkite fail (only darwin-14-aarch64-test-bun, all other lanes pass)
- [ ] [#33529](https://github.com/oven-sh/bun/pull/33529) **Route node:net DNS resolution through Bun's DNS cache** — @robobun  
      _perf-regression_: Body states "node:net connected through Bun.connect (and therefore the cache) until the node:net rework in #18962" — DNS caching regresse...  
      CI: buildkite fail (6 lanes expired - infrastructure, all executed tests passed)
- [ ] [#32348](https://github.com/oven-sh/bun/pull/32348) **crypto: use BoringSSL RAND_bytes for node:crypto random functions** — @robobun  
      _perf-regression_: Body states `crypto.randomInt()` "regressed to making one kernel syscall per call instead of using a userspace CSPRNG" — 5000 syscalls vs...  
      CI: buildkite fail (only darwin-14-x64-test-bun lane failed, all others pass)
- [ ] [#32312](https://github.com/oven-sh/bun/pull/32312) **resolver: store exports expansion_keys as indices instead of cloned subtrees** — @robobun  
      _perf-regression_: Explicit memory regression vs prior (Zig) release: "paid roughly 2x the heap for their exports map ... Pure memory regression vs the Zig ...  
      CI: buildkite aggregate fail but all 75 individual lanes pass
- [ ] [#32295](https://github.com/oven-sh/bun/pull/32295) **node:fs: skip zero-fill of 256 KB pre-stat buffer in async readFile** — @robobun  
      _perf-regression_: Perf regression vs Zig reference on every async fs.readFile: "The 256 KB memset is wasted work the Zig version did not do."  
      CI: buildkite fail (only darwin-14-x64-test-bun lane, all others pass)

### Critical bugs (crash / UAF / leak / corruption) (28)

- [ ] [#32257](https://github.com/oven-sh/bun/pull/32257) **usockets: stop deriving eof from kqueue EV_EOF** — @Jarred-Sumner  
      _critical-bug_: On macOS kqueue "closes with the response still in the kernel buffer ... ConnectionClosed from fetch()" — dropped response bytes / data l...  
      CI: buildkite pass
- [ ] [#33884](https://github.com/oven-sh/bun/pull/33884) **install: never delete a concurrently published cache entry on Windows** — @Jarred-Sumner  
      _critical-bug_: Race where concurrent `bun install` on Windows "renames the winner's already-published cache entry out of the cache and delete_trees it",...  
      CI: buildkite fail (windows-2019-x64 + alpine-aarch64 exit 2, windows-11-aarch64 expired; rest pass)
- [ ] [#33838](https://github.com/oven-sh/bun/pull/33838) **crypto: fix generatePrime infinite loop and oversized output with add/rem** — @robobun  
      _critical-bug_: Body states generatePrime "loops forever at 100% CPU", "sync form wedges the whole process" and "anywhere add/rem derive from configurati...  
      CI: buildkite pass (build #70977)
- [ ] [#32215](https://github.com/oven-sh/bun/pull/32215) **serve: trace handler callbacks from the wrapper instead of rooting them as Strong** — @alii  
      _critical-bug_: Fixes Bun.serve memory leak where a handler closing over server "forms a cycle the GC can't see through" so "the box is never freed", plu...  
      CI: buildkite fail (2 alpine musl test lanes only; all other platforms pass)
- [ ] [#32860](https://github.com/oven-sh/bun/pull/32860) **Request: dependent AbortSignal per spec; async_hooks: fix ALS disable/run leak and AsyncResource.bind** — @robobun  
      _critical-bug_: Fixes AsyncLocalStorage where "disable() then run() leaks" the value permanently into the async context and nested disable throws "TypeEr...  
      CI: buildkite fail (2 lanes: darwin-14-x64-test-bun, windows-2019-x64-baseline-test-bun; all other platforms pass)
- [ ] [#31505](https://github.com/oven-sh/bun/pull/31505) **node:zlib: reject invalid brotli flush values at validation** — @alii  
      _critical-bug_: Process crash: "Brotli streams aborted the whole process (an uncatchable unreachable!() panic, in release too)" on a zlib flush value.  
      CI: buildkite fail (darwin-14-aarch64 expired, all others pass)
- [ ] [#32303](https://github.com/oven-sh/bun/pull/32303) **fix(napi): leak of WTFStringImpl in napi_create_string_latin1** — @robobun  
      _critical-bug_: Memory leak: "Every call to `napi_create_string_latin1` with a non-empty string leaks one `WTFStringImpl`" plus same bug in crypto.random...  
      CI: buildkite summary marked fail but all 75 individual lanes passed
- [ ] [#31788](https://github.com/oven-sh/bun/pull/31788) **http: fix use-after-free when aborting an h2 request after its custom TLS context is evicted** — @robobun  
      _critical-bug_: "Fixes a use-after-free / double-free crash cluster in the HTTP/2 fetch client" with ASAN traces and a crash-reporter cluster.  
      CI: buildkite fail (2 lanes: alpine-x64-baseline, darwin-14-aarch64 test failures; rest pass incl. asan)
- [ ] [#33767](https://github.com/oven-sh/bun/pull/33767) **util.parseArgs: fix segfault with allowNegative and bare "--no-"** — @robobun  
      _critical-bug_: Title says "fix segfault" and body shows "panic(main thread): Segmentation fault at address 0x0" from user-supplied argv input.  
      CI: buildkite fail (2 lanes: darwin-14-x64-test-bun, debian-13-x64-test-bun; all others pass)
- [ ] [#31737](https://github.com/oven-sh/bun/pull/31737) **FileSink: honor the truncate option when opening by path** — @alii  
      _critical-bug_: Data corruption in common file writes: "Overwriting a longer existing file through any FileSink path open left the old file's tail beyond...  
      CI: buildkite fail (3 darwin test lanes: 2 Expired, 1 exit -1 — infra-looking; all linux/alpine/debian/freebsd pass)
- [ ] [#31787](https://github.com/oven-sh/bun/pull/31787) **Don't crash at startup when a standalone executable's embedded graph is corrupted** — @robobun  
      _critical-bug_: "Fixes a startup segfault in standalone (bun build --compile) executables" tied to Sentry issue BUN-2V2F with ~6,300 crash events.  
      CI: buildkite fail (linux-x64-asan-build-rust exit 1, darwin-14-aarch64 expired); all other lanes pass
- [ ] [#32933](https://github.com/oven-sh/bun/pull/32933) **shell: fix $.escape corrupting Latin-1 characters and dropping empty strings** — @robobun  
      _critical-bug_: Fixes data corruption where "$.escape destroys Latin-1 code units" (é becomes U+FFFD), "Interpolating a Latin-1 string duplicates its lea...  
      CI: buildkite fail (top-level only; all 76 individual lanes pass)
- [ ] [#33211](https://github.com/oven-sh/bun/pull/33211) **Fix segfault when a lazy Bun.* getter throws during reification** — @robobun  
      _critical-bug_: Body states "Segmentation fault at address 0x5 in release, UBSAN null member call in a debug build" — segfault when accessing Bun.$ / Bun...  
      CI: buildkite fail (aggregate only; all 74 individual lanes pass)
- [ ] [#31988](https://github.com/oven-sh/bun/pull/31988) **Fix segfault printing an uncaught AggregateError with a tampered errors property** — @robobun  
      _critical-bug_: Body states "bun segfaults inside the uncaught-exception printer" with "panic(main thread): Segmentation fault at address 0x5" when an Ag...  
      CI: buildkite fail (all failures are Expired infra jobs; completed test lanes passed)
- [ ] [#33015](https://github.com/oven-sh/bun/pull/33015) **fix(windows): segfault in uv__wake_all_loops after system resume** — @robobun  
      _critical-bug_: Fixes a Windows segfault with "1646 Sentry events" where "the loop is freed anyway and stays registered in uv__loops[]" — a use-after-fre...  
      CI: buildkite fail (aggregate only; all 74 individual lanes pass)
- [ ] [#31515](https://github.com/oven-sh/bun/pull/31515) **blob: copy the source slice window in Bun.write(file, file.slice()), not the whole file** — @alii  
      _critical-bug_: Data corruption: "Bun.write(dst, Bun.file(src).slice(a, b)) wrote the entire source file instead of the slice".  
      CI: buildkite fail (only darwin-14-x64-test-bun lane failed, all others pass)
- [ ] [#33009](https://github.com/oven-sh/bun/pull/33009) **process: fill the nextTick ring buffer so a numeric Array.prototype index cannot corrupt it** — @robobun  
      _critical-bug_: Prototype pollution on Array breaks process.nextTick with "a permanent hang: the nextTick queue never drains again for the rest of the pr...  
      CI: buildkite fail (2 darwin-aarch64 test lanes only; all linux/windows/darwin-x64 pass)
- [ ] [#31735](https://github.com/oven-sh/bun/pull/31735) **node:path: fix win32 drive-relative resolve on non-Windows hosts** — @alii  
      _critical-bug_: Fixes a panic: "panicked (index out of bounds) when the process cwd was `/`" plus drive-letter corruption in path.win32.resolve.  
      CI: buildkite fail (darwin-14-aarch64 expired; all other lanes pass)
- [ ] [#32199](https://github.com/oven-sh/bun/pull/32199) **node:fs: fix GC root leak for Buffer path arguments in async ops** — @robobun  
      _critical-bug_: Every async fs op with a Buffer path "leaked one GC root ... permanently pinning the path buffer" — a memory leak surviving Bun.gc(true).  
      CI: buildkite fail (only darwin-26-aarch64-test-bun lane failed, all other lanes pass)
- [ ] [#30598](https://github.com/oven-sh/bun/pull/30598) **bun test: interrupt synchronous infinite loops on --timeout** — @robobun  
      _critical-bug_: Fixes hang: "bun test --timeout=1000 foo.test.ts # hangs forever" when test body is a synchronous infinite loop.  
      CI: buildkite pass (Build #64133 passed)
- [ ] [#30920](https://github.com/oven-sh/bun/pull/30920) **fs: fix fd leak in ReadStream._destroy for { start, autoClose } (dead kReadStreamFastPath branch)** — @robobun  
      _critical-bug_: Fixes fd leak where "_destroy registers a one-shot listener that never fires: the destroy callback never runs, 'close' is never emitted, ...  
      CI: buildkite pass (Build #63311 passed)
- [ ] [#32098](https://github.com/oven-sh/bun/pull/32098) **Fix use-after-free in Error.appendStackTrace when source is destination** — @robobun  
      _critical-bug_: Fixes "a use-after-free found by fuzzing" with "AddressSanitizer: heap-use-after-free" that corrupts memory when Error.appendStackTrace i...  
      CI: buildkite pass
- [ ] [#31739](https://github.com/oven-sh/bun/pull/31739) **Stream network bodies to disk in Bun.write instead of buffering them** — @alii  
      _critical-bug_: Fixes case where "the buffered wait could deadlock and the returned promise never settled" plus full-body buffering that "grew RSS by ~1....  
      CI: buildkite fail (darwin-14-aarch64 test expired/infra, cargo clippy fail); all other test lanes pass
- [ ] [#31336](https://github.com/oven-sh/bun/pull/31336) **node:zlib: defer params() and reject init() while a write is in flight** — @Jarred-Sumner  
      _critical-bug_: Data race: params()/init() "could mutate the native compression state from the JS thread while the thread pool was still processing a wri...  
      CI: buildkite aggregate marked fail but all 75 individual lanes pass (0 lane failures)
- [ ] [#30997](https://github.com/oven-sh/bun/pull/30997) **server: don't send a fallback content-type on detached responses** — @alii  
      _critical-bug_: Regression from released Bun ("Released Bun sends no content-type") that "can make a browser download a streamed HTML page instead of ren...  
      CI: buildkite fail (aggregate only; all individual lanes pass)
- [ ] [#31451](https://github.com/oven-sh/bun/pull/31451) **Copy the message string when constructing Error/AggregateError from a ZigString** — @Jarred-Sumner  
      _critical-bug_: Use-after-free: "the JSString backing error.message aliases freed memory by the time user code reads it — a long-standing latent use-afte...  
      CI: buildkite pass (Build #58573 passed)
- [ ] [#30760](https://github.com/oven-sh/bun/pull/30760) **WebKit: fix Set iteration corrupting large-integer keys to INT32_MIN (#30757)** — @robobun  
      _critical-bug_: Fixes silent data corruption in core JS Set/Map where "[...new Set([1751241600000])][0] // got: -2147483648" due to UB cast under LTO rel...  
      CI: buildkite fail (only windows-2019-x64 test lane, exit 0xC000013A/cancelled — looks infra-related)
- [ ] [#29466](https://github.com/oven-sh/bun/pull/29466) **Fix use-after-free when HTMLRewriter document end handler throws** — @robobun _(stale)_  
      _critical-bug_: Fixes a use-after-free found by Fuzzilli: "ASAN use-after-poison in JSRef.deinit → Response.finalize" when an HTMLRewriter end handler th...  
      CI: buildkite fail (~19 jobs Expired/infra; all completed test lanes passed)

---

## 🔄 Needs rebase — 66

### Security (16)

- [ ] [#33191](https://github.com/oven-sh/bun/pull/33191) **node:http/http2 hardening: enforce h2 request pseudo-headers, never report a failed handler as success, and bound/validate the parser paths** — @cirospaciari  
      _security_: Body states "A security review of that branch reported findings" and fixes h2 protocol validation, handler-failure-as-200, and parser bou...  
      CI: buildkite fail (only darwin-14-x64-test-bun lane failed, all others pass)
- [ ] [#33365](https://github.com/oven-sh/bun/pull/33365) **node:tls: make Server.setSecureContext() rebuild the live TLS context** — @robobun  
      _security_: Certificate rotation is a silent no-op: "the server kept serving its original certificate until the process restarted... the first sympto...  
      CI: buildkite fail (8 test lanes across darwin/debian/ubuntu/windows)
- [ ] [#33801](https://github.com/oven-sh/bun/pull/33801) **ws: respect client maxPayload and handshakeTimeout options** — @robobun  
      _security_: Silently dropping maxPayload/handshakeTimeout means "code that relies on these as a memory-DoS guard or stalled-handshake guard... got no...  
      CI: buildkite fail (TypeScript types check + darwin-x64 and darwin-aarch64 test lanes)
- [ ] [#33568](https://github.com/oven-sh/bun/pull/33568) **dns: normalize legacy IPv4 literals with inet_aton semantics** — @robobun  
      _security_: Body explicitly states "Leading-zero-octet ambiguity is the canonical SSRF / allowlist-bypass vector (CVE-2021-29418, CVE-2021-28918)" — ...  
      CI: buildkite fail (1 darwin-26-aarch64 test failure + ~11 expired build jobs; most test lanes pass)
- [ ] [#33061](https://github.com/oven-sh/bun/pull/33061) **node:http: enforce server headersTimeout and requestTimeout** — @Jarred-Sumner  
      _security_: headersTimeout/requestTimeout were stored but never enforced, so "a connection with stalled request headers... or that never sent a byte ...  
      CI: buildkite fail (all test lanes failing across linux/darwin/windows)
- [ ] [#32362](https://github.com/oven-sh/bun/pull/32362) **worker_threads: honor --no-addons in execArgv after value-taking flags** — @robobun  
      _security_: Security-relevant sandbox flag silently ignored: "--no-addons ... never sees it ... so process.dlopen is not disabled"; a Rust-port regre...  
      CI: buildkite fail (only darwin-14-aarch64-test-bun failed, all other lanes pass)
- [ ] [#31585](https://github.com/oven-sh/bun/pull/31585) **archive: contain extraction writes within the destination root** — @Jarred-Sumner  
      _security_: Prevents archive path-traversal via pre-existing symlinks: "extracting an archive containing `pivot/file.txt` into a destination that alr...  
      CI: buildkite pass
- [ ] [#31481](https://github.com/oven-sh/bun/pull/31481) **install: skip symlink entries in extracted tarballs (matching npm) and harden extraction with O_NOFOLLOW** — @alii  
      _security_: Path traversal: a crafted tarball with NFC/NFD-aliased entries "can therefore create a symlink under one normalization and then mkdirat/s...  
      CI: buildkite pass (Build #58660 passed)
- [ ] [#29144](https://github.com/oven-sh/bun/pull/29144) **fix(cron): write macOS launchd logs to ~/Library/Logs, not /tmp** — @alii  
      _security_: Body explicitly cites "/tmp is world-writable and the log path is fully predictable... another local user can pre-create a symlink... (CW...  
      CI: buildkite pass (Build #59257 passed)
- [ ] [#31329](https://github.com/oven-sh/bun/pull/31329) **Only reconstruct file-backed Blobs from in-process structured clone** — @Jarred-Sumner  
      _security_: Deserializing caller-supplied bytes reconstructed path/fd-backed Blobs where "the embedded path or descriptor cannot be assumed to refer ...  
      CI: buildkite fail (darwin-14-aarch64 expired; all other lanes pass)
- [ ] [#31197](https://github.com/oven-sh/bun/pull/31197) **install: preserve empty trustedDependencies across install + lockfile round-trip** — @robobun  
      _security_: With trustedDependencies:[] "packages like bcrypt, electron, and esbuild had their postinstalls run even though the user had asked to tru...  
      CI: buildkite aggregate fail but all individual build/test lanes pass
- [ ] [#31217](https://github.com/oven-sh/bun/pull/31217) **blob: only re-create file-backed Blobs from in-process structured clones** — @Jarred-Sumner  
      _security_: Hardening fix so deserialize()/v8.deserialize()/IPC "no longer re-opens Blob records that reference a file path, an open file descriptor,...  
      CI: buildkite pass (Build #57148 passed)
- [ ] [#31419](https://github.com/oven-sh/bun/pull/31419) **socket: align Bun.connect TLS client verification with node:tls** — @Jarred-Sumner  
      _security_: TLS verification bypass: "Previously socket.authorized was true and the connection proceeded regardless of the verification result and of...  
      CI: buildkite pass (Build #58398 passed)
- [ ] [#31337](https://github.com/oven-sh/bun/pull/31337) **shell: address recursive rm deletions relative to open parent directory fds** — @Jarred-Sumner  
      _security_: Symlink TOCTOU in recursive rm: re-resolving paths per syscall let a swapped-in symlink cause deletions outside the tree; fix verifies de...  
      CI: buildkite pass (Build #57871 passed)
- [ ] [#30681](https://github.com/oven-sh/bun/pull/30681) **Input validation and bounds-checking hardening** — @Jarred-Sumner  
      _security_: Body describes a "Defensive hardening pass across the runtime — tightens input validation and adds bounds checks"; branch is `jarred/secu...  
      CI: buildkite fail (13 test-bun lanes across linux/darwin/windows)
- [ ] [#28837](https://github.com/oven-sh/bun/pull/28837) **tls: derive checkServerIdentity hostname from connect options** — @dylan-conway _(stale)_  
      _security_: Body states it "closes a silent TLS verification gap where cert subject/SAN mismatches were never surfaced" because Bun "skipped the chec...  
      CI: buildkite fail (4 test lanes: alpine-x64-baseline, darwin-13-aarch64, darwin-13-x64, darwin-14-x64)

### Breaking changes (5)

- [ ] [#31831](https://github.com/oven-sh/bun/pull/31831) **process: port Node.js v26.3.0 process compatibility tests and fix the gaps they surface — env exotic-object/TZ semantics, warnings pipeline + CLI flags, uncaught origin/exit codes, execve throw, threadCpuUsage/finalization/loadEnvFile, active-resource tracking, native-module identity (+29 tests)** — @cirospaciari  
      _breaking-change_: Changes user-facing defaults: "`process.env` is now an exotic object" with string coercion on write, new uncaught-exception origin/exit-c...  
      CI: buildkite fail (only darwin-14-x64-test-bun lane failed, all others pass)
- [ ] [#33160](https://github.com/oven-sh/bun/pull/33160) **Remove libuv on Windows** — @dylan-conway  
      _breaking-change_: Replaces the entire Windows event-loop backend and "JS-visible file descriptors are backed by Bun's own fd table ... instead of the C run...  
      CI: buildkite fail (12 test lanes across darwin/debian/ubuntu/windows + cargo clippy)
- [ ] [#33724](https://github.com/oven-sh/bun/pull/33724) **node:net: default to Nagle on (TCP_NODELAY=0) and make setNoDelay(false) reach the kernel** — @robobun  
      _breaking-change_: Flips the default TCP_NODELAY from 1 to 0 so "Wire behaviour silently differs from Node for every small-write workload that relies on the...  
      CI: buildkite fail (darwin-14 aarch64+x64 test-bun only; all linux/alpine/debian/darwin-26/windows pass)
- [ ] [#31818](https://github.com/oven-sh/bun/pull/31818) **Upgrade reported Node.js version to 26.3.0** — @Jarred-Sumner  
      _breaking-change_: Bumps reported Node version / NODE_MODULE_VERSION and ships upstream semver-major changes: "`read()` one-chunk semantics in paused mode (...  
      CI: buildkite fail (darwin aarch64/x64, darwin-26, windows-aarch64 test lanes + windows-x64-baseline-verify)
- [ ] [#19637](https://github.com/oven-sh/bun/pull/19637) **`bunx`: trust packages** — @dylan-conway _(stale)_  
      _breaking-change_: Changes a user-facing default — "Scripts should be allowed to run by default" for bunx, altering postinstall-script trust behavior, which...  
      CI: buildkite fail (3 darwin test lanes: darwin-13-x64, darwin-14-aarch64, darwin-14-x64)

### Node compat regressions (2)

- [ ] [#32250](https://github.com/oven-sh/bun/pull/32250) **worker_threads: accept SHARE_ENV instead of throwing** — @robobun  
      _node-compat-regression_: Body states "This was a regression introduced by #18758 (the throw did not exist in v1.2.2)" — SHARE_ENV worked before and now throws ERR...  
      CI: buildkite fail (only darwin-14-aarch64 exit -1; all other lanes pass)
- [ ] [#29167](https://github.com/oven-sh/bun/pull/29167) **fetch: support getReader({ mode: 'byob' }) on response bodies** — @robobun _(stale)_  
      _node-compat-regression_: Body states "They used to be byte streams until 1.1.44, but were downgraded" and "Node.js on the same snippet reads the body... Bun threw...  
      CI: buildkite fail (only 2 darwin-aarch64 test lanes Expired; all other lanes pass)

### Performance regressions (3)

- [ ] [#32375](https://github.com/oven-sh/bun/pull/32375) **Bun.Transpiler: stop deep-cloning macro_map/replace_exports per transform() call** — @robobun  
      _perf-regression_: Explicit port-introduced perf regression (branch 'transpiler-clone-regression'): "The Zig implementation shared the backing storage... so...  
      CI: buildkite pass (Build #62746)
- [ ] [#31764](https://github.com/oven-sh/bun/pull/31764) **fs: batch small WriteStream writes via `_writev` and handle IOV_MAX** — @robobun  
      _perf-regression_: "Fixes #21252 — that issue bisected a 1.1.x→1.2.0 regression in line-by-line writes to fs.createWriteStream (~7x slower)"; restores batch...  
      CI: buildkite pass
- [ ] [#18798](https://github.com/oven-sh/bun/pull/18798) **Fix performance regression in node:http from #18599** — @Jarred-Sumner _(stale)_  
      _perf-regression_: Title and body explicitly state "Fix performance regression in node:http from #18599", indicating a measurable slowdown introduced by a p...  
      CI: no buildkite run (only Graphite AI Reviewer, skipped)

### Critical bugs (crash / UAF / leak / corruption) (40)

- [ ] [#31833](https://github.com/oven-sh/bun/pull/31833) **test: run the full ported Node suite leak-clean under the ASAN runner and fix the bugs it surfaces — subprocess fd double-close, console/Strong-handle teardown UAFs, SAN/scope-function/valkey leaks, post-exit child_process streams, runner tmpdir isolation (2322 tests green)** — @cirospaciari  
      _critical-bug_: Explicitly fixes "Subprocess stdio fd double-close" (EBADF/SIGTRAP), "Strong handles released after VM teardown (heap-use-after-free ×2)"...  
      CI: buildkite fail (only darwin-14-x64-test-bun failed, all other lanes pass)
- [ ] [#30609](https://github.com/oven-sh/bun/pull/30609) **fix(formdata): multipart parser corrupts parts located past 4 GiB** — @robobun  
      _critical-bug_: Data corruption: "req.formData() on a multipart body larger than 4 GiB returns corrupted field names/filenames" due to u32 offset wrap.  
      CI: buildkite fail (alpine-x64, alpine-x64-baseline, darwin-14-x64 test lanes)
- [ ] [#31245](https://github.com/oven-sh/bun/pull/31245) **install: synchronize trusted dependency lookups in the isolated installer** — @Jarred-Sumner  
      _critical-bug_: Fixes data race where concurrent task threads read trusted_dependencies while "an insert can reallocate the map's backing storage out fro...  
      CI: buildkite fail (aggregate only; all individual lanes pass)
- [ ] [#32217](https://github.com/oven-sh/bun/pull/32217) **Fix segfault in jsc.startSamplingProfiler(directory)** — @robobun  
      _critical-bug_: "Segfaults 100% of the time on every platform" — write to read-only page plus dangling pointer in startSamplingProfiler.  
      CI: buildkite fail (only darwin-14-x64-test-bun lane failed, all other lanes pass)
- [ ] [#30644](https://github.com/oven-sh/bun/pull/30644) **watcher: fix use-after-free and thread leak when deinit runs right after start** — @robobun  
      _critical-bug_: Title and body explicitly fix a "Use-after-free (the Windows crash)" plus "Thread leak (all platforms)" causing a segfault in the watcher.  
      CI: buildkite pass (Build #68281 passed)
- [ ] [#31940](https://github.com/oven-sh/bun/pull/31940) **Fix Windows use-after-free of the pipe reader buffer retained by in-flight libuv reads** — @robobun  
      _critical-bug_: Fixes "the dominant Windows crash family in Sentry: SEGV during full GC... 8k+ events" caused by a use-after-free of the libuv pipe reade...  
      CI: buildkite fail (aggregate only; all individual lanes incl. Windows pass)
- [ ] [#30028](https://github.com/oven-sh/bun/pull/30028) **usockets(win): defer READABLE re-arm past on_open so .end() mid-handshake doesn't hang** — @Jarred-Sumner  
      _critical-bug_: Calling .end() on a TLS socket from its open handler "hung the process on Windows" because uv_close "wedges the handle (close_cb never fi...  
      CI: buildkite fail (5 test lanes: darwin-14 aarch64/x64, darwin-26 aarch64, windows-2019 x64/x64-baseline)
- [ ] [#32443](https://github.com/oven-sh/bun/pull/32443) **watcher: don't free the Watcher in the spawn-to-start window** — @cirospaciari  
      _critical-bug_: Fixes "AddressSanitizer: heap-use-after-free" race in Watcher::shutdown() where the struct is freed before the spawned thread writes to it.  
      CI: buildkite fail (only darwin-14-aarch64 test lane, all others pass)
- [ ] [#31674](https://github.com/oven-sh/bun/pull/31674) **Take the native blob path for Response-wrapped Bun.file() streams** — @alii  
      _critical-bug_: Body shows wrapping a sliced file stream in a Response "stalls until idleTimeout, then resets the connection" and "never resolves" on rel...  
      CI: buildkite fail (darwin-14-aarch64 test fail, android-verify-baseline expired)
- [ ] [#31856](https://github.com/oven-sh/bun/pull/31856) **Bundler ParseTask ownership, socket adopt, and watcher thread handoff fixes** — @Jarred-Sumner  
      _critical-bug_: Fixes use-after-free hazards: socket adopt could leave "the caller's reference pointing at a freed allocation" and "three latent-UAF-shap...  
      CI: buildkite fail (cargo clippy, windows-aarch64 test, windows-x64-baseline verify, 2 darwin lanes expired)
- [ ] [#31837](https://github.com/oven-sh/bun/pull/31837) **timers: root the event-loop-delay histogram, fix aliased-&mut in timer drain, arm uv timer on the owning VM's loop** — @Jarred-Sumner  
      _critical-bug_: Fixes unrooted JSValue where "the timer fire path could record into a collected object", aliased-&mut UB in timer drain, and Windows uv t...  
      CI: buildkite fail (cargo clippy, windows-x64-baseline verify, alpine/ubuntu x64 tests, darwin expired)
- [ ] [#31488](https://github.com/oven-sh/bun/pull/31488) **http: bind HPACK decode results to the decoder's lifetime** — @Jarred-Sumner  
      _critical-bug_: Memory safety: HPACK decode returned slices into a thread-local buffer so "the slices silently went stale after the next decode/encode ca...  
      CI: buildkite fail (only darwin-14-aarch64 lane expired; all other lanes pass)
- [ ] [#28114](https://github.com/oven-sh/bun/pull/28114) **ws: support upgrade and unexpected-response events** — @alii  
      _critical-bug_: Described as "the load-bearing fix for miniflare/wrangler hanging" — fixes a hang in widely-used tooling.  
      CI: buildkite fail (aggregate only; all individual lanes pass)
- [ ] [#31522](https://github.com/oven-sh/bun/pull/31522) **zlib: decode all members of a concatenated gzip stream, not just the first** — @alii  
      _critical-bug_: Silent data corruption: concatenated gzip "was silently truncated to the first member by Bun.gunzipSync and by fetch()... HTTP 200, no er...  
      CI: buildkite fail (2 darwin test lanes: darwin-14-x64, darwin-26-aarch64; all linux/alpine/debian/ubuntu pass)
- [ ] [#31206](https://github.com/oven-sh/bun/pull/31206) **node:zlib: pin _writeState buffer so transfer() can't free it mid-work** — @alii  
      _critical-bug_: Use-after-free where _writeState.buffer.transfer(0) "frees the storage under the cached pointer (debug: assertion/UAF; release: silent co...  
      CI: buildkite fail (darwin-14-aarch64/x64 + darwin-26-aarch64 test-bun, windows-x64-baseline verify-baseline)
- [ ] [#31517](https://github.com/oven-sh/bun/pull/31517) **spawn: enforce maxBuffer on streaming stdout/stderr reads** — @alii  
      _critical-bug_: Regression from 1.3.14 where "maxBuffer was silently ignored... the runaway child was never killed and its full output was buffered" — un...  
      CI: buildkite fail (3 darwin test lanes: darwin-14-aarch64/x64, darwin-26-aarch64)
- [ ] [#31209](https://github.com/oven-sh/bun/pull/31209) **mysql: compute civil date from day count so pre-1970 Date params don't abort** — @alii  
      _critical-bug_: Binding a pre-1970 Date to a MySQL prepared-statement "aborted the whole process" via an uncatchable panic in release builds.  
      CI: buildkite fail (3 darwin test lanes: darwin-14-aarch64, darwin-14-x64, darwin-26-aarch64)
- [ ] [#31626](https://github.com/oven-sh/bun/pull/31626) **Fix node:http2 and worker_threads crashes found by extended Node test suite runs** — @Jarred-Sumner  
      _critical-bug_: Title/body explicitly say it "Fixes three crashes" (assertions/exception-check trips) in node:http2 and worker_threads paths.  
      CI: buildkite fail (3 darwin test lanes; all linux/windows/asan pass)
- [ ] [#31524](https://github.com/oven-sh/bun/pull/31524) **napi: report the real byteOffset from napi_get_typedarray_info/get_dataview_info** — @alii  
      _critical-bug_: N-API returned byte_offset=0 always, so "a native addon doing the canonical zero-copy reconstruction `arraybuffer.Data() + byte_offset` r...  
      CI: buildkite pass (Build #59555 passed)
- [ ] [#28342](https://github.com/oven-sh/bun/pull/28342) **Percent-encode unix socket paths in server.url** — @alii  
      _critical-bug_: Fixes "before: crash (or throw after #28309)" when server.url is accessed for unix sockets with special characters.  
      CI: buildkite pass (Build #59191 passed)
- [ ] [#29148](https://github.com/oven-sh/bun/pull/29148) **fix(cron): cap macOS launchd calendar-interval product at 256** — @alii  
      _critical-bug_: "A 24-byte input... produced ~537,372 <dict> blocks ≈ 130 MB plist (≈260 MB peak)" — uncapped memory blowup from tiny input handed to lau...  
      CI: buildkite fail (only windows-x64-baseline-verify-baseline, unrelated)
- [ ] [#31208](https://github.com/oven-sh/bun/pull/31208) **FileSystemRouter: keep the percent-decoded path buffer alive for the matched route** — @alii  
      _critical-bug_: Use-after-free: reading .query/.pathname/.params on any URL containing '%' "read freed memory"; ASAN confirms "heap-use-after-free".  
      CI: buildkite pass (Build #59180 passed)
- [ ] [#28338](https://github.com/oven-sh/bun/pull/28338) **Use direct property lookup instead of HashSet in FormData/CookieMap toJSON** — @alii  
      _critical-bug_: Fixes "a crash in FormData.toJSON()" that "crashed with SIGSEGV" when a numeric key is appended twice.  
      CI: buildkite fail (aggregate only; all individual test lanes pass)
- [ ] [#31487](https://github.com/oven-sh/bun/pull/31487) **webcore: make object URL blob stores safe to share across workers** — @Jarred-Sumner  
      _critical-bug_: Cross-thread data race: the shared ObjectURLRegistry handed the same mutable Blob store to multiple worker threads, so "a blob resolved i...  
      CI: buildkite pass (build #58552)
- [ ] [#31359](https://github.com/oven-sh/bun/pull/31359) **node:http2: release completed streams from the session map** — @Jarred-Sumner  
      _critical-bug_: Memory leak: closed HTTP/2 streams "were retained in the native session map" so a long-lived connection "accumulated one retained stream ...  
      CI: buildkite pass (Build #58765 passed)
- [ ] [#31364](https://github.com/oven-sh/bun/pull/31364) **tls: defer upgraded-duplex teardown to the owning context's close task** — @Jarred-Sumner  
      _critical-bug_: Use-after-free: teardown ran "with wrapper frames still on the stack below it that re-read the wrapper's fields after it returns" (ASAN S...  
      CI: buildkite fail (darwin-14-aarch64 job expired, all other lanes pass)
- [ ] [#31583](https://github.com/oven-sh/bun/pull/31583) **htmlrewriter: release end-tag handler resources after each rewrite** — @Jarred-Sumner  
      _critical-bug_: Fixes an unbounded memory leak: "Memory therefore grew with every matching element, across every rewrite, for as long as the process live...  
      CI: buildkite pass
- [ ] [#31011](https://github.com/oven-sh/bun/pull/31011) **net: a /0 BlockList subnet matches all instead of overflow-panicking** — @alii  
      _critical-bug_: Fixes shift-overflow panics where a /0 subnet "aborted the debug build... crashes on the first incoming connection, uncatchable" and "a g...  
      CI: buildkite fail (aggregate only; all individual build/test lanes pass)
- [ ] [#30995](https://github.com/oven-sh/bun/pull/30995) **streams: truncate chunk size to i51/i52 before clamping** — @alii  
      _critical-bug_: Fixes process abort: "aborted the process (SIGABRT — Vec::reserve_exact → handle_alloc_error)"; regressed vs "Released Bun wraps the valu...  
      CI: buildkite pass (Build #56846 passed)
- [ ] [#29928](https://github.com/oven-sh/bun/pull/29928) **windows: flush all loaded CRTs then TerminateProcess on exit** — @Jarred-Sumner _(stale)_  
      _critical-bug_: Windows ExitProcess is "a recurring crash source when third-party DLLs fault during teardown" (rspack, skia-canvas, AV hooks); also fixes...  
      CI: buildkite fail (most build lanes expired)
- [ ] [#29739](https://github.com/oven-sh/bun/pull/29739) **runtime: exit 13 on unsettled top-level await instead of hanging** — @dylan-conway _(stale)_  
      _critical-bug_: Fixes process that would "hang at 100% CPU" / "busy-spinning forever" on unsettled top-level await instead of exiting like Node.  
      CI: buildkite fail (many build-cpp/build-zig lanes Expired; darwin tests passed)
- [ ] [#28289](https://github.com/oven-sh/bun/pull/28289) **fix(net): server socket loses data when 'data' listener is added asynchronously** — @dylan-conway _(stale)_  
      _critical-bug_: Fixes "a data-loss race in net.createServer" that causes "silently dropping bytes" and stalls in TCP proxy patterns.  
      CI: buildkite fail (only darwin-13-aarch64 test lane; all other lanes pass)
- [ ] [#27323](https://github.com/oven-sh/bun/pull/27323) **fix: prevent double-fulfill when import() and require() race on same ESM module** — @dylan-conway _(stale)_  
      _critical-bug_: Fixes "ASSERT(status() == Status::Pending) crash in JSPromise::fulfillPromise" when import() and require() race.  
      CI: buildkite fail (16 test-bun lanes failing across all platforms: alpine/darwin/debian/ubuntu/windows)
- [ ] [#15047](https://github.com/oven-sh/bun/pull/15047) **Fix tagged template literal with unicode** — @pfgithub _(stale)_  
      _critical-bug_: Fixes data corruption in core JS feature where String.raw/regex with unicode output escaped sequences instead of actual chars ("\u00E6\u2...  
      CI: buildkite fail (all darwin test lanes + windows-2019-x64)
- [ ] [#26293](https://github.com/oven-sh/bun/pull/26293) **fix(worker_threads): use MessagePort for parentPort instead of dispatching to self** — @dylan-conway _(stale)_  
      _critical-bug_: Body states Bun "dispatched messages to both `self.onmessage` AND `parentPort`... causing handlers to run twice" which "broke fflate's as...  
      CI: buildkite fail (darwin-13-x64 + darwin-14-aarch64 test lanes only; all other lanes pass)
- [ ] [#24914](https://github.com/oven-sh/bun/pull/24914) **Fix websocket CPU usage issue?** — @Jarred-Sumner _(stale)_  
      _critical-bug_: Title indicates a "websocket CPU usage issue" — excessive CPU/busy-loop in WebSocket is a resource-exhaustion/hang-class bug affecting co...  
      CI: buildkite fail (all 4 darwin test lanes failed; linux/windows/alpine pass)
- [ ] [#23987](https://github.com/oven-sh/bun/pull/23987) **Fix kqueue readable & writable check** — @Jarred-Sumner _(stale)_  
      _critical-bug_: Body says it makes writable one-shot "to avoid spamming the event loop with wakeup events" and fixes "checking writable when it's actuall...  
      CI: buildkite fail (all 4 darwin test lanes + debian-asan + ubuntu-25.04-aarch64)
- [ ] [#22796](https://github.com/oven-sh/bun/pull/22796) **Add missing close call when SO_REUSEPORT is used for UDP socket and there's a conflict** — @Jarred-Sumner _(stale)_  
      _critical-bug_: Fixes a file-descriptor leak: "Add missing close call" on the UDP socket error path when SO_REUSEPORT conflicts, i.e. a resource leak on ...  
      CI: buildkite fail (only darwin-14-aarch64 test lane failed, all others pass)
- [ ] [#20627](https://github.com/oven-sh/bun/pull/20627) **node: fix test-net-connect-memleak.js** — @nektro _(stale)_  
      _critical-bug_: Fixes Node's "test-net-connect-memleak.js", implying a memory leak in net.connect is being addressed.  
      CI: buildkite fail (only darwin-13-aarch64 test lane failed, all others pass)
- [ ] [#19620](https://github.com/oven-sh/bun/pull/19620) **Speculative fix for BUN-JC6** — @Jarred-Sumner _(stale)_  
      _critical-bug_: Targets a crash-reporter issue id ("Speculative fix for BUN-JC6") on branch `bytes-finalize`, indicating a finalizer/memory crash fix.  
      CI: no CI run

---

## 🔴 Mergeable but CI failing (needs investigation) — 20

### Security (9)

- [ ] [#33906](https://github.com/oven-sh/bun/pull/33906) **node:dgram: honor receiveBlockList and sendBlockList options** — @robobun  
      _security_: IP blocklist security control fails open: "filter object was constructed and silently inert: datagrams from blocked sources were delivered".  
      CI: buildkite fail (7 test lanes: debian-13, ubuntu-25.04, windows-x64-baseline)
- [ ] [#33630](https://github.com/oven-sh/bun/pull/33630) **node:tls: abort handshake with fatal alert when server rejects client cert** — @robobun  
      _security_: mTLS server with rejectUnauthorized:true "would let the TLS handshake run to completion" so the client sees secureConnect and a clean clo...  
      CI: buildkite fail (5 test lanes: darwin-x64, debian x64/aarch64/baseline, ubuntu x64)
- [ ] [#33837](https://github.com/oven-sh/bun/pull/33837) **crypto: throw when BN_generate_prime_ex fails in generatePrime** — @robobun  
      _security_: Body says on failure "the caller gets back a full-width random number that looks exactly like a prime but has never been primality-tested...  
      CI: buildkite fail (5 test lanes: alpine aarch64/x64/x64-baseline, darwin-14 aarch64/x64)
- [ ] [#33477](https://github.com/oven-sh/bun/pull/33477) **webcrypto: reject RSA modulusLength values that cannot be generated exactly** — @robobun  
      _security_: Crypto weakness: "a caller with a policy-mandated size such as 2000 bits was getting weaker RSA than it asked for, with no error" — silen...  
      CI: buildkite fail (13 lanes Expired — infra timeout, no real test failures; lanes that ran passed)
- [ ] [#33206](https://github.com/oven-sh/bun/pull/33206) **node:url: return "" from domainToASCII/domainToUnicode for invalid domains** — @robobun  
      _security_: These functions are "the hostname *validation* API" and under Bun "every invalid domain 'succeeds', so allowlist and normalization code t...  
      CI: buildkite fail (all test-bun lanes failing across linux/darwin/windows/alpine)
- [ ] [#32891](https://github.com/oven-sh/bun/pull/32891) **Bun.serve websocket: don't alias pub/sub topics that differ only in lone surrogates** — @robobun  
      _security_: Body states "any application that builds topic names from user-controlled input ... can be made to receive, or have its publishes deliver...  
      CI: buildkite fail (cargo clippy + darwin-14 aarch64/x64 tests)
- [ ] [#32767](https://github.com/oven-sh/bun/pull/32767) **Harden bun.exe against DLL search-order hijacking on Windows** — @robobun  
      _security_: Fixes DLL search-order hijacking where "a DLL named after one of bun's system dependencies, dropped next to bun.exe... is loaded in place...  
      CI: buildkite fail (8 test lanes: alpine/debian/ubuntu/darwin aarch64+x64, windows-aarch64)
- [ ] [#32129](https://github.com/oven-sh/bun/pull/32129) **Fix AES-GCM output when OPENSSL_ia32cap masks AES-NI but not VAES** — @robobun  
      _security_: AES-GCM produces "deterministic garbage ciphertext" whose "tag still verifies" — "silently wrong crypto is the worst possible failure mod...  
      CI: buildkite fail (4 test lanes: darwin-14 aarch64/x64, debian-13 x64-baseline, ubuntu-25.04 x64-baseline)
- [ ] [#31916](https://github.com/oven-sh/bun/pull/31916) **css: budget selector expansion and nesting substitution by bytes** — @robobun  
      _security_: Fixes a fuzzer-found CSS minifier OOM where "30 KB in -> 265,879,552 bytes out, 581 MB RSS" — an attacker-controlled input-amplification ...  
      CI: buildkite fail (all test-bun lanes failing across linux/darwin/windows)

### Breaking changes (3)

- [ ] [#33604](https://github.com/oven-sh/bun/pull/33604) **webcore: File.prototype.slice() returns a plain Blob, not a File** — @robobun  
      _breaking-change_: Changes observable instanceof/name behavior so slices no longer "leak the parent's name onto arbitrary derived fragments"; safest to land...  
      CI: buildkite fail (darwin-14-aarch64 + windows-x64-baseline test lanes) and cargo clippy fail
- [ ] [#32851](https://github.com/oven-sh/bun/pull/32851) **bun build --compile: read NODE_ENV at runtime, set argv[0] to the executable path** — @robobun  
      _breaking-change_: Changes compiled-binary defaults so NODE_ENV is no longer "inlined at build time" and argv[0] is no longer the literal "bun" — observable...  
      CI: buildkite fail (3 test lanes: alpine-x64, darwin-14-aarch64, darwin-14-x64)
- [ ] [#32937](https://github.com/oven-sh/bun/pull/32937) **bun:sqlite: throw TypeError for async transaction callbacks** — @robobun  
      _breaking-change_: Changes db.transaction() to "throw a TypeError if fn is an async function" where it previously "silently accepted" it, fixing "Partial wr...  
      CI: buildkite fail (TypeScript types fail, darwin-26-aarch64 test fail, bun-plugin-svelte fail, many lanes expired)

### Node compat regressions (1)

- [ ] [#33746](https://github.com/oven-sh/bun/pull/33746) **node:zlib: make deflate output independent of write() chunking** — @robobun  
      _node-compat-regression_: Regressed after zlib-ng switch: "Bun links zlib-ng (since #29433)" causing non-deterministic deflate output so "content hashes, ETags, de...  
      CI: buildkite fail (alpine-aarch64, darwin-14-x64, darwin-14-aarch64 test-bun)

### Critical bugs (crash / UAF / leak / corruption) (7)

- [ ] [#33979](https://github.com/oven-sh/bun/pull/33979) **install: don't leak package extraction temp directories into $TMPDIR** — @robobun  
      _critical-bug_: Body states bun install "leaks package-extraction temp directories" ("~25 GB over 40 hours") and on Windows the race "deleted [the cache ...  
      CI: buildkite fail (all 3 darwin test lanes failing, linux/alpine pass)
- [ ] [#32172](https://github.com/oven-sh/bun/pull/32172) **Don't leak scope objects through host functions' raw this** — @robobun  
      _critical-bug_: Fixes "a fuzzer-found segfault (Fuzzilli fingerprint 07bc0d3cfacec7ff, SIGSEGV in baseline JIT code)" where leaked JSLexicalEnvironment l...  
      CI: buildkite fail (11 test lanes across darwin/debian/ubuntu/windows)
- [ ] [#33018](https://github.com/oven-sh/bun/pull/33018) **Fix Windows heap corruption when a socket's data callback closes it and re-enters the event loop** — @robobun  
      _critical-bug_: Fixes a reproducible "STATUS_HEAP_CORRUPTION (exit 0xC0000374)" crash / use-after-free in the Windows libuv event loop path.  
      CI: buildkite fail (8 test lanes across darwin, debian, ubuntu, windows)
- [ ] [#33289](https://github.com/oven-sh/bun/pull/33289) **bun:test: settle async matchers with promise reactions instead of re-entering the event loop** — @alii  
      _critical-bug_: Fixes deadlock in bun:test: "`expect(p).resolves.toBe(25); resolve(25)` deadlocked at 100% CPU until the test timeout" and "used to hang ...  
      CI: buildkite fail (all 16 test-bun lanes across linux/darwin/windows + cargo clippy)
- [ ] [#33197](https://github.com/oven-sh/bun/pull/33197) **watcher: do not leak a file descriptor of the entrypoint on every `bun --hot` reload** — @robobun  
      _critical-bug_: File descriptor leak: "A long editing session eventually exhausts the fd limit and every later open/socket/accept in the process starts f...  
      CI: buildkite fail (all 16 test lanes failing across linux/darwin/windows)
- [ ] [#31951](https://github.com/oven-sh/bun/pull/31951) **Fix use-after-free when a terminated worker has children still starting** — @robobun  
      _critical-bug_: Fixes a use-after-free (ASAN heap-use-after-free) in nested Worker teardown; body states "The first regression test below crashes release...  
      CI: buildkite fail (7 test lanes: alpine x3, darwin-x64, debian-asan, ubuntu-x64-baseline, windows-aarch64)
- [ ] [#27106](https://github.com/oven-sh/bun/pull/27106) **fix(libuv): patch CancelIoEx data loss in Windows pipe reads** — @dylan-conway _(stale)_  
      _critical-bug_: Fixes "intermittent data loss when reading from subprocess stdout pipes on Windows" where "CancelIoEx can discard in-flight data".  
      CI: buildkite fail (14 test lanes failing across linux/darwin/windows)

---

## 📝 Draft (review whether to finish for 1.4) — 6

### Breaking changes (1)

- [ ] [#14936](https://github.com/oven-sh/bun/pull/14936) **Changes expect().resolves.toThrow behaviour to more closely match jest** — @pfgithub _(stale)_  
      _breaking-change_: Explicitly "Changes expect().resolves.toThrow behaviour" so that non-Error values no longer count as throwing, which will cause previousl...  
      CI: buildkite fail (only darwin-14-aarch64 test lane, all others pass)

### Node compat regressions (1)

- [ ] [#32788](https://github.com/oven-sh/bun/pull/32788) **node:http: forward https-proxy-agent constructor TLS options to the tunneled connection** — @cirospaciari  
      _node-compat-regression_: Explicitly "Fixes a regression on main" where "this worked on every released Bun; #31587 dropped that as a side effect of the rewrite" — ...  
      CI: buildkite fail (6 test lanes: darwin-aarch64/x64, darwin-26, debian-x64-baseline, ubuntu-x64, windows-aarch64)

### Performance regressions (1)

- [ ] [#19072](https://github.com/oven-sh/bun/pull/19072) **should setImmediate be this fast?** — @Jarred-Sumner _(stale)_  
      _perf-regression_: Body benchmarks show setImmediate in canary became "4.53 ± 0.17 times" slower than bun-1.2.9, i.e. a measurable perf regression this PR a...  
      CI: no CI run (only Graphite AI Reviewer, skipping)

### Critical bugs (crash / UAF / leak / corruption) (3)

- [ ] [#22328](https://github.com/oven-sh/bun/pull/22328) **Fixes #20514** — @Jarred-Sumner _(stale)_  
      _critical-bug_: Linked issue #20514 is a confirmed crash: "Defining `Object.prototype[\"1\"]` setter causes Bun to panic during env access" (labels: cras...  
      CI: buildkite fail (only darwin-13-aarch64 test lane failed, all others pass)
- [ ] [#20255](https://github.com/oven-sh/bun/pull/20255) **Attempt to fix out of bounds access in onFileDeleted** — @Jarred-Sumner _(stale)_  
      _critical-bug_: Title explicitly states it fixes an "out of bounds access in onFileDeleted", a memory-safety bug that can cause crashes/corruption.  
      CI: buildkite fail (only windows-2019-x64-test-bun lane failed, all other platforms pass)
- [ ] [#18709](https://github.com/oven-sh/bun/pull/18709) **Implement `headersDistinct` and fix `NodeHTTPResponse.getAbort`** — @zackradisic _(stale)_  
      _critical-bug_: Body states it "Fixes null pointer dereference with `NodeHTTPResponse.getAbort`", which is a crash in the node:http server path.  
      CI: no CI run (only Graphite AI Reviewer, skipped)


